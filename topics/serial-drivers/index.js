/**
 * Topic: Serial Drivers
 */
const TOPIC_SERIAL_DRIVERS = {
  "id": "serial-drivers",
  "category": "drivers",
  "icon": "🔌",
  "title": "Serial Drivers",
  "description": "Linux serial/UART & TTY drivers — the TTY layer architecture, uart_driver/uart_port/uart_ops, the TX/RX data path, termios & flow control, and user-space serial programming, with flow charts throughout",
  "keywords": [
    "serial driver", "uart", "tty", "serial", "line discipline", "n_tty",
    "uart_driver", "uart_port", "uart_ops", "termios", "baud", "baud rate",
    "flow control", "rts", "cts", "xon", "xoff", "start_tx", "set_termios",
    "ttyS0", "ttyUSB0", "ttyAMA0", "rs232", "rs485", "stty", "minicom",
    "picocom", "circ_buf", "serial core", "dialout", "8n1"
  ],
  "sections": [
    {
      "title": "1. Serial Communication & the TTY Layer",
      "content": "<p>A <strong>serial</strong> link sends data one bit at a time over a wire. A <strong>UART</strong> (Universal Asynchronous Receiver/Transmitter) is the hardware that serializes bytes into bits (TX) and reassembles received bits into bytes (RX), framed by start/stop bits at an agreed <strong>baud rate</strong>.</p><h4>A UART frame (the classic 8N1)</h4><pre><code># \"8N1\" = 8 data bits, No parity, 1 stop bit\n#\n#  idle   start  b0 b1 b2 b3 b4 b5 b6 b7  stop  idle\n#  ─────┐ ┌──┐    ┌──┐    ┌──┐          ┌───────────\n#       └─┘  └────┘  └────┘  └──────────┘\n#       ▲ falling edge = start of a frame\n#\n# Both ends must agree on baud (e.g. 115200), data bits, parity, stop bits\n# and flow control -- otherwise you get garbage.</code></pre><h4>The TTY layer — the big picture</h4><p>In Linux, serial ports are exposed through the <strong>TTY subsystem</strong>. A driver you write plugs into the bottom of a well-defined stack; the layers above give you line editing, buffering, and the standard <code>/dev/tty*</code> interface for free.</p><pre><code>┌─────────────────────────────────────────────────────────────┐\n│ USER SPACE   open(\"/dev/ttyS0\"); read()/write(); termios      │\n└───────────────────────────────┬─────────────────────────────┘\n                                 │ system call\n══════════════════════════════════▼════════════════════════════ user/kernel\n┌─────────────────────────────────────────────────────────────┐\n│ TTY CORE          the /dev/tty* char devices, buffering        │\n├─────────────────────────────────────────────────────────────┤\n│ LINE DISCIPLINE   (N_TTY) canonical/raw mode, echo, signals    │\n│                   -- turns raw bytes into \"lines\" and back      │\n├─────────────────────────────────────────────────────────────┤\n│ SERIAL CORE       (serial_core.c) generic UART management      │\n├─────────────────────────────────────────────────────────────┤\n│ YOUR UART DRIVER  uart_driver + uart_ops for THIS chip         │\n├─────────────────────────────────────────────────────────────┤\n│ HARDWARE          UART registers / FIFO, TX & RX lines, IRQ    │\n└─────────────────────────────────────────────────────────────┘\n#\n# You implement only the bottom box; serial_core + the tty layer provide\n# everything above. (A serial port is ultimately a character device --\n# see the Character Drivers topic for the cdev fundamentals.)</code></pre>"
    },
    {
      "title": "2. The Layered Architecture",
      "content": "<p>Understanding who does what saves you from re-implementing the wheel — the serial core already handles most of the plumbing.</p><h4>Responsibilities</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Layer</th><th style=\"text-align:left; padding:0.5rem;\">Provided by</th><th style=\"text-align:left; padding:0.5rem;\">Does</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">TTY core</td><td style=\"padding:0.5rem;\">kernel</td><td style=\"padding:0.5rem;\">/dev nodes, open/close, buffering</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Line discipline</td><td style=\"padding:0.5rem;\">kernel (N_TTY)</td><td style=\"padding:0.5rem;\">Canonical vs raw, echo, Ctrl-C→signal</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Serial core</td><td style=\"padding:0.5rem;\">kernel (serial_core)</td><td style=\"padding:0.5rem;\">uart_port mgmt, calls your ops</td></tr><tr><td style=\"padding:0.5rem;\"><strong>UART driver</strong></td><td style=\"padding:0.5rem;\"><strong>you</strong></td><td style=\"padding:0.5rem;\">Touch the actual hardware registers</td></tr></table><h4>Key data structures</h4><pre><code>struct uart_driver   // ONE per driver: name, /dev prefix, major, port array\nstruct uart_port     // ONE per physical port: base address, IRQ, FIFO size\nstruct uart_ops      // the callbacks the serial core invokes on your port\nstruct circ_buf      // the TX ring buffer the core fills; you drain it</code></pre><h4>Data flow through the layers</h4><pre><code># WRITE (down the stack):\n#   write() ─► tty core ─► line discipline ─► serial core ─► YOUR start_tx\n#            ─► pull bytes from the ring buffer ─► UART TX FIFO ─► wire\n#\n# READ (up the stack, driven by a hardware RX interrupt):\n#   wire ─► UART RX FIFO ─► YOUR irq handler reads bytes\n#        ─► uart_insert_char()/tty_flip_buffer_push()\n#        ─► line discipline ─► tty core ─► read() returns to user\n#\n# Note the asymmetry: TX is pull (core hands you a buffer to drain),\n# RX is push (your IRQ hands received bytes UP to the core).</code></pre>"
    },
    {
      "title": "3. Registering a UART Driver",
      "content": "<p>A UART driver registers a <code>uart_driver</code> once, then adds one <code>uart_port</code> per physical port.</p><h4>Declare the driver</h4><pre><code>#include <linux/serial_core.h>\n#include <linux/tty.h>\n\n#define MYUART_MAJOR   204\n#define MYUART_MINOR   0\n#define MYUART_NR      4        // support up to 4 ports\n\nstatic struct uart_driver my_uart_driver = {\n    .owner        = THIS_MODULE,\n    .driver_name  = \"myuart\",\n    .dev_name     = \"ttyMY\",    // -> /dev/ttyMY0, /dev/ttyMY1, ...\n    .major        = MYUART_MAJOR,\n    .minor        = MYUART_MINOR,\n    .nr           = MYUART_NR,\n};</code></pre><h4>Registration flow</h4><pre><code>module_init / probe()\n     │\n     ▼\nuart_register_driver(&my_uart_driver)   ── registers with the tty layer\n     │                                      (reserves the major, dev_name)\n     ▼\nfor each physical port:\n   set up struct uart_port  (membase, irq, uartclk, fifosize, ops)\n     │\n     ▼\nuart_add_one_port(&my_uart_driver, &port)  ── /dev/ttyMY<n> becomes usable\n     │\n     ▼\n(on remove) uart_remove_one_port()  then  uart_unregister_driver()</code></pre><h4>Setting up a port</h4><pre><code>static struct uart_port my_port;\n\nmy_port.iotype   = UPIO_MEM;\nmy_port.membase  = devm_platform_ioremap_resource(pdev, 0); // mapped regs\nmy_port.irq      = platform_get_irq(pdev, 0);\nmy_port.uartclk  = 24000000;          // input clock (for baud divisor)\nmy_port.fifosize = 16;\nmy_port.ops      = &my_uart_ops;      // the callback table (section 4)\nmy_port.line     = 0;                 // port index\nmy_port.dev      = &pdev->dev;\n\nuart_add_one_port(&my_uart_driver, &my_port);</code></pre><h4>Device node naming</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Node</th><th style=\"text-align:left; padding:0.5rem;\">Typical device</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>/dev/ttyS0</code></td><td style=\"padding:0.5rem;\">Standard PC 16550 UART</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>/dev/ttyUSB0</code></td><td style=\"padding:0.5rem;\">USB-serial adapter (FTDI, CP210x)</td></tr><tr><td style=\"padding:0.5rem;\"><code>/dev/ttyAMA0</code></td><td style=\"padding:0.5rem;\">ARM PL011 (e.g. Raspberry Pi)</td></tr></table>"
    },
    {
      "title": "4. The uart_ops Callbacks",
      "content": "<p><code>struct uart_ops</code> is the contract between the serial core and your hardware. The core calls these; you translate them into register accesses.</p><h4>The core callbacks</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Callback</th><th style=\"text-align:left; padding:0.5rem;\">Must do</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>startup</code></td><td style=\"padding:0.5rem;\">Request IRQ, enable RX, power up the port (on open)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>shutdown</code></td><td style=\"padding:0.5rem;\">Disable IRQs, free IRQ (on last close)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>set_termios</code></td><td style=\"padding:0.5rem;\">Apply baud, data/stop/parity, flow control</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>start_tx</code></td><td style=\"padding:0.5rem;\">Begin/continue transmitting (enable TX IRQ)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>stop_tx</code></td><td style=\"padding:0.5rem;\">Stop transmitting (disable TX IRQ)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>stop_rx</code></td><td style=\"padding:0.5rem;\">Stop receiving</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>tx_empty</code></td><td style=\"padding:0.5rem;\">Return TIOCSER_TEMT if TX FIFO/shifter is empty</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>set_mctrl</code>/<code>get_mctrl</code></td><td style=\"padding:0.5rem;\">Modem control lines (RTS/DTR)</td></tr><tr><td style=\"padding:0.5rem;\"><code>type</code>/<code>config_port</code></td><td style=\"padding:0.5rem;\">Identify/claim the port resources</td></tr></table><h4>The ops table</h4><pre><code>static const struct uart_ops my_uart_ops = {\n    .startup      = my_startup,\n    .shutdown     = my_shutdown,\n    .set_termios  = my_set_termios,\n    .start_tx     = my_start_tx,\n    .stop_tx      = my_stop_tx,\n    .stop_rx      = my_stop_rx,\n    .tx_empty     = my_tx_empty,\n    .set_mctrl    = my_set_mctrl,\n    .get_mctrl    = my_get_mctrl,\n    .type         = my_type,\n    .config_port  = my_config_port,\n};</code></pre><h4>startup — the open path</h4><pre><code>startup() called when the port is first opened\n     │\n     ▼\nrequest_irq(port->irq, my_isr, ...)   ── hook the UART interrupt\n     │\n     ▼\nenable RX interrupts in the hardware control register\n     │\n     ▼\nreturn 0  ── the device is now live; reads/writes will flow</code></pre>"
    },
    {
      "title": "5. The TX & RX Data Path",
      "content": "<p>This is the heart of a UART driver: draining the transmit ring buffer, and pushing received bytes up to the TTY layer. Both are driven by the UART interrupt.</p><h4>TX flow (transmit)</h4><pre><code>user: write(fd, \"hi\", 2)\n   │\n   ▼\ntty/serial core fills the port's circular buffer, then calls .start_tx\n   │\n   ▼\nmy_start_tx()  ── enable the TX-empty interrupt\n   │\n   ▼  (UART TX FIFO has room -> fires TX interrupt)\nmy_isr(): while FIFO not full AND buffer not empty:\n            write next byte to the UART data register\n            advance the ring buffer tail\n   │\n   ▼\nbuffer empty ─► stop_tx() (disable TX IRQ)   |   more data ─► keep going</code></pre><pre><code>// TX portion of the interrupt handler (simplified)\nstatic void my_tx_chars(struct uart_port *port)\n{\n    struct tty_port *tport = &port->state->port;\n    unsigned char ch;\n\n    while (uart_fifo_has_room(port)) {\n        if (!uart_fifo_get(port, &ch))   // pull one byte from the ring buffer\n            break;                       // (returns false when empty)\n        writeb(ch, port->membase + UART_TX_REG);\n        port->icount.tx++;\n    }\n    if (uart_tx_stopped(port) || kfifo_is_empty(&tport->xmit_fifo))\n        my_stop_tx(port);                // nothing left -> stop TX IRQ\n}</code></pre><h4>RX flow (receive)</h4><pre><code>wire ─► UART RX FIFO fills ─► RX interrupt fires\n   │\n   ▼\nmy_isr(): while RX FIFO not empty:\n            ch = read UART data register\n            check error flags (parity/frame/overrun)\n            uart_insert_char(port, status, overrun, ch, TTY_NORMAL)\n   │\n   ▼\ntty_flip_buffer_push(&port->state->port)  ── hand bytes UP to the tty layer\n   │\n   ▼\nline discipline ─► tty core ─► user's read() returns the bytes</code></pre><pre><code>static void my_rx_chars(struct uart_port *port)\n{\n    unsigned char ch;\n    while (uart_rx_ready(port)) {\n        ch = readb(port->membase + UART_RX_REG);\n        port->icount.rx++;\n        uart_insert_char(port, 0, 0, ch, TTY_NORMAL);\n    }\n    tty_flip_buffer_push(&port->state->port);\n}\n\n// The single IRQ handler dispatches to both paths:\nstatic irqreturn_t my_isr(int irq, void *dev_id)\n{\n    struct uart_port *port = dev_id;\n    u32 status = readl(port->membase + UART_STATUS);\n    if (status & RX_READY) my_rx_chars(port);\n    if (status & TX_EMPTY) my_tx_chars(port);\n    return IRQ_HANDLED;\n}</code></pre>"
    },
    {
      "title": "6. termios: Baud, Line Settings & Flow Control",
      "content": "<p>The line configuration (speed, framing, flow control) is expressed by a <code>termios</code> structure. The core calls your <code>set_termios</code> whenever user space changes settings.</p><h4>set_termios flow</h4><pre><code>user: stty -F /dev/ttyMY0 115200 cs8 -parenb  (or tcsetattr() in C)\n   │\n   ▼\nserial core computes the requested baud, byte size, parity, stop bits\n   │\n   ▼\nmy_set_termios(port, new, old)\n   │  1. baud = uart_get_baud_rate(port, new, old, min, max)\n   │  2. divisor = port->uartclk / (16 * baud)\n   │  3. program divisor + framing bits into UART registers\n   │  4. configure hardware flow control if CRTSCTS set\n   ▼\nnew line settings are now in effect</code></pre><pre><code>static void my_set_termios(struct uart_port *port, struct ktermios *new,\n                           const struct ktermios *old)\n{\n    unsigned int baud, divisor, lcr = 0;\n    unsigned long flags;\n\n    baud    = uart_get_baud_rate(port, new, old, 0, port->uartclk / 16);\n    divisor = DIV_ROUND_CLOSEST(port->uartclk, 16 * baud);\n\n    // Data bits\n    switch (new->c_cflag & CSIZE) {\n    case CS8: lcr |= LCR_8BIT; break;\n    case CS7: lcr |= LCR_7BIT; break;\n    }\n    if (new->c_cflag & PARENB) lcr |= LCR_PARITY;   // parity on\n    if (new->c_cflag & CSTOPB) lcr |= LCR_2STOP;    // 2 stop bits\n\n    spin_lock_irqsave(&port->lock, flags);\n    writeb(divisor & 0xff, port->membase + UART_DLL);\n    writeb(divisor >> 8,   port->membase + UART_DLM);\n    writeb(lcr,            port->membase + UART_LCR);\n    uart_update_timeout(port, new->c_cflag, baud);\n    spin_unlock_irqrestore(&port->lock, flags);\n}</code></pre><h4>Flow control</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Type</th><th style=\"text-align:left; padding:0.5rem;\">Mechanism</th><th style=\"text-align:left; padding:0.5rem;\">termios flag</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Hardware</td><td style=\"padding:0.5rem;\">RTS/CTS wires</td><td style=\"padding:0.5rem;\"><code>CRTSCTS</code></td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Software</td><td style=\"padding:0.5rem;\">XON/XOFF bytes (0x11/0x13)</td><td style=\"padding:0.5rem;\"><code>IXON</code>/<code>IXOFF</code></td></tr><tr><td style=\"padding:0.5rem;\">None</td><td style=\"padding:0.5rem;\">Sender must not overrun receiver</td><td style=\"padding:0.5rem;\">(neither set)</td></tr></table>"
    },
    {
      "title": "7. User-Space Serial Programming",
      "content": "<p>From an application, a serial port is just a file — you <code>open()</code> it, set the line parameters with termios, then <code>read()</code>/<code>write()</code>.</p><h4>Command-line tools</h4><pre><code>ls -l /dev/ttyUSB0            # crw-rw---- root dialout  (need 'dialout' group)\nsudo usermod -aG dialout $USER   # so you can use it without sudo (re-login)\n\nstty -F /dev/ttyUSB0 115200 cs8 -cstopb -parenb raw   # configure the line\nstty -F /dev/ttyUSB0 -a       # show ALL current settings\n\n# Interactive terminals over serial:\npicocom -b 115200 /dev/ttyUSB0\nminicom -D /dev/ttyUSB0 -b 115200\nscreen /dev/ttyUSB0 115200    # exit: Ctrl-a k\n\n# Quick one-shot send/receive:\necho -ne 'AT\\r' > /dev/ttyUSB0\ncat /dev/ttyUSB0</code></pre><h4>Opening and configuring in C</h4><pre><code>#include <termios.h>\n#include <fcntl.h>\n#include <unistd.h>\n\nint fd = open(\"/dev/ttyUSB0\", O_RDWR | O_NOCTTY);\nif (fd < 0) { perror(\"open\"); return 1; }\n\nstruct termios tty;\ntcgetattr(fd, &tty);                 // read current settings\n\ncfsetispeed(&tty, B115200);          // input baud\ncfsetospeed(&tty, B115200);          // output baud\ncfmakeraw(&tty);                     // RAW mode: no line editing/echo\ntty.c_cflag |= (CLOCAL | CREAD);     // ignore modem lines, enable receiver\ntty.c_cflag &= ~CRTSCTS;             // no hardware flow control\ntty.c_cc[VMIN]  = 0;                 // read() returns...\ntty.c_cc[VTIME] = 10;                // ...after 1.0s timeout if no data\n\ntcsetattr(fd, TCSANOW, &tty);        // apply now\n\nwrite(fd, \"AT\\r\", 3);\nchar buf[128];\nssize_t n = read(fd, buf, sizeof(buf));   // blocks up to VTIME\nclose(fd);</code></pre><h4>Canonical vs raw mode</h4><pre><code># CANONICAL (default): input is line-buffered; read() returns whole lines,\n#   Backspace/Ctrl-C are processed by the line discipline. Good for a shell.\n# RAW (cfmakeraw): every byte delivered as-is, no editing/echo. Required for\n#   binary protocols and talking to modems/microcontrollers.</code></pre>"
    },
    {
      "title": "8. Debugging & References",
      "content": "<h4>Inspecting serial ports</h4><pre><code>dmesg | grep -i tty          # driver registration + errors at boot\ncat /proc/tty/drivers        # all registered tty drivers\ncat /proc/tty/driver/serial  # per-port line, IRQ, tx/rx counts (needs root)\nls /sys/class/tty/           # every tty the kernel knows about\ncat /sys/class/tty/ttyS0/device/resources\n\nsetserial -g /dev/ttyS0      # low-level UART info (type, base, IRQ)\nudevadm info /dev/ttyUSB0    # USB-serial: vendor/product, by-id path</code></pre><h4>RX/TX counters &amp; errors</h4><pre><code># The core maintains per-port counts you update (port->icount):\n#   rx, tx, frame, parity, overrun, brk\n# Growing 'overrun' means you're not draining the RX FIFO fast enough;\n# growing 'frame'/'parity' means a baud/framing mismatch.\ncat /proc/tty/driver/serial   # shows these counters per port</code></pre><h4>Common problems</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Symptom</th><th style=\"text-align:left; padding:0.5rem;\">Likely cause</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Garbage / random characters</td><td style=\"padding:0.5rem;\">Baud rate mismatch on the two ends</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Data stops after a burst</td><td style=\"padding:0.5rem;\">Flow control mismatch (one side using RTS/CTS)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Overrun errors climbing</td><td style=\"padding:0.5rem;\">RX FIFO not serviced fast enough (IRQ latency)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Permission denied on /dev/ttyUSB0</td><td style=\"padding:0.5rem;\">User not in the <code>dialout</code> group</td></tr><tr><td style=\"padding:0.5rem;\">Extra CR/LF or missing bytes</td><td style=\"padding:0.5rem;\">Canonical mode instead of raw</td></tr></table><h4>RS-232 vs RS-485</h4><pre><code># RS-232: point-to-point, +/-12V signalling, short runs.\n# RS-485: differential, multi-drop bus, long runs; often HALF-DUPLEX where\n#   the driver toggles a direction (DE/RE) line around each transmission.\n#   The kernel supports this via the RS485 termios ioctls (TIOCSRS485).</code></pre><p><strong>References:</strong></p><ul><li><a href=\"https://docs.kernel.org/driver-api/serial/driver.html\" target=\"_blank\">Linux Kernel: Serial drivers (the low-level API)</a></li><li><a href=\"https://docs.kernel.org/driver-api/tty/index.html\" target=\"_blank\">Linux Kernel: The TTY layer</a></li><li><a href=\"https://man7.org/linux/man-pages/man3/termios.3.html\" target=\"_blank\">termios(3) — line settings from user space</a></li><li><a href=\"https://tldp.org/HOWTO/Serial-HOWTO.html\" target=\"_blank\">Linux Serial HOWTO</a></li><li><a href=\"https://lwn.net/Kernel/LDD3/\" target=\"_blank\">Linux Device Drivers, 3rd ed. — TTY chapter</a></li></ul>"
    }
  ]
};
