/**
 * Topic: Git
 */
const TOPIC_GIT = {
  "id": "git",
  "icon": "🔀",
  "title": "Git",
  "description": "Master Git version control — branching, merging, rebasing, workflows, and server-side hooks",
  "sections": [
    {
      "title": "1. Git Fundamentals",
      "content": "\n<p><strong>Git</strong> is a distributed version control system created by Linus Torvalds in 2005 for Linux kernel development. Every clone is a full repository with complete history.</p>\n<h4>Core Concepts</h4>\n<ul>\n  <li><strong>Repository</strong> — A directory tracked by Git (.git/ folder contains all history)</li>\n  <li><strong>Commit</strong> — A snapshot of all tracked files at a point in time (identified by SHA-1 hash)</li>\n  <li><strong>Branch</strong> — A movable pointer to a commit (lightweight, cheap to create)</li>\n  <li><strong>HEAD</strong> — Pointer to the current branch/commit you're working on</li>\n  <li><strong>Staging Area (Index)</strong> — Intermediate area between working directory and commit</li>\n  <li><strong>Remote</strong> — A reference to another copy of the repository (e.g., origin on GitHub)</li>\n</ul>\n<h4>Git Object Model</h4>\n<pre><code># Git stores 4 types of objects (all content-addressed by SHA-1):\n#\n# blob     — file content (no filename, just raw bytes)\n# tree     — directory listing (maps names to blob/tree SHAs)\n# commit   — snapshot (points to tree + parent commits + metadata)\n# tag      — named reference to a commit (annotated tags)\n#\n# Example commit structure:\n#   commit abc123\n#   ├── tree def456\n#   │   ├── blob 111... → README.md\n#   │   ├── blob 222... → main.py\n#   │   └── tree 333... → src/\n#   │       └── blob 444... → src/app.py\n#   ├── parent commit 789xyz (previous commit)\n#   ├── author: John Smith <john@example.com>\n#   └── message: \"Add feature X\"\n\n# Inspect objects\ngit cat-file -t abc123    # show type\ngit cat-file -p abc123    # show content\ngit ls-tree HEAD          # show current tree</code></pre>\n<h4>Basic Workflow</h4>\n<pre><code># Initialize a repository\ngit init\ngit init --bare /path/to/repo.git  # server-side (no working directory)\n\n# Clone an existing repository\ngit clone https://github.com/user/repo.git\ngit clone git@github.com:user/repo.git  # SSH\n\n# Check status\ngit status\ngit status -s  # short format\n\n# Stage files\ngit add file.txt          # stage specific file\ngit add -A                # stage all changes\ngit add -p                # interactive staging (hunk by hunk)\n\n# Commit\ngit commit -m \"message\"\ngit commit -am \"message\"  # stage tracked files + commit\ngit commit --amend        # modify last commit (rewrite history)\n\n# View history\ngit log --oneline --graph --all\ngit log -5                # last 5 commits\ngit log --stat            # show file changes per commit\ngit log -p file.txt       # history of a specific file</code></pre>\n"
    },
    {
      "title": "2. Branching & Merging",
      "content": "\n<h4>Branch Operations</h4>\n<pre><code># List branches\ngit branch            # local\ngit branch -r         # remote\ngit branch -a         # all\n\n# Create and switch to branch\ngit branch feature-x\ngit checkout feature-x\n# Or combined (modern):\ngit switch -c feature-x\n\n# Delete branch\ngit branch -d feature-x     # safe delete (must be merged)\ngit branch -D feature-x     # force delete\n\n# Rename branch\ngit branch -m old-name new-name\n\n# Track remote branch\ngit checkout --track origin/feature-x\ngit switch -c feature-x origin/feature-x</code></pre>\n<h4>Merging Strategies</h4>\n<pre><code># Fast-forward merge (linear history, no merge commit)\ngit checkout main\ngit merge feature-x\n# If main hasn't diverged, pointer just moves forward\n\n# No-fast-forward (always create merge commit)\ngit merge --no-ff feature-x\n\n# Squash merge (combine all feature commits into one)\ngit merge --squash feature-x\ngit commit -m \"Add feature X\"\n\n# Resolve conflicts\ngit merge feature-x\n# CONFLICT: edit files to resolve\ngit add resolved-file.txt\ngit commit  # completes the merge\n\n# Abort a conflicted merge\ngit merge --abort</code></pre>\n<h4>Rebasing</h4>\n<pre><code># Rebase: replay commits on top of another branch\ngit checkout feature-x\ngit rebase main\n# Result: feature-x commits re-applied on top of main (linear history)\n\n# Interactive rebase (rewrite history)\ngit rebase -i HEAD~5\n# pick   abc1234 Add feature\n# squash def5678 Fix typo\n# reword 789abcd Update docs\n# drop   111aaaa Remove debug code\n\n# Rebase vs Merge:\n# - Rebase: clean linear history (rewrite commits)\n# - Merge: preserve true history (merge commit)\n# Rule: never rebase shared/pushed branches</code></pre>\n"
    },
    {
      "title": "3. Remote Operations",
      "content": "\n<h4>Remote Management</h4>\n<pre><code># List remotes\ngit remote -v\n\n# Add remote\ngit remote add origin git@github.com:user/repo.git\ngit remote add upstream https://github.com/original/repo.git\n\n# Change remote URL\ngit remote set-url origin git@github.com:user/new-repo.git\n\n# Remove remote\ngit remote remove upstream</code></pre>\n<h4>Push & Pull</h4>\n<pre><code># Push branch to remote\ngit push origin main\ngit push -u origin feature-x    # set upstream tracking\ngit push --force-with-lease      # safe force push (checks remote state)\ngit push --tags                  # push all tags\n\n# Pull (fetch + merge)\ngit pull origin main\ngit pull --rebase origin main    # fetch + rebase instead of merge\n\n# Fetch (download without merging)\ngit fetch origin\ngit fetch --all                  # all remotes\ngit fetch --prune                # remove stale remote-tracking branches\n\n# Push delete remote branch\ngit push origin --delete feature-x</code></pre>\n<h4>Forking Workflow</h4>\n<pre><code># 1. Fork on GitHub/GitLab (creates your copy)\n# 2. Clone your fork\ngit clone git@github.com:you/repo.git\ngit remote add upstream git@github.com:original/repo.git\n\n# 3. Keep fork in sync\ngit fetch upstream\ngit checkout main\ngit merge upstream/main\ngit push origin main\n\n# 4. Create feature branch, push to your fork\ngit switch -c my-feature\n# ... make changes ...\ngit push -u origin my-feature\n\n# 5. Open Pull Request / Merge Request from your fork</code></pre>\n"
    },
    {
      "title": "4. Advanced Git Operations",
      "content": "\n<h4>Stashing</h4>\n<pre><code># Save uncommitted work temporarily\ngit stash\ngit stash push -m \"WIP: feature X\"\ngit stash list\ngit stash pop               # apply + remove top stash\ngit stash apply stash@{2}   # apply specific stash (keep in list)\ngit stash drop stash@{0}    # remove specific stash</code></pre>\n<h4>Cherry-Pick</h4>\n<pre><code># Apply a specific commit to current branch\ngit cherry-pick abc1234\ngit cherry-pick abc1234 def5678   # multiple commits\ngit cherry-pick --no-commit abc1234  # stage but don't commit</code></pre>\n<h4>Reset & Revert</h4>\n<pre><code># Reset (move HEAD, potentially dangerous)\ngit reset --soft HEAD~1    # undo commit, keep changes staged\ngit reset --mixed HEAD~1   # undo commit, unstage changes (default)\ngit reset --hard HEAD~1    # undo commit, discard all changes\n\n# Revert (create new commit that undoes a previous one — safe for shared branches)\ngit revert abc1234\ngit revert HEAD~3..HEAD    # revert last 3 commits</code></pre>\n<h4>Bisect (Find Bug-Introducing Commit)</h4>\n<pre><code># Binary search through commits to find which one introduced a bug\ngit bisect start\ngit bisect bad                # current commit is broken\ngit bisect good v1.0          # this tag was working\n# Git checks out a middle commit — test it, then:\ngit bisect good   # or\ngit bisect bad\n# Repeat until Git finds the first bad commit\ngit bisect reset  # return to original state\n\n# Automated bisect with script\ngit bisect start HEAD v1.0\ngit bisect run ./test-script.sh</code></pre>\n<h4>Reflog (Recovery)</h4>\n<pre><code># Git keeps a log of all HEAD movements (even after reset --hard)\ngit reflog\n# abc1234 HEAD@{0}: reset: moving to HEAD~1\n# def5678 HEAD@{1}: commit: Add feature\n\n# Recover \"lost\" commits\ngit checkout def5678\ngit branch recovered-feature def5678</code></pre>\n"
    },
    {
      "title": "5. Git Workflows",
      "content": "\n<h4>Gitflow</h4>\n<pre><code># Long-lived branches: main (release), develop (integration)\n# Short-lived branches: feature/*, release/*, hotfix/*\n\nmain ─────●─────────────────●──── (releases only)\n           \\               /\ndevelop ────●───●───●───●─── (integration)\n             \\     /\nfeature/x ────●───●  (feature branch)\n\n# Suitable for: scheduled releases, versioned software</code></pre>\n<h4>GitHub Flow (Trunk-Based)</h4>\n<pre><code># Single long-lived branch: main\n# Short-lived feature branches → PR → merge to main → deploy\n\nmain ─────●───●───●───●───●── (always deployable)\n           \\   / \\   /\nfeature-a ──●──  ──●──\n                feature-b\n\n# Steps:\n# 1. Branch from main\n# 2. Make changes + commit\n# 3. Open Pull Request\n# 4. Code review + CI passes\n# 5. Merge to main\n# 6. Deploy\n\n# Suitable for: continuous deployment, web apps, SaaS</code></pre>\n<h4>Commit Message Convention</h4>\n<pre><code># Conventional Commits (widely adopted)\n&lt;type&gt;(&lt;scope&gt;): &lt;short description&gt;\n\n# Types:\n# feat:     new feature\n# fix:      bug fix\n# docs:     documentation only\n# style:    formatting (no code change)\n# refactor: code restructuring (no feature/fix)\n# perf:     performance improvement\n# test:     adding tests\n# chore:    maintenance (deps, CI, build)\n\n# Examples:\nfeat(pcie): add AER error monitoring probe\nfix(storage): correct latency histogram bucket calculation\ndocs(ebpf): add development plan and test plan\nchore(deps): upgrade BCC to v0.30.0</code></pre>\n"
    },
    {
      "title": "6. Git Hooks & Automation",
      "content": "\n<p>Git hooks are scripts that run automatically at specific points in the Git workflow. They live in <code>.git/hooks/</code> (local) or can be shared via tools like <code>pre-commit</code>.</p>\n<h4>Client-Side Hooks</h4>\n<table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\">\n  <tr style=\"border-bottom:1px solid #30363d;\">\n    <th style=\"text-align:left; padding:0.5rem;\">Hook</th>\n    <th style=\"text-align:left; padding:0.5rem;\">When</th>\n    <th style=\"text-align:left; padding:0.5rem;\">Common Use</th>\n  </tr>\n  <tr style=\"border-bottom:1px solid #30363d;\">\n    <td style=\"padding:0.5rem;\">pre-commit</td><td style=\"padding:0.5rem;\">Before commit is created</td><td style=\"padding:0.5rem;\">Lint, format, run fast tests</td>\n  </tr>\n  <tr style=\"border-bottom:1px solid #30363d;\">\n    <td style=\"padding:0.5rem;\">commit-msg</td><td style=\"padding:0.5rem;\">After message written</td><td style=\"padding:0.5rem;\">Validate commit message format</td>\n  </tr>\n  <tr style=\"border-bottom:1px solid #30363d;\">\n    <td style=\"padding:0.5rem;\">pre-push</td><td style=\"padding:0.5rem;\">Before push to remote</td><td style=\"padding:0.5rem;\">Run full test suite</td>\n  </tr>\n  <tr>\n    <td style=\"padding:0.5rem;\">post-merge</td><td style=\"padding:0.5rem;\">After merge completes</td><td style=\"padding:0.5rem;\">Install new dependencies</td>\n  </tr>\n</table>\n<h4>pre-commit Framework</h4>\n<pre><code># Install pre-commit\npip install pre-commit\n\n# .pre-commit-config.yaml\nrepos:\n  - repo: https://github.com/pre-commit/pre-commit-hooks\n    rev: v4.6.0\n    hooks:\n      - id: trailing-whitespace\n      - id: end-of-file-fixer\n      - id: check-yaml\n      - id: check-json\n  - repo: https://github.com/psf/black\n    rev: 24.4.2\n    hooks:\n      - id: black\n  - repo: https://github.com/pycqa/flake8\n    rev: 7.1.0\n    hooks:\n      - id: flake8\n\n# Install hooks into .git/hooks/\npre-commit install\n\n# Run manually on all files\npre-commit run --all-files</code></pre>\n<h4>Server-Side Hooks</h4>\n<pre><code># Server hooks (GitLab/GitHub/bare repos):\n# pre-receive  — reject pushes that violate rules\n# update       — per-branch rejection (more granular)\n# post-receive — trigger CI/CD, notifications, deployments\n\n# Example: pre-receive hook to block force-push to main\n#!/bin/bash\nwhile read oldrev newrev refname; do\n  if [[ \"$refname\" == \"refs/heads/main\" ]]; then\n    if git merge-base --is-ancestor \"$newrev\" \"$oldrev\" 2>/dev/null; then\n      echo \"ERROR: Force push to main is not allowed!\"\n      exit 1\n    fi\n  fi\ndone</code></pre>\n"
    },
    {
      "title": "7. Troubleshooting & Best Practices",
      "content": "\n<h4>Common Problems & Solutions</h4>\n<table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\">\n  <tr style=\"border-bottom:1px solid #30363d;\">\n    <th style=\"text-align:left; padding:0.5rem;\">Problem</th>\n    <th style=\"text-align:left; padding:0.5rem;\">Solution</th>\n  </tr>\n  <tr style=\"border-bottom:1px solid #30363d;\">\n    <td style=\"padding:0.5rem;\">Accidentally committed to wrong branch</td>\n    <td style=\"padding:0.5rem;\"><code>git stash && git switch correct-branch && git stash pop</code></td>\n  </tr>\n  <tr style=\"border-bottom:1px solid #30363d;\">\n    <td style=\"padding:0.5rem;\">Need to undo last commit (keep changes)</td>\n    <td style=\"padding:0.5rem;\"><code>git reset --soft HEAD~1</code></td>\n  </tr>\n  <tr style=\"border-bottom:1px solid #30363d;\">\n    <td style=\"padding:0.5rem;\">Committed sensitive data (secrets)</td>\n    <td style=\"padding:0.5rem;\"><code>git filter-repo --path secrets.env --invert-paths</code> + rotate credentials</td>\n  </tr>\n  <tr style=\"border-bottom:1px solid #30363d;\">\n    <td style=\"padding:0.5rem;\">Merge conflicts everywhere</td>\n    <td style=\"padding:0.5rem;\">Rebase frequently: <code>git pull --rebase origin main</code></td>\n  </tr>\n  <tr style=\"border-bottom:1px solid #30363d;\">\n    <td style=\"padding:0.5rem;\">Detached HEAD state</td>\n    <td style=\"padding:0.5rem;\"><code>git switch -c new-branch</code> (create branch at current point)</td>\n  </tr>\n  <tr>\n    <td style=\"padding:0.5rem;\">Repository too large (binary files)</td>\n    <td style=\"padding:0.5rem;\">Use Git LFS: <code>git lfs track \"*.bin\"</code></td>\n  </tr>\n</table>\n<h4>Git Configuration</h4>\n<pre><code># Set identity\ngit config --global user.name \"Your Name\"\ngit config --global user.email \"you@example.com\"\n\n# Default branch name\ngit config --global init.defaultBranch main\n\n# Useful aliases\ngit config --global alias.lg \"log --oneline --graph --all --decorate\"\ngit config --global alias.st \"status -sb\"\ngit config --global alias.co \"checkout\"\ngit config --global alias.last \"log -1 HEAD --stat\"\n\n# Auto-prune on fetch\ngit config --global fetch.prune true\n\n# Rebase by default on pull\ngit config --global pull.rebase true\n\n# Sign commits with GPG\ngit config --global commit.gpgsign true\ngit config --global user.signingkey YOUR_GPG_KEY_ID</code></pre>\n<h4>Best Practices</h4>\n<ul>\n  <li><strong>Commit early, commit often</strong> — Small, atomic commits are easier to review and revert</li>\n  <li><strong>Write meaningful commit messages</strong> — Future you will thank present you</li>\n  <li><strong>Never force-push shared branches</strong> — Use <code>--force-with-lease</code> if absolutely necessary</li>\n  <li><strong>Use .gitignore properly</strong> — Never commit build artifacts, secrets, or IDE files</li>\n  <li><strong>Keep branches short-lived</strong> — Merge within days, not weeks</li>\n  <li><strong>Protect main branch</strong> — Require PRs, CI passing, and code review</li>\n  <li><strong>Tag releases</strong> — Use annotated tags: <code>git tag -a v1.0.0 -m \"Release 1.0\"</code></li>\n  <li><strong>Use SSH keys</strong> — Avoid password auth; set up SSH agent</li>\n</ul>\n<p><strong>References:</strong></p>\n<ul>\n  <li><a href=\"https://git-scm.com/doc\" target=\"_blank\">Git Official Documentation</a></li>\n  <li><a href=\"https://git-scm.com/book/en/v2\" target=\"_blank\">Pro Git Book (free)</a></li>\n  <li><a href=\"https://docs.github.com/en/get-started\" target=\"_blank\">GitHub Docs</a></li>\n  <li><a href=\"https://www.conventionalcommits.org/\" target=\"_blank\">Conventional Commits Spec</a></li>\n</ul>\n"
    }
  ]
};
