/**
 * Topic: Regmap (Register Map Abstraction)
 */
const TOPIC_REGMAP = {
  "id": "regmap",
  "icon": "🗺️",
  "title": "Regmap (Register Map API)",
  "description": "The Linux kernel's unified register-access abstraction — write a driver once and use it over I2C, SPI, MMIO or a custom bus, with caching, bulk operations, atomic bit updates and access validation",
  "sections": [
    {
      "title": "1. Overview & Architecture",
      "content": "<p><strong>Regmap</strong> is the kernel's <em>unified register access abstraction</em>. Before regmap, every driver reimplemented the same boilerplate for talking to a device's registers over whatever bus it happened to sit on (I2C, SPI, memory-mapped I/O, ...). Regmap factors that out: the driver expresses <em>what</em> register to read/write, and regmap handles <em>how</em> to move the bits over the chosen bus — plus caching, locking, byte-order handling, and debugfs.</p><p><strong>The core idea:</strong> write once — use with different buses. Caching, bulk ops, bit updates and access validation come for free.</p>" +
        regmapArchitectureSVG() +
        "<h4>Layered Architecture</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Layer</th><th style=\"text-align:left; padding:0.5rem;\">Role</th><th style=\"text-align:left; padding:0.5rem;\">Example</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><strong>Driver Layer</strong></td><td style=\"padding:0.5rem;\">Device-specific logic</td><td style=\"padding:0.5rem;\">Calls regmap_read()/regmap_write()/regmap_update_bits()</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><strong>Regmap Core</strong> (regmap.c)</td><td style=\"padding:0.5rem;\">Register-map abstraction &amp; helpers</td><td style=\"padding:0.5rem;\">Caching, locking, debugfs, validation</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><strong>Regmap Bus Backend</strong></td><td style=\"padding:0.5rem;\">Bus-specific regmap implementation</td><td style=\"padding:0.5rem;\">regmap-i2c, regmap-spi, regmap-mmio</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><strong>Bus Core</strong></td><td style=\"padding:0.5rem;\">Linux subsystem core</td><td style=\"padding:0.5rem;\">I2C core / SPI core / platform bus</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><strong>Controller Driver</strong></td><td style=\"padding:0.5rem;\">Hardware controller driver</td><td style=\"padding:0.5rem;\">I2C/SPI/memory controller</td></tr><tr><td style=\"padding:0.5rem;\"><strong>Hardware</strong></td><td style=\"padding:0.5rem;\">Physical bus &amp; device</td><td style=\"padding:0.5rem;\">Bus wires + slave device / memory</td></tr></table><p>The flow is a clean stack: <em>Device Driver → Regmap Core → Regmap Bus Backend → Linux Bus Core → Bus Controller Driver → Hardware</em>. Swapping the bus means swapping only the backend and the config — the driver's register logic is untouched.</p>"
    },
    {
      "title": "2. Why Regmap? (Motivation)",
      "content": "<h4>The problem it solves</h4><pre><code># Before regmap, an I2C sensor driver and an SPI variant of the SAME chip\n# each hand-rolled their own register access:\n#\n#   i2c_smbus_read_byte_data(client, reg);      // I2C variant\n#   spi_write_then_read(spi, &reg, 1, &val, 1);  // SPI variant\n#\n# Two code paths, two sets of bugs, duplicated caching/locking/endianness.\n\n# With regmap, the driver just does:\n#   regmap_read(map, reg, &val);\n# ...and a one-line config selects the bus. Same driver, any bus.</code></pre><h4>Benefits</h4><ul><li><strong>Write one driver, use with multiple buses</strong> — I2C, SPI, MMIO, custom, and more share the same register logic.</li><li><strong>Reduces boilerplate</strong> — no per-driver caching/locking/byte-swapping.</li><li><strong>Consistent API across the kernel</strong> — every regmap-based driver reads the same way, so maintenance is easier.</li><li><strong>Portability</strong> — porting a chip to a new bus is mostly a config change.</li><li><strong>Free infrastructure</strong> — register cache, bulk transfers, atomic bit updates, access validation, and debugfs inspection.</li></ul><h4>Key Features at a glance</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Feature</th><th style=\"text-align:left; padding:0.5rem;\">What it does</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">🗄️ <strong>Register Caching</strong></td><td style=\"padding:0.5rem;\">Improves performance by caching register values (skip bus reads)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">✂️ <strong>Bit Operations</strong></td><td style=\"padding:0.5rem;\">Update specific bits atomically (read-modify-write handled for you)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">📦 <strong>Bulk Operations</strong></td><td style=\"padding:0.5rem;\">Read/write multiple registers efficiently in one transfer</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">🔢 <strong>Different Register Widths</strong></td><td style=\"padding:0.5rem;\">8/16/32/64-bit register address &amp; value support</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">✅ <strong>Access Validation</strong></td><td style=\"padding:0.5rem;\">Validate register ranges and permissions (readable/writable/volatile)</td></tr><tr><td style=\"padding:0.5rem;\">🔍 <strong>Debug &amp; Trace</strong></td><td style=\"padding:0.5rem;\">debugfs support to inspect register access at runtime</td></tr></table>"
    },
    {
      "title": "3. Bus Backends",
      "content": "<p>Regmap ships a set of <strong>bus backends</strong>. Each implements the low-level read/write for one bus type; the driver picks one via a single init call. The register logic above never changes.</p><h4>1) I2C — regmap-i2c</h4><pre><code>struct regmap *map = devm_regmap_init_i2c(client, &config);</code></pre><ul><li>8/16/32-bit register addresses</li><li>All I2C transfer types supported (byte, word, block, SMBus)</li><li>Automatic cache sync &amp; endianness handling</li><li>Stack: <em>Device Driver → Regmap Core → regmap-i2c → I2C Core → I2C Controller Driver</em></li></ul><h4>2) SPI — regmap-spi</h4><pre><code>struct regmap *map = devm_regmap_init_spi(spi, &config);</code></pre><ul><li>Customizable command / address / data phases</li><li>8/16/32-bit addresses</li><li>Chip-select (CS) handling by regmap</li><li>High-speed register access</li></ul><h4>3) MMIO — regmap-mmio</h4><pre><code>void __iomem *base = devm_platform_ioremap_resource(pdev, 0);\nstruct regmap *map = devm_regmap_init_mmio(dev, base, &config);</code></pre><ul><li>Direct memory-mapped access — very high performance, no bus overhead</li><li>No bus core involved (implicit memory controller)</li><li>Suitable for on-chip / SoC registers</li><li>Access pattern over a memory-mapped region: <code>0x1000 | 0x1004 | 0x1008 | ...</code> as 32-bit words</li></ul><h4>4) Platform / Custom — regmap (custom)</h4><pre><code>struct regmap_bus my_bus = {\n    .reg_write = my_reg_write,\n    .reg_read  = my_reg_read,\n};\nstruct regmap *map = devm_regmap_init(dev, &my_bus, ctx, &config);</code></pre><ul><li>For non-standard buses</li><li>User provides custom read/write callbacks</li><li>Flexible and extensible</li></ul><h4>Other supported backends</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Backend</th><th style=\"text-align:left; padding:0.5rem;\">Bus / Use</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>regmap-rsb</code></td><td style=\"padding:0.5rem;\">Remote shared bus</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>regmap-mfd</code></td><td style=\"padding:0.5rem;\">Multi-Function Device</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>regmap-w1</code></td><td style=\"padding:0.5rem;\">1-Wire bus</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>regmap-pmbus</code></td><td style=\"padding:0.5rem;\">PMBus devices</td></tr><tr><td style=\"padding:0.5rem;\"><code>regmap-sdw</code></td><td style=\"padding:0.5rem;\">MIPI SoundWire</td></tr></table><p>…and more. Adding a bus is a matter of providing a backend; every existing regmap driver can then run on it.</p>"
    },
    {
      "title": "4. regmap_config — Describing the Device",
      "content": "<p>A driver describes its register layout once with a <code>struct regmap_config</code>. Regmap uses it for width handling, caching, and access validation.</p><pre><code>#include &lt;linux/regmap.h&gt;\n\n// Access-validation callbacks: which registers are readable / writable /\n// volatile (must never be cached, e.g. status &amp; data registers).\nstatic bool foo_writeable_reg(struct device *dev, unsigned int reg)\n{\n    switch (reg) {\n    case FOO_REG_CTRL:\n    case FOO_REG_CONFIG:\n        return true;\n    default:\n        return false;   // read-only regs rejected on write\n    }\n}\n\nstatic bool foo_volatile_reg(struct device *dev, unsigned int reg)\n{\n    // Status/data change under the device's control -> never cache them\n    return reg == FOO_REG_STATUS || reg == FOO_REG_DATA;\n}\n\nstatic const struct regmap_config foo_regmap_config = {\n    .reg_bits      = 8,             // register ADDRESS width\n    .val_bits      = 8,             // register VALUE width\n    .max_register  = FOO_REG_MAX,   // largest valid register (range check)\n    .writeable_reg = foo_writeable_reg,\n    .volatile_reg  = foo_volatile_reg,\n    .cache_type    = REGCACHE_RBTREE, // enable caching (see section 5)\n    // .val_format_endian = REGMAP_ENDIAN_BIG, // byte order if non-default\n};</code></pre><h4>Common config fields</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Field</th><th style=\"text-align:left; padding:0.5rem;\">Purpose</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>reg_bits / val_bits</code></td><td style=\"padding:0.5rem;\">Address and value width (8/16/32/64-bit)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>reg_stride</code></td><td style=\"padding:0.5rem;\">Spacing between valid register addresses</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>max_register</code></td><td style=\"padding:0.5rem;\">Upper bound used for range validation</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>writeable_reg / readable_reg</code></td><td style=\"padding:0.5rem;\">Permission checks (access validation)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>volatile_reg</code></td><td style=\"padding:0.5rem;\">Registers that must bypass the cache</td></tr><tr><td style=\"padding:0.5rem;\"><code>cache_type</code></td><td style=\"padding:0.5rem;\">REGCACHE_NONE / RBTREE / MAPLE / FLAT</td></tr></table>"
    },
    {
      "title": "5. Register Caching",
      "content": "<p>Caching is one of regmap's biggest wins. A shadow copy of register values lets reads return instantly and lets writes be deferred/coalesced — critical on slow buses like I2C.</p><h4>Cache types</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">cache_type</th><th style=\"text-align:left; padding:0.5rem;\">Storage</th><th style=\"text-align:left; padding:0.5rem;\">Best for</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>REGCACHE_NONE</code></td><td style=\"padding:0.5rem;\">No cache</td><td style=\"padding:0.5rem;\">Every access hits the bus</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>REGCACHE_FLAT</code></td><td style=\"padding:0.5rem;\">Flat array</td><td style=\"padding:0.5rem;\">Small, dense register maps (fastest lookup)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>REGCACHE_RBTREE</code></td><td style=\"padding:0.5rem;\">Red-black tree</td><td style=\"padding:0.5rem;\">Large / sparse register maps</td></tr><tr><td style=\"padding:0.5rem;\"><code>REGCACHE_MAPLE</code></td><td style=\"padding:0.5rem;\">Maple tree</td><td style=\"padding:0.5rem;\">Modern default for sparse maps (kernel 6.x+)</td></tr></table><h4>Cache-aware operations</h4><pre><code>// A volatile register always bypasses the cache and hits the device.\nregmap_read(map, FOO_REG_STATUS, &val);   // real bus read (volatile)\nregmap_read(map, FOO_REG_CONFIG, &val);   // may return cached value\n\n// Mark the cache stale after the device changes state behind our back\nregcache_mark_dirty(map);\n\n// Only push changed (dirty) entries back to hardware — e.g. on resume\nregcache_sync(map);\n\n// Temporarily bypass the cache (force real bus traffic)\nregcache_cache_bypass(map, true);\n/* ... direct hardware access ... */\nregcache_cache_bypass(map, false);\n\n// Suspend: put cache in \"cache only\" mode so no bus traffic happens,\n// then on resume regcache_sync() writes everything back.\nregcache_cache_only(map, true);   // in suspend\nregcache_cache_only(map, false);  // in resume\nregcache_sync(map);</code></pre><p><strong>Power management pattern:</strong> in <code>suspend()</code> set <code>regcache_cache_only(true)</code> and <code>regcache_mark_dirty()</code>; in <code>resume()</code> set <code>regcache_cache_only(false)</code> and <code>regcache_sync()</code> to restore the device from the shadow cache in one burst.</p>"
    },
    {
      "title": "6. Common Regmap APIs",
      "content": "<h4>Single register access</h4><pre><code>int regmap_read(struct regmap *map, unsigned int reg, unsigned int *val);\nint regmap_write(struct regmap *map, unsigned int reg, unsigned int val);</code></pre><h4>Atomic bit updates (read-modify-write)</h4><pre><code>// Change only the bits in 'mask' to the value in 'val' — regmap does the\n// read, applies the mask, and writes back, all under the regmap lock.\nint regmap_update_bits(struct regmap *map, unsigned int reg,\n                       unsigned int mask, unsigned int val);\n\n// Example: set bit 3 (enable) without disturbing other bits\nregmap_update_bits(map, FOO_REG_CTRL, BIT(3), BIT(3));  // set\nregmap_update_bits(map, FOO_REG_CTRL, BIT(3), 0);       // clear\n\n// Convenience helpers\nregmap_set_bits(map, FOO_REG_CTRL, BIT(3));\nregmap_clear_bits(map, FOO_REG_CTRL, BIT(3));</code></pre><h4>Bulk operations</h4><pre><code>// Transfer many consecutive registers in one bus operation\nint regmap_bulk_read(struct regmap *map, unsigned int reg,\n                     void *val, size_t val_count);\nint regmap_bulk_write(struct regmap *map, unsigned int reg,\n                      const void *val, size_t val_count);\n\n// Example: read 6 bytes of sensor data (X/Y/Z, 2 bytes each) at once\nu8 raw[6];\nregmap_bulk_read(map, ACCEL_DATA_START, raw, sizeof(raw));</code></pre><h4>Polling &amp; fields</h4><pre><code>// Poll a register until a condition holds (or times out)\nunsigned int v;\nregmap_read_poll_timeout(map, FOO_REG_STATUS, v,\n                         (v & STATUS_READY), 100 /*us*/, 10000 /*us*/);\n\n// regmap_field: bind a named bitfield once, then read/write it directly\nstruct reg_field ctrl_mode = REG_FIELD(FOO_REG_CTRL, 4, 6); // bits 4..6\nstruct regmap_field *f = devm_regmap_field_alloc(dev, map, ctrl_mode);\nregmap_field_write(f, 0x3);\nregmap_field_read(f, &v);</code></pre><h4>API summary</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">API</th><th style=\"text-align:left; padding:0.5rem;\">Purpose</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>regmap_read(map, reg, &val)</code></td><td style=\"padding:0.5rem;\">Read a single register</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>regmap_write(map, reg, val)</code></td><td style=\"padding:0.5rem;\">Write a single register</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>regmap_update_bits(map, reg, mask, val)</code></td><td style=\"padding:0.5rem;\">Atomic masked bit update</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>regmap_bulk_read(map, reg, buf, count)</code></td><td style=\"padding:0.5rem;\">Read multiple registers</td></tr><tr><td style=\"padding:0.5rem;\"><code>regmap_bulk_write(map, reg, buf, count)</code></td><td style=\"padding:0.5rem;\">Write multiple registers</td></tr></table><p>…and more.</p>"
    },
    {
      "title": "7. Full Driver Example & Debugging",
      "content": "<h4>Minimal I2C driver using regmap</h4><pre><code>#include &lt;linux/i2c.h&gt;\n#include &lt;linux/regmap.h&gt;\n#include &lt;linux/module.h&gt;\n\n#define FOO_REG_CTRL    0x00\n#define FOO_REG_STATUS  0x01\n#define FOO_REG_DATA    0x02\n#define FOO_REG_MAX     0x0F\n\nstruct foo_dev {\n    struct regmap *regmap;\n};\n\nstatic bool foo_volatile_reg(struct device *dev, unsigned int reg)\n{\n    return reg == FOO_REG_STATUS || reg == FOO_REG_DATA;\n}\n\nstatic const struct regmap_config foo_config = {\n    .reg_bits     = 8,\n    .val_bits     = 8,\n    .max_register = FOO_REG_MAX,\n    .volatile_reg = foo_volatile_reg,\n    .cache_type   = REGCACHE_RBTREE,\n};\n\nstatic int foo_probe(struct i2c_client *client)\n{\n    struct foo_dev *foo;\n    unsigned int val;\n    int ret;\n\n    foo = devm_kzalloc(&client->dev, sizeof(*foo), GFP_KERNEL);\n    if (!foo)\n        return -ENOMEM;\n\n    // One line selects the I2C backend. Swap to devm_regmap_init_spi()\n    // for an SPI variant of the same chip -- rest of the driver is identical.\n    foo->regmap = devm_regmap_init_i2c(client, &foo_config);\n    if (IS_ERR(foo->regmap))\n        return PTR_ERR(foo->regmap);\n\n    i2c_set_clientdata(client, foo);\n\n    // Enable the device: set control bit 0 without touching other bits\n    ret = regmap_update_bits(foo->regmap, FOO_REG_CTRL, BIT(0), BIT(0));\n    if (ret)\n        return ret;\n\n    // Read status (volatile -> always a real bus read)\n    ret = regmap_read(foo->regmap, FOO_REG_STATUS, &val);\n    if (ret)\n        return ret;\n\n    dev_info(&client->dev, \"status = 0x%02x\\n\", val);\n    return 0;\n}\n\nstatic const struct i2c_device_id foo_id[] = { { \"foo\", 0 }, { } };\nMODULE_DEVICE_TABLE(i2c, foo_id);\n\nstatic struct i2c_driver foo_driver = {\n    .driver = { .name = \"foo\" },\n    .probe  = foo_probe,\n    .id_table = foo_id,\n};\nmodule_i2c_driver(foo_driver);\nMODULE_LICENSE(\"GPL\");</code></pre><h4>Debug &amp; Trace via debugfs</h4><pre><code># Regmap exposes each map under debugfs (needs CONFIG_REGMAP + debugfs mounted):\nls /sys/kernel/debug/regmap/\n#   1-0068   (i2c bus-addr) etc.\n\ncd /sys/kernel/debug/regmap/1-0068/\n\n# Dump all readable registers (address: value)\ncat registers\n#   0: 01\n#   1: 80\n#   2: 00\n\n# Read/write a single register from userspace for debugging\ncat range           # valid register range\ncat name            # device name\n\n# Trace regmap access with ftrace\ncd /sys/kernel/debug/tracing\necho 1 > events/regmap/enable\ncat trace_pipe\n#   regmap_reg_write: ... reg=0x0 val=0x1\n#   regmap_reg_read:  ... reg=0x1 val=0x80</code></pre><h4>Common pitfalls</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Pitfall</th><th style=\"text-align:left; padding:0.5rem;\">Fix</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Status/data reads return stale values</td><td style=\"padding:0.5rem;\">Mark them in <code>volatile_reg</code> so they bypass the cache</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Registers lost after suspend/resume</td><td style=\"padding:0.5rem;\">Use regcache_cache_only + mark_dirty + regcache_sync</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Wrong values on big-endian device</td><td style=\"padding:0.5rem;\">Set <code>val_format_endian</code> in the config</td></tr><tr><td style=\"padding:0.5rem;\">Write to a read-only register silently ignored</td><td style=\"padding:0.5rem;\">Define <code>writeable_reg</code> to catch it during development</td></tr></table><p><strong>References:</strong></p><ul><li><a href=\"https://docs.kernel.org/driver-api/regmap.html\" target=\"_blank\">Linux Kernel: Register Map (regmap) API</a></li><li><a href=\"https://www.kernel.org/doc/html/latest/driver-api/regmap.html\" target=\"_blank\">driver-api/regmap.html (latest)</a></li><li><a href=\"https://lwn.net/Articles/462013/\" target=\"_blank\">LWN: Regmap: A register map abstraction</a></li><li><a href=\"https://elixir.bootlin.com/linux/latest/source/include/linux/regmap.h\" target=\"_blank\">include/linux/regmap.h (API header)</a></li></ul>"
    }
  ]
};

/**
 * Inline SVG recreation of the Regmap architecture diagram.
 * Self-contained (no external image dependency) so it renders anywhere.
 * Colors chosen for high contrast / low-vision accessibility.
 */
function regmapArchitectureSVG() {
  const stack = [
    { label: "Device Driver",              fill: "#dbeafe", note: "Uses Regmap APIs (regmap_read/write, regmap_update_bits)" },
    { label: "Regmap Core (regmap.c)",     fill: "#dcfce7", note: "Register map abstraction, caching, locking, debugfs" },
    { label: "Regmap Bus Backends",        fill: "#fef9c3", note: "Bus-specific impl (regmap-i2c, regmap-spi, regmap-mmio)" },
    { label: "Linux Bus Core",             fill: "#ede9fe", note: "I2C Core / SPI Core / Platform Bus" },
    { label: "Bus Controller Driver",      fill: "#fee2e2", note: "I2C / SPI / Memory Controller" },
    { label: "Hardware (Bus + Device)",    fill: "#e5e7eb", note: "Physical bus wires + slave device / memory" },
  ];
  const rowH = 46, gap = 10, boxW = 360, boxX = 30, noteX = 410;
  const height = stack.length * (rowH + gap) + 40;
  let rows = "";
  stack.forEach((s, i) => {
    const y = 20 + i * (rowH + gap);
    rows +=
      `<rect x="${boxX}" y="${y}" width="${boxW}" height="${rowH}" rx="6" ` +
      `fill="${s.fill}" stroke="#334155" stroke-width="1.5"></rect>` +
      `<text x="${boxX + boxW / 2}" y="${y + rowH / 2 + 5}" text-anchor="middle" ` +
      `font-family="sans-serif" font-size="15" font-weight="600" fill="#0f172a">${s.label}</text>` +
      `<text x="${noteX}" y="${y + rowH / 2 + 4}" font-family="sans-serif" font-size="12" ` +
      `fill="#94a3b8">${s.note}</text>`;
    if (i < stack.length - 1) {
      const ay = y + rowH, ay2 = y + rowH + gap;
      const ax = boxX + boxW / 2;
      rows +=
        `<line x1="${ax}" y1="${ay}" x2="${ax}" y2="${ay2}" stroke="#64748b" stroke-width="2"></line>` +
        `<polygon points="${ax - 5},${ay2 - 6} ${ax + 5},${ay2 - 6} ${ax},${ay2}" fill="#64748b"></polygon>`;
    }
  });
  return (
    `<div style="overflow-x:auto; margin:1rem 0; padding:1rem; background:#0d1117; ` +
    `border:1px solid #30363d; border-radius:8px;">` +
    `<div style="text-align:center; font-weight:700; font-size:1.05rem; color:#58a6ff; margin-bottom:0.25rem;">` +
    `Regmap — Unified Register Access Abstraction in Linux Kernel</div>` +
    `<div style="text-align:center; font-size:0.85rem; color:#8b949e; margin-bottom:0.75rem;">` +
    `Write once — use with different buses &nbsp;|&nbsp; Caching &nbsp;|&nbsp; Bulk ops &nbsp;|&nbsp; Bit updates &nbsp;|&nbsp; Access validation</div>` +
    `<svg viewBox="0 0 760 ${height}" width="100%" role="img" ` +
    `aria-label="Regmap layered architecture: Device Driver on top calling down through Regmap Core, ` +
    `Regmap Bus Backends, Linux Bus Core, Bus Controller Driver, to Hardware at the bottom.">${rows}</svg>` +
    `</div>`
  );
}
