/**
 * Topic: Tracepoints
 */
const TOPIC_TRACEPOINTS = {
  "id": "tracepoints",
  "category": "kernel",
  "icon": "🎯",
  "title": "Tracepoints",
  "description": "Static kernel instrumentation — how tracepoints work, the TRACE_EVENT macro, the events/ interface, filters & triggers, and how ftrace/perf/eBPF attach to them, with flow charts throughout",
  "keywords": [
    "tracepoint", "tracepoints", "trace_event", "static tracing", "events",
    "trace_", "register_trace", "static key", "jump label", "nop", "format",
    "filter", "trigger", "trace_pipe", "perf tracepoint", "bpf tracepoint",
    "raw_tracepoint", "sched_switch", "block_rq_issue", "stable abi",
    "instrumentation", "DEFINE_EVENT", "TP_printk", "tracefs", "dtrace"
  ],
  "sections": [
    {
      "title": "1. What are Tracepoints?",
      "content": "<p><strong>Tracepoints</strong> are <em>static</em> instrumentation points that kernel developers place in the source code at meaningful events — a task being scheduled, a block I/O being issued, a packet transmitted. Each is a named hook that does nothing until someone attaches to it, then hands its callback the event's data. They are the <strong>stable, low-overhead, preferred</strong> way to observe the kernel.</p><h4>Static vs dynamic instrumentation</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\"></th><th style=\"text-align:left; padding:0.5rem;\">Tracepoint (static)</th><th style=\"text-align:left; padding:0.5rem;\">kprobe (dynamic)</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Placed by</td><td style=\"padding:0.5rem;\">Developer, in source</td><td style=\"padding:0.5rem;\">You, at any address at runtime</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Where</td><td style=\"padding:0.5rem;\">Only where one exists</td><td style=\"padding:0.5rem;\">Almost anywhere</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Stability</td><td style=\"padding:0.5rem;\">STABLE ABI (kept working)</td><td style=\"padding:0.5rem;\">Unstable (breaks on rename/inline)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Overhead when off</td><td style=\"padding:0.5rem;\">~zero (a NOP)</td><td style=\"padding:0.5rem;\">~zero (also patched)</td></tr><tr><td style=\"padding:0.5rem;\">Data exposed</td><td style=\"padding:0.5rem;\">Named, structured fields</td><td style=\"padding:0.5rem;\">Raw registers (arch-specific)</td></tr></table><h4>The golden rule</h4><pre><code># Prefer a TRACEPOINT when one exists:\n#   - stable (won't break across kernel versions)\n#   - low overhead\n#   - gives you clean, NAMED fields instead of raw registers\n# Reach for a kprobe only when there's NO tracepoint at the spot you need.\n#   (See the kprobes and ftrace topics; eBPF attaches to all of them.)</code></pre><h4>Where tracepoints sit</h4><pre><code>┌───────────────────────────────────────────────────────────────┐\n│ CONSUMERS:  ftrace events/ | perf | eBPF (bpftrace/BCC) | modules │\n└─────────────────────────────────┬─────────────────────────────┘\n                                   ▼  all attach to the SAME tracepoints\n┌───────────────────────────────────────────────────────────────┐\n│ TRACEPOINT   a named hook in kernel code: trace_sched_switch()   │\n│              -- calls registered callbacks when enabled          │\n├───────────────────────────────────────────────────────────────┤\n│ KERNEL CODE   the function where the developer placed the point  │\n└───────────────────────────────────────────────────────────────┘\n#\n# One tracepoint, many consumers. You mostly USE tracepoints (via ftrace/\n# perf/eBPF); kernel developers DEFINE them (sections 3-4).</code></pre>"
    },
    {
      "title": "2. How a Tracepoint Fires",
      "content": "<p>The clever part: a tracepoint costs almost nothing when disabled. It compiles to a <strong>NOP</strong> guarded by a <strong>static key</strong> (jump label). Enabling it patches the NOP into a jump to the callback path — so the hot path pays nothing until you're actually tracing.</p><h4>Disabled vs enabled (the static-key mechanism)</h4><pre><code>DISABLED (the common case):\n   kernel code ... [ NOP ] ... continue     ← the tracepoint is a no-op\n                     │\n                     └ a static_key guards a branch that is patched OUT\n\nENABLED (someone attached):\n   kernel code ... [ JMP ]──────────┐\n                                      ▼\n                    for each registered probe: call probe(args)\n                                      │\n                     ◄────────────────┘  return, continue kernel code\n#\n# Switching a tracepoint on/off LIVE-PATCHES the instruction (jump label /\n# static key), so the disabled path has no test, no branch mispredict --\n# effectively zero cost. This is why the kernel can ship thousands of them.</code></pre><h4>The firing flow when enabled</h4><pre><code>execution reaches trace_sched_switch(prev, next)  in the scheduler\n   │\n   ▼\nstatic key is ON ─► enter the tracepoint's probe list\n   │\n   ▼\nfor each attached probe (ftrace / perf / eBPF / module callback):\n     call probe(data, prev, next)   ── passes the event's TYPED arguments\n   │\n   ▼\nreturn to the scheduler, which continues normally\n#\n# Multiple consumers can attach to ONE tracepoint at once -- e.g. ftrace\n# recording it while an eBPF program also samples it.</code></pre><h4>Two callback flavors</h4><pre><code># A tracepoint exposes its data two ways:\n#   'trace event'    -> formatted, named fields (what ftrace/perf events use)\n#   'raw tracepoint' -> the raw callback arguments (what eBPF often uses for\n#                       lower overhead; BPF_PROG_TYPE_RAW_TRACEPOINT)\n# Same underlying hook, different access shape.</code></pre>"
    },
    {
      "title": "3. Defining a Tracepoint (TRACE_EVENT)",
      "content": "<p>Kernel developers add tracepoints with the <strong><code>TRACE_EVENT</code></strong> macro. It generates everything: the <code>trace_&lt;name&gt;()</code> function to call, the structured record, and the human-readable format used by ftrace/perf.</p><h4>The TRACE_EVENT macro</h4><pre><code>// In include/trace/events/myevents.h\n#undef TRACE_SYSTEM\n#define TRACE_SYSTEM myevents          // the 'subsystem' (events/myevents/)\n\n#include <linux/tracepoint.h>\n\nTRACE_EVENT(my_task_latency,\n    TP_PROTO(int pid, u64 latency_ns),          // the trace_ function's signature\n    TP_ARGS(pid, latency_ns),                    // the argument names\n\n    TP_STRUCT__entry(                            // the recorded fields\n        __field(int, pid)\n        __field(u64, latency_ns)\n    ),\n    TP_fast_assign(                              // copy args -> the record\n        __entry->pid        = pid;\n        __entry->latency_ns = latency_ns;\n    ),\n    TP_printk(\"pid=%d latency=%llu ns\",          // how ftrace prints it\n        __entry->pid, __entry->latency_ns)\n);</code></pre><h4>What TRACE_EVENT generates (flow)</h4><pre><code>TRACE_EVENT(my_task_latency, ...)\n   │  macro expansion at build time generates:\n   ├─► trace_my_task_latency(pid, ns)   ── the function you CALL in kernel code\n   ├─► a static key + probe list for it\n   ├─► the 'format' file exposed under events/myevents/my_task_latency/\n   └─► the record layout + the TP_printk formatter\n#\n# One macro -> the callable hook, the on/off switch, and the userspace\n# interface all at once.</code></pre><h4>Calling it from kernel code</h4><pre><code>#include <trace/events/myevents.h>\n\nvoid finish_task(int pid, u64 start_ns) {\n    u64 lat = ktime_get_ns() - start_ns;\n    trace_my_task_latency(pid, lat);   // NOP unless someone is tracing it\n}\n// DECLARE_EVENT_CLASS + DEFINE_EVENT let several similar tracepoints share\n// one record layout (e.g. many sched events reuse a class).</code></pre>"
    },
    {
      "title": "4. Attaching in a Kernel Module",
      "content": "<p>Beyond the built-in consumers, a module can register its own callback on an existing tracepoint. This shows the raw mechanism the higher-level tools wrap.</p><h4>register_trace_&lt;name&gt;()</h4><pre><code>#include <linux/module.h>\n#include <linux/tracepoint.h>\n#include <trace/events/sched.h>    // for the sched_switch tracepoint\n\n// Your probe -- signature MUST match the tracepoint's TP_PROTO:\nstatic void probe_sched_switch(void *data, bool preempt,\n                               struct task_struct *prev,\n                               struct task_struct *next,\n                               unsigned int prev_state)\n{\n    pr_info(\"switch %s -> %s\\n\", prev->comm, next->comm);\n}\n\nstatic int __init my_init(void)\n{\n    // Attach; 'data' is passed back to the probe as its first arg.\n    return register_trace_sched_switch(probe_sched_switch, NULL);\n}\nstatic void __exit my_exit(void)\n{\n    unregister_trace_sched_switch(probe_sched_switch, NULL);\n    tracepoint_synchronize_unregister();   // wait for in-flight probes to finish\n}\nmodule_init(my_init); module_exit(my_exit);\nMODULE_LICENSE(\"GPL\");</code></pre><h4>Attach / detach flow</h4><pre><code>register_trace_sched_switch(probe, data)\n   │\n   ▼\nadd 'probe' to the tracepoint's probe list; FLIP the static key ON\n   │\n   ▼\nevery sched_switch now also calls your probe  ── (module code covered in\n   │                                               the Character Drivers topic)\n   ▼\nunregister_trace_sched_switch(probe, data)\n   │\n   ▼\nremove from list; if list is empty, flip the static key OFF\n   │\n   ▼\ntracepoint_synchronize_unregister()  ── ensure no CPU is still in your probe\n                                          BEFORE the module unloads (else UB)</code></pre><h4>Handler constraints</h4><pre><code># A tracepoint probe runs in the context of the traced code -- often atomic:\n#   - keep it fast; do NOT sleep\n#   - the argument signature must EXACTLY match TP_PROTO\n#   - synchronize on unregister (above) so you don't free code a CPU is running\n# Most people never do this -- they use ftrace/perf/eBPF instead (next).</code></pre>"
    },
    {
      "title": "5. Using Tracepoints via ftrace",
      "content": "<p>The zero-code way: the kernel exposes every tracepoint through <strong>tracefs</strong> as an event you enable and read. (See the <strong>ftrace</strong> topic for the full tracing framework; here's the tracepoint angle.)</p><h4>Browse and enable</h4><pre><code>cd /sys/kernel/tracing              # or /sys/kernel/debug/tracing\nls events/                          # subsystems: sched block net irq syscalls...\nls events/sched/                    # sched_switch sched_wakeup sched_migrate...\n\necho 1 > events/sched/sched_switch/enable    # enable one tracepoint\necho 1 > events/block/enable                 # a whole subsystem\ncat trace_pipe\n#   ...: sched_switch: prev_comm=bash prev_pid=1234 ==> next_comm=firefox ...</code></pre><h4>The enable → record → read flow</h4><pre><code>echo 1 > events/sched/sched_switch/enable\n   │  flips the tracepoint's static key ON; attaches ftrace's probe\n   ▼\nevery sched_switch writes a formatted record into the RING BUFFER\n   │  (using the tracepoint's TP_printk layout)\n   ▼\ncat trace         (snapshot)   |   cat trace_pipe   (live stream)\n   │\n   ▼\necho 0 > events/sched/sched_switch/enable   ── detach + static key OFF</code></pre><h4>Discovering a tracepoint's fields</h4><pre><code># Every event advertises its fields in a 'format' file -- these names are\n# what you filter and print on:\ncat events/block/block_rq_issue/format\n#   field:dev_t dev;   field:sector_t sector;   field:unsigned int nr_sector; ...\n\ncat available_events | wc -l        # every tracepoint you can enable\ncat available_events | grep sched   # find sched-related ones</code></pre>"
    },
    {
      "title": "6. Filters & Triggers",
      "content": "<p>Enabling a busy tracepoint (like <code>sched_switch</code>) floods the buffer. <strong>Filters</strong> keep only matching events; <strong>triggers</strong> perform an action when an event fires — all evaluated in the kernel, before anything is recorded.</p><h4>Filters</h4><pre><code>cd /sys/kernel/tracing\n\n# Only record switches involving firefox:\necho 'prev_comm == \"firefox\" || next_comm == \"firefox\"' \\\n    > events/sched/sched_switch/filter\n\n# Only large block I/O:\necho 'nr_sector > 128' > events/block/block_rq_issue/filter\n\necho 0 > events/sched/sched_switch/filter    # clear it\n# Operators: == != < > <= >= &amp;&amp; || , and ~ for string globs.</code></pre><h4>Filter flow (evaluated at fire time)</h4><pre><code>tracepoint fires ─► ftrace probe\n   │\n   ▼\nevaluate the FILTER expression against this event's fields\n   │\n   ├─ matches   ─► write the record to the ring buffer\n   └─ no match  ─► DROP it (nothing recorded -> keeps the buffer usable)</code></pre><h4>Triggers — act on an event</h4><pre><code># Stop tracing the instant something interesting happens (freeze the buffer):\necho 'traceoff if pid == 1234' > events/sched/sched_switch/trigger\n\n# Take a stack trace on each matching event:\necho 'stacktrace if nr_sector > 1024' \\\n    > events/block/block_rq_issue/trigger\n\n# Build a histogram entirely in the kernel (no userspace post-processing):\necho 'hist:key=comm:vals=hitcount' > events/sched/sched_switch/trigger\ncat events/sched/sched_switch/hist\n#   { comm: firefox } hitcount: 4213\n#   { comm: bash    } hitcount:  219\n# hist triggers + synthetic events can even compute latency across TWO\n# tracepoints -- see the ftrace topic.</code></pre>"
    },
    {
      "title": "7. Tracepoints with perf & eBPF",
      "content": "<p>Beyond ftrace, <strong>perf</strong> records tracepoints into a sample file, and <strong>eBPF</strong> attaches a program to a tracepoint for safe, programmable in-kernel processing — the most powerful option.</p><h4>perf</h4><pre><code>perf list tracepoint                     # every tracepoint perf can record\nsudo perf record -e sched:sched_switch -a sleep 5   # record system-wide\nsudo perf script                         # decode the samples\nsudo perf stat -e block:block_rq_issue -a sleep 5   # just count them</code></pre><h4>eBPF on a tracepoint (bpftrace)</h4><pre><code># Count context switches per process:\nsudo bpftrace -e 'tracepoint:sched:sched_switch { @[comm] = count(); }'\n\n# Use the tracepoint's NAMED fields directly (args->...):\nsudo bpftrace -e 'tracepoint:block:block_rq_issue {\n  printf(\"%s %d sectors\\n\", comm, args->nr_sector); }'\n\n# Because tracepoint fields are a STABLE ABI, these one-liners keep working\n# across kernel versions -- unlike kprobe-based ones. (See the eBPF topic.)</code></pre><h4>How the consumers relate</h4><pre><code>                        ONE tracepoint (e.g. sched:sched_switch)\n                                   │\n        ┌──────────────┬───────────┼───────────────┬──────────────┐\n        ▼              ▼           ▼                ▼              ▼\n     ftrace         perf        eBPF            raw_tracepoint   your\n   events/          record/     (bpftrace/      (low-overhead    module\n   (echo 1)         report      BCC)            BPF programs)    (register_)\n#\n# Same hook, different front-ends. Pick by need:\n#   ftrace  -> quick, no tools, scriptable      perf   -> sampling + files\n#   eBPF    -> programmable aggregation (usual choice for real analysis)</code></pre><h4>Choosing a consumer</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Need</th><th style=\"text-align:left; padding:0.5rem;\">Use</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Quick look, no tools</td><td style=\"padding:0.5rem;\">ftrace <code>events/</code></td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Record + offline analysis</td><td style=\"padding:0.5rem;\"><code>perf record</code></td></tr><tr><td style=\"padding:0.5rem;\">Custom in-kernel aggregation</td><td style=\"padding:0.5rem;\">eBPF (bpftrace/BCC)</td></tr></table>"
    },
    {
      "title": "8. Debugging & References",
      "content": "<h4>Discovering & inspecting tracepoints</h4><pre><code>cd /sys/kernel/tracing\ncat available_events              # all tracepoints (subsystem:event)\nls events/                        # by subsystem\ncat events/sched/sched_switch/format   # the event's fields (for filters)\ncat events/sched/sched_switch/enable   # 1 = currently enabled\n\nsudo perf list tracepoint         # perf's view of the same set\nsudo bpftrace -l 'tracepoint:*'   # bpftrace's view</code></pre><h4>Common subsystems worth knowing</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Subsystem</th><th style=\"text-align:left; padding:0.5rem;\">Sample events</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>sched</code></td><td style=\"padding:0.5rem;\">sched_switch, sched_wakeup, sched_migrate_task</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>block</code></td><td style=\"padding:0.5rem;\">block_rq_issue, block_rq_complete</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>syscalls</code></td><td style=\"padding:0.5rem;\">sys_enter_*, sys_exit_* (per syscall)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>net</code> / <code>tcp</code></td><td style=\"padding:0.5rem;\">net_dev_xmit, tcp_retransmit_skb</td></tr><tr><td style=\"padding:0.5rem;\"><code>irq</code> / <code>timer</code></td><td style=\"padding:0.5rem;\">irq_handler_entry, hrtimer_expire</td></tr></table><h4>Common pitfalls</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Symptom</th><th style=\"text-align:left; padding:0.5rem;\">Cause</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Buffer floods / LOST EVENTS</td><td style=\"padding:0.5rem;\">Enabled a hot tracepoint with no filter</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Module oops on unload</td><td style=\"padding:0.5rem;\">Missing <code>tracepoint_synchronize_unregister()</code></td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">No such tracepoint</td><td style=\"padding:0.5rem;\">None exists there — use a kprobe instead</td></tr><tr><td style=\"padding:0.5rem;\">Filter rejected</td><td style=\"padding:0.5rem;\">Field name not in the event's <code>format</code></td></tr></table><h4>Key takeaways</h4><ul><li>Tracepoints are <strong>static, developer-placed</strong> hooks with a <strong>stable ABI</strong> and named fields — prefer them over kprobes when one exists.</li><li>Disabled = a <strong>NOP</strong> guarded by a static key → near-zero overhead until enabled.</li><li><strong>TRACE_EVENT</strong> generates the <code>trace_*()</code> hook, the switch, and the <code>events/</code> interface.</li><li>Consume them via <strong>ftrace</strong> (events/), <strong>perf</strong>, or <strong>eBPF</strong> — one hook, many front-ends; add <strong>filters/triggers</strong> to stay efficient.</li></ul><p><strong>References:</strong></p><ul><li><a href=\"https://docs.kernel.org/trace/tracepoints.html\" target=\"_blank\">Linux Kernel: Using the Linux Kernel Tracepoints</a></li><li><a href=\"https://docs.kernel.org/trace/events.html\" target=\"_blank\">Linux Kernel: Event Tracing (format, filters, triggers)</a></li><li><a href=\"https://docs.kernel.org/trace/ftrace.html\" target=\"_blank\">Linux Kernel: ftrace</a></li><li><a href=\"https://lwn.net/Articles/379903/\" target=\"_blank\">LWN: Using the TRACE_EVENT() macro</a></li><li><a href=\"https://docs.kernel.org/staging/static-keys.html\" target=\"_blank\">Linux Kernel: Static keys (jump labels)</a></li></ul>"
    }
  ]
};
