/**
 * Topic: NIC Drivers
 */
const TOPIC_NIC_DRIVERS = {
  "id": "nic-drivers",
  "category": "drivers",
  "icon": "📡",
  "title": "NIC Drivers",
  "description": "Writing Linux network device drivers — net_device & net_device_ops, the TX/RX datapaths, NAPI polling, DMA ring buffers, sk_buff, interrupts & offloads, ethtool, with flow charts throughout",
  "keywords": [
    "nic driver", "network driver", "net_device", "net_device_ops", "napi",
    "sk_buff", "skb", "ndo_start_xmit", "netif_rx", "napi_poll", "rx ring",
    "tx ring", "descriptor ring", "dma", "netdev", "register_netdev",
    "ethtool", "gso", "tso", "checksum offload", "rss", "multiqueue",
    "netif_receive_skb", "napi_gro_receive", "phy", "mii", "interrupt"
  ],
  "sections": [
    {
      "title": "1. NIC Drivers & the Network Stack",
      "content": "<p>This topic is the <strong>driver author's</strong> view — how to write a Linux driver for a network interface card. For the protocol/config side (interfaces, addressing, firewalls) see the <strong>Network Management</strong> topic; for the physical layer/frames see <strong>Ethernet</strong>; for kernel-bypass fast paths see <strong>XDP</strong>. A NIC is the third major device class — unlike char/block it has <em>no</em> <code>/dev</code> node; it plugs into the networking stack.</p><h4>Where a NIC driver sits</h4><pre><code>┌───────────────────────────────────────────────────────────────┐\n│ USER SPACE   sockets (send/recv), apps                          │\n└─────────────────────────────────┬─────────────────────────────┘\n══════════════════════════════════▼════════════════════════════ user/kernel\n┌───────────────────────────────────────────────────────────────┐\n│ SOCKET / PROTOCOL STACK   TCP/UDP, IP, routing, netfilter        │\n├───────────────────────────────────────────────────────────────┤\n│ NET CORE   sk_buff mgmt, qdisc (traffic control), NAPI           │\n├───────────────────────────────────────────────────────────────┤\n│ YOUR NIC DRIVER  net_device + net_device_ops                     │\n│   TX: ndo_start_xmit()   RX: NAPI poll -> netif_receive_skb()    │\n├───────────────────────────────────────────────────────────────┤\n│ NIC HARDWARE   DMA descriptor rings, TX/RX FIFOs, MAC + PHY      │\n└─────────────────────────────────┬─────────────────────────────┘\n                                   ▼   the wire (see Ethernet topic)\n#\n# You implement net_device_ops. The net core gives you the socket layer,\n# protocol stack, queueing (qdisc), and NAPI polling for free.</code></pre><h4>The two central objects</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Object</th><th style=\"text-align:left; padding:0.5rem;\">Represents</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>struct net_device</code></td><td style=\"padding:0.5rem;\">One network interface (e.g. eth0) — its state, MAC, ops</td></tr><tr><td style=\"padding:0.5rem;\"><code>struct sk_buff</code> (skb)</td><td style=\"padding:0.5rem;\">One packet as it travels the stack (headers + data)</td></tr></table><p>Most NICs are PCIe devices, so a NIC driver is usually a <strong>PCI driver</strong> (see the PCI Drivers topic) whose <code>probe()</code> also registers a <code>net_device</code>.</p>"
    },
    {
      "title": "2. net_device & Registration",
      "content": "<p>A NIC driver allocates a <strong><code>net_device</code></strong>, fills in its operations and attributes, and registers it — which makes the interface (e.g. <code>eth0</code>) appear to the networking stack and user space.</p><h4>net_device_ops — the driver's callbacks</h4><pre><code>#include <linux/netdevice.h>\n#include <linux/etherdevice.h>\n\nstatic const struct net_device_ops my_netdev_ops = {\n    .ndo_open        = my_open,        // 'ip link set up' -> alloc rings, IRQ, start\n    .ndo_stop        = my_stop,        // 'ip link set down' -> stop, free\n    .ndo_start_xmit  = my_start_xmit,  // TRANSMIT one packet (skb)\n    .ndo_get_stats64 = my_get_stats,   // report counters\n    .ndo_set_rx_mode = my_set_rx_mode, // promisc / multicast filters\n    .ndo_set_mac_address = eth_mac_addr,\n    .ndo_validate_addr   = eth_validate_addr,\n};</code></pre><h4>Registration flow (inside a PCI probe)</h4><pre><code>pci probe(pdev)  ── enable device, map BARs, set_master, DMA mask (see PCI topic)\n   │\n   ▼\nalloc_etherdev(sizeof(priv))     ── allocate net_device + private area\n   │\n   ▼\nndev->netdev_ops = &my_netdev_ops;   set features, MAC, MTU\nSET_NETDEV_DEV(ndev, &pdev->dev);\n   │\n   ▼\nnetif_napi_add(ndev, &priv->napi, my_poll)   ── register the NAPI poller\n   │\n   ▼\nregister_netdev(ndev)            ── PUBLISH: 'eth0' now exists\n   │\n   ▼\ninterface is DOWN until 'ip link set eth0 up' -> ndo_open()\n#\n# Teardown: unregister_netdev(ndev); netif_napi_del(); free_netdev(ndev);</code></pre><h4>open()/stop() — the up/down path</h4><pre><code>static int my_open(struct net_device *ndev)\n{\n    struct my_priv *priv = netdev_priv(ndev);\n    // allocate TX/RX descriptor rings + DMA buffers (section 5)\n    // request the NIC interrupt (section 6)\n    napi_enable(&priv->napi);\n    netif_start_queue(ndev);      // allow the stack to hand us packets to TX\n    return 0;\n}\nstatic int my_stop(struct net_device *ndev)\n{\n    struct my_priv *priv = netdev_priv(ndev);\n    netif_stop_queue(ndev);       // stop accepting TX\n    napi_disable(&priv->napi);\n    // free IRQ + rings\n    return 0;\n}</code></pre>"
    },
    {
      "title": "3. sk_buff: The Packet Container",
      "content": "<p>Every packet in the kernel is an <strong><code>sk_buff</code></strong> (\"socket buffer\", skb). It holds the packet data plus metadata, and grows/shrinks headers as it moves through the layers without copying the payload.</p><h4>The skb data area</h4><pre><code># An skb points into a data buffer with movable boundaries:\n#\n#   ┌──────────┬──────────────┬───────────────┬──────────┐\n#   │ headroom │  headers      │  payload       │ tailroom │\n#   └──────────┴──────────────┴───────────────┴──────────┘\n#     ^head      ^data          ^tail            ^end\n#\n#   skb_push(skb, n)  -> grow toward head (PREPEND a header, e.g. Ethernet)\n#   skb_pull(skb, n)  -> shrink from data (STRIP a header on RX)\n#   skb_put(skb, n)   -> extend tail (append received data)\n#   skb_reserve(skb, n) -> set aside headroom before filling</code></pre><h4>Key skb fields a driver uses</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Field / helper</th><th style=\"text-align:left; padding:0.5rem;\">Meaning</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>skb->data</code>, <code>skb->len</code></td><td style=\"padding:0.5rem;\">Start + length of the packet</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>skb->protocol</code></td><td style=\"padding:0.5rem;\">Set on RX via <code>eth_type_trans()</code></td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>skb->ip_summed</code></td><td style=\"padding:0.5rem;\">Checksum-offload status</td></tr><tr><td style=\"padding:0.5rem;\"><code>skb_headlen</code> / frags</td><td style=\"padding:0.5rem;\">Linear vs paged (scatter-gather) data</td></tr></table><h4>Allocating & freeing</h4><pre><code>// RX: get a buffer to receive into\nstruct sk_buff *skb = netdev_alloc_skb_ip_align(ndev, len);\nskb_put(skb, len);                 // claim 'len' bytes of received data\nskb->protocol = eth_type_trans(skb, ndev);   // set protocol, pull eth header\n\n// TX completion: free the skb once the NIC has sent it\ndev_kfree_skb_any(skb);            // (or napi_consume_skb in poll context)\n// The stack allocates TX skbs; the driver frees them after transmit.\n// The driver allocates RX skbs; the stack frees them after delivery.</code></pre>"
    },
    {
      "title": "4. The Transmit (TX) Path",
      "content": "<p>When the stack has a packet to send, it calls your <code>ndo_start_xmit()</code>. You map the skb for DMA, hand it to the hardware TX ring, and free it later when the NIC signals completion.</p><h4>TX flow</h4><pre><code>socket send() ─► TCP/IP ─► qdisc (traffic control) ─► your ndo_start_xmit(skb)\n   │\n   ▼\nmap the skb data for DMA:  dma_map_single(dev, skb->data, skb->len, TO_DEVICE)\n   │\n   ▼\nwrite a TX DESCRIPTOR (dma addr + len + flags) into the TX ring\n   │\n   ▼\n\"kick\" the NIC (ring the doorbell register) ─► NIC DMA-reads + transmits\n   │\n   ▼\nif the TX ring is now full:  netif_stop_queue(ndev)   (back-pressure)\n   │\n   ▼  return NETDEV_TX_OK\n   ▼\nlater: TX-done IRQ ─► unmap DMA, dev_kfree_skb, netif_wake_queue() if space</code></pre><pre><code>static netdev_tx_t my_start_xmit(struct sk_buff *skb, struct net_device *ndev)\n{\n    struct my_priv *priv = netdev_priv(ndev);\n    dma_addr_t dma;\n    struct tx_desc *desc;\n\n    dma = dma_map_single(priv->dev, skb->data, skb->len, DMA_TO_DEVICE);\n    if (dma_mapping_error(priv->dev, dma)) {\n        dev_kfree_skb_any(skb);\n        ndev->stats.tx_dropped++;\n        return NETDEV_TX_OK;\n    }\n\n    desc = &priv->tx_ring[priv->tx_head];\n    desc->addr = cpu_to_le64(dma);\n    desc->len  = cpu_to_le16(skb->len);\n    desc->flags = cpu_to_le16(TXD_OWN | TXD_EOP);   // hand ownership to NIC\n    priv->tx_skb[priv->tx_head] = skb;              // remember for completion\n    priv->tx_head = (priv->tx_head + 1) % TX_RING_SIZE;\n\n    wmb();                                // ensure descriptor is visible first\n    writel(1, priv->regs + REG_TX_DOORBELL);   // kick the NIC\n\n    if (tx_ring_full(priv))\n        netif_stop_queue(ndev);           // stop until completions free space\n    return NETDEV_TX_OK;\n}\n// TX completion (in the NAPI poll or a TX IRQ):\n//   dma_unmap_single(...); napi_consume_skb(skb);\n//   if (netif_queue_stopped(ndev) && space) netif_wake_queue(ndev);</code></pre>"
    },
    {
      "title": "5. The Receive (RX) Path & NAPI",
      "content": "<p>Receiving at high packet rates with one interrupt per packet would melt the CPU. Linux uses <strong>NAPI</strong>: the first packet raises an interrupt, then the driver switches to <em>polling</em> to drain many packets per interrupt, re-enabling interrupts only when the ring drains.</p><h4>NAPI: interrupt → poll → re-arm (flow)</h4><pre><code>packet arrives ─► NIC DMA-writes it into an RX ring buffer ─► RX IRQ fires\n   │\n   ▼\nmy_isr():  disable NIC RX interrupts;  napi_schedule(&priv->napi)\n   │        (do NOT process packets in the hardirq -- just schedule)\n   ▼\nnet core calls my_poll(napi, budget)  in softirq context\n   │\n   ▼\nmy_poll: loop up to 'budget' packets:\n     - read an RX descriptor the NIC has filled\n     - build/refill an skb, dma_unmap, skb_put, eth_type_trans\n     - napi_gro_receive(napi, skb)   ── hand it up the stack\n   │\n   ├─ processed == budget  ─► return budget (poll again soon; stay polling)\n   └─ processed <  budget  ─► napi_complete_done(); RE-ENABLE RX interrupts\n#\n# One interrupt can absorb hundreds of packets -> huge efficiency win at\n# high load, while staying interrupt-driven (low latency) at low load.</code></pre><pre><code>static int my_poll(struct napi_struct *napi, int budget)\n{\n    struct my_priv *priv = container_of(napi, struct my_priv, napi);\n    int done = 0;\n\n    while (done < budget && rx_desc_ready(priv)) {\n        struct rx_desc *d = &priv->rx_ring[priv->rx_tail];\n        int len = le16_to_cpu(d->len);\n        struct sk_buff *skb;\n\n        dma_unmap_single(priv->dev, priv->rx_dma[priv->rx_tail],\n                         RX_BUF_SIZE, DMA_FROM_DEVICE);\n        skb = priv->rx_skb[priv->rx_tail];\n        skb_put(skb, len);\n        skb->protocol = eth_type_trans(skb, priv->ndev);\n\n        napi_gro_receive(napi, skb);      // deliver upward (GRO coalesces)\n        priv->ndev->stats.rx_packets++;\n\n        my_refill_rx(priv, priv->rx_tail);   // give the NIC a fresh buffer\n        priv->rx_tail = (priv->rx_tail + 1) % RX_RING_SIZE;\n        done++;\n    }\n\n    if (done < budget) {\n        napi_complete_done(napi, done);\n        my_enable_rx_irq(priv);           // ring drained -> re-arm interrupts\n    }\n    return done;\n}</code></pre><h4>GRO — Generic Receive Offload</h4><pre><code># napi_gro_receive() lets the stack MERGE consecutive same-flow packets into\n# one large skb before pushing it up -> fewer trips through TCP/IP = higher\n# throughput. It's the RX-side complement to TSO/GSO on TX (section 7).</code></pre>"
    },
    {
      "title": "6. DMA Rings & Interrupts",
      "content": "<p>NICs move packets by <strong>DMA</strong> using <strong>descriptor rings</strong> — circular arrays of descriptors shared between the driver and the NIC, each pointing to a packet buffer. (See the DMA topic for the mapping model.)</p><h4>The ring model</h4><pre><code># A ring is a circular buffer of DESCRIPTORS. Each descriptor has:\n#   - a DMA address of a packet buffer   - a length   - flags + an OWN bit\n#\n# The OWN bit hands a slot between the two sides:\n#   RX ring: driver posts empty buffers (OWN=NIC); NIC fills them (OWN=CPU)\n#   TX ring: driver posts full buffers (OWN=NIC); NIC sends, returns (OWN=CPU)\n#\n#        head (producer) ─┐         ┌─ tail (consumer)\n#   [d0][d1][d2][d3][d4][d5][d6][d7]  ... wraps around\n#\n# Coherent DMA for the descriptor ring (shared continuously); streaming DMA\n# maps for each packet buffer (mapped for one transfer). See the DMA topic.</code></pre><pre><code>// Allocate the descriptor ring (coherent) + buffers (in my_open):\npriv->tx_ring = dma_alloc_coherent(priv->dev,\n                    TX_RING_SIZE * sizeof(struct tx_desc),\n                    &priv->tx_ring_dma, GFP_KERNEL);\n// Tell the NIC where the ring lives:\nwritel(lower_32_bits(priv->tx_ring_dma), priv->regs + REG_TX_RING_LO);\nwritel(upper_32_bits(priv->tx_ring_dma), priv->regs + REG_TX_RING_HI);</code></pre><h4>The interrupt handler (thin — just schedule NAPI)</h4><pre><code>static irqreturn_t my_isr(int irq, void *data)\n{\n    struct my_priv *priv = data;\n    u32 status = readl(priv->regs + REG_IRQ_STATUS);\n    if (!status) return IRQ_NONE;                 // shared IRQ: not ours\n    writel(status, priv->regs + REG_IRQ_ACK);\n\n    if (status & (IRQ_RX | IRQ_TX)) {\n        my_disable_irq(priv);                     // mask; NAPI will re-arm\n        napi_schedule(&priv->napi);               // defer to poll (softirq)\n    }\n    return IRQ_HANDLED;\n}\n// Modern NICs use MSI-X with one vector per queue (see PCI Drivers) so each\n// RX/TX queue can interrupt a different CPU.</code></pre><h4>Multiqueue & RSS</h4><pre><code># High-speed NICs have MANY hardware queues. RSS (Receive Side Scaling) hashes\n# each flow to a queue so different CPUs handle different flows in parallel:\n#   alloc_etherdev_mq(sizeof(priv), NR_QUEUES)   // multiqueue net_device\n#   one NAPI + one MSI-X vector per queue\n# Tune from user space: ethtool -L / -X (see section 8).</code></pre>"
    },
    {
      "title": "7. Offloads, PHY & Features",
      "content": "<p>Modern NICs offload work from the CPU to hardware. A driver advertises which offloads it supports; the stack then skips that work in software.</p><h4>Common offloads</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Offload</th><th style=\"text-align:left; padding:0.5rem;\">Does</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Checksum (CSUM)</td><td style=\"padding:0.5rem;\">NIC computes/verifies IP/TCP checksums</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">TSO / GSO</td><td style=\"padding:0.5rem;\">Send one big buffer; NIC segments into MTU-sized packets</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">GRO / LRO</td><td style=\"padding:0.5rem;\">Coalesce received packets (RX side)</td></tr><tr><td style=\"padding:0.5rem;\">Scatter-gather</td><td style=\"padding:0.5rem;\">DMA a packet from multiple memory fragments</td></tr></table><pre><code>// Advertise features in probe(); the stack honors them per skb:\nndev->hw_features = NETIF_F_SG | NETIF_F_IP_CSUM | NETIF_F_TSO | NETIF_F_RXCSUM;\nndev->features   = ndev->hw_features;\n// On TX, check skb_is_gso(skb) / skb->ip_summed to see what the stack expects\n// the NIC to do for this packet.</code></pre><h4>The PHY & link state</h4><pre><code># The MAC (in the NIC) does framing; the PHY does the analog signaling and\n# auto-negotiation. Linux's PHYLIB manages the PHY over the MDIO/MII bus:\n#   phy_connect() in open(), and a link-change callback that calls\n#   netif_carrier_on()/off() so the stack knows if the cable is up.\n#\n#   link up   ─► netif_carrier_on(ndev)   -> 'ip link' shows LOWER_UP\n#   link down ─► netif_carrier_off(ndev)  -> stack stops sending</code></pre><h4>Statistics</h4><pre><code>// Maintain counters the stack/ethtool report:\nstatic void my_get_stats(struct net_device *ndev,\n                         struct rtnl_link_stats64 *s)\n{\n    struct my_priv *p = netdev_priv(ndev);\n    s->rx_packets = p->rx_packets;  s->tx_packets = p->tx_packets;\n    s->rx_bytes   = p->rx_bytes;    s->tx_bytes   = p->tx_bytes;\n    s->rx_errors  = p->rx_errors;   s->tx_errors  = p->tx_errors;\n}\n// Plus an ethtool_ops for driver info, ring sizes, link settings, etc.</code></pre>"
    },
    {
      "title": "8. Debugging & References",
      "content": "<h4>Is the driver bound / interface up?</h4><pre><code>ip -br link                       # interfaces + state (UP/LOWER_UP)\nethtool -i eth0                   # DRIVER name, version, firmware, bus-info\nethtool eth0                      # link speed/duplex, autoneg\nethtool -S eth0                   # per-driver HW statistics (rx/tx/errors/drops)\nlspci -k -s <bdf>                 # confirm your driver claimed the PCI NIC\ndmesg | grep <driver>             # probe/link/errors</code></pre><h4>Rings, queues & offloads (ethtool)</h4><pre><code>ethtool -g eth0                   # RX/TX ring sizes (and -G to change)\nethtool -l eth0                   # number of queues (and -L to change)\nethtool -k eth0                   # offload features on/off (and -K to toggle)\nethtool -x eth0                   # RSS hash indirection table\nethtool -c eth0                   # interrupt coalescing (and -C to tune)\n\n# See the packets themselves (kernel matches via BPF -- see eBPF/XDP topics):\nsudo tcpdump -i eth0 -c 20\ncat /proc/interrupts | grep eth0  # per-CPU IRQ counts (check MSI-X spread)</code></pre><h4>Common pitfalls</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Symptom</th><th style=\"text-align:left; padding:0.5rem;\">Cause</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">No packets, no throughput</td><td style=\"padding:0.5rem;\">Forgot <code>pci_set_master()</code> — DMA disabled (PCI topic)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">100% CPU at high pps</td><td style=\"padding:0.5rem;\">Processing in the hardirq instead of NAPI poll</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">TX hangs after a burst</td><td style=\"padding:0.5rem;\">Missing <code>netif_wake_queue()</code> after freeing TX slots</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Corrupt / stale packet data</td><td style=\"padding:0.5rem;\">Wrong DMA direction or missing unmap</td></tr><tr><td style=\"padding:0.5rem;\">Link never comes up</td><td style=\"padding:0.5rem;\">PHY not connected / no <code>netif_carrier_on()</code></td></tr></table><h4>Key takeaways</h4><ul><li>A NIC driver provides a <strong>net_device</strong> + <strong>net_device_ops</strong>; packets are <strong>sk_buff</strong>s. No <code>/dev</code> node.</li><li>TX: <code>ndo_start_xmit</code> → DMA map → TX ring → doorbell; free the skb on TX-done.</li><li>RX: use <strong>NAPI</strong> — IRQ schedules a poll that drains the RX ring and re-arms interrupts (efficient at high load).</li><li>Data moves via <strong>DMA descriptor rings</strong>; advertise <strong>offloads</strong> (CSUM/TSO/GRO) and manage the <strong>PHY</strong> for link state.</li></ul><p><strong>References:</strong></p><ul><li><a href=\"https://docs.kernel.org/networking/netdevices.html\" target=\"_blank\">Linux Kernel: Network devices, the Kernel, and You</a></li><li><a href=\"https://docs.kernel.org/networking/napi.html\" target=\"_blank\">Linux Kernel: NAPI</a></li><li><a href=\"https://docs.kernel.org/networking/driver.html\" target=\"_blank\">Linux Kernel: Network driver design (xmit path locking)</a></li><li><a href=\"https://docs.kernel.org/networking/scaling.html\" target=\"_blank\">Linux Kernel: Scaling in the Linux networking stack (RSS/RPS)</a></li><li><a href=\"https://lwn.net/Kernel/LDD3/\" target=\"_blank\">Linux Device Drivers, 3rd ed. — network drivers chapter</a></li></ul>"
    }
  ]
};
