/**
 * Topic: Kernel RCU (Read-Copy-Update)
 */
const TOPIC_KERNEL_RCU = {
  "id": "kernel-rcu",
  "icon": "🔄",
  "title": "Kernel RCU (Read-Copy-Update)",
  "description": "Deep dive into RCU — the Linux kernel lock-free read-side synchronization mechanism for scalable concurrent data access",
  "sections": [
    {
      "title": "1. What is RCU?",
      "content": "<p><strong>RCU (Read-Copy-Update)</strong> is the most important lock-free synchronization mechanism in the Linux kernel. It allows extremely fast, lock-free reads of shared data structures while writers create updated copies and defer destruction of old versions until all readers have finished.</p><h4>Core Principle</h4><pre><code># The \"Read-Copy-Update\" pattern:\n#\n# 1. READ:   Readers access shared data with zero overhead (no locks, no atomics)\n# 2. COPY:   Writer creates a new version of the data\n# 3. UPDATE: Writer atomically swaps the pointer to new version\n# 4. WAIT:   Writer waits for all pre-existing readers to finish (grace period)\n# 5. FREE:   Writer frees the old version (safe — no readers remain)\n\n# Timeline:\n#  Reader A: [rcu_read_lock ... reads OLD data ... rcu_read_unlock]\n#  Reader B:      [rcu_read_lock ... reads OLD data ... rcu_read_unlock]\n#  Reader C:              [rcu_read_lock ... reads NEW data ... rcu_read_unlock]\n#\n#  Writer:   publish NEW ──────▶ synchronize_rcu() ──▶ kfree(OLD)\n#                                (waits for A and B)    (safe now)</code></pre><h4>Why RCU Exists</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Problem with Locks</th><th style=\"text-align:left; padding:0.5rem;\">RCU Solution</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Reader-writer lock: readers still contend on lock cacheline</td><td style=\"padding:0.5rem;\">RCU readers have literally zero overhead (no shared state)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">rwlock doesn't scale beyond ~4 CPUs</td><td style=\"padding:0.5rem;\">RCU scales linearly to 1000+ CPUs</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Lock holder can sleep → priority inversion</td><td style=\"padding:0.5rem;\">RCU readers never block (preempt_disable only)</td></tr><tr><td style=\"padding:0.5rem;\">Deadlock risk with nested locks</td><td style=\"padding:0.5rem;\">RCU read sections can nest freely</td></tr></table><h4>Where RCU is Used in Linux</h4><ul><li><strong>Routing tables</strong> — Looked up on every packet (millions/sec)</li><li><strong>File descriptor table</strong> — Every syscall does fd lookup</li><li><strong>Module list</strong> — Checked during symbol lookup</li><li><strong>Process list (task_struct)</strong> — /proc, ps, kill</li><li><strong>VFS dcache</strong> — Path lookup (every open/stat)</li><li><strong>Network namespaces</strong> — Container isolation checks</li><li><strong>Radix trees / xarray</strong> — Page cache lookups</li></ul>"
    },
    {
      "title": "2. RCU API — Read Side",
      "content": "<h4>Read-Side Critical Section</h4><pre><code>#include &lt;linux/rcupdate.h&gt;\n\nstruct my_data {\n    int value;\n    char name[64];\n    struct rcu_head rcu;  // for deferred free\n};\n\nstruct my_data __rcu *global_ptr;  // __rcu annotation for sparse checking\n\n// === Reader (fast path — near-zero cost) ===\nvoid reader_function(void)\n{\n    struct my_data *p;\n\n    rcu_read_lock();  // marks start of read-side critical section\n                      // (disables preemption on non-PREEMPT_RT kernels)\n\n    p = rcu_dereference(global_ptr);  // load pointer with acquire semantics\n    if (p) {\n        printk(\"value = %d, name = %s\\n\", p->value, p->name);\n        // Safe to access all fields of *p within this section\n    }\n\n    rcu_read_unlock();  // end of read-side critical section\n    // After this point, p may become invalid (writer may free it)\n}</code></pre><h4>Key Rules for Readers</h4><ul><li><strong>Cannot sleep</strong> inside rcu_read_lock/unlock (classic RCU)</li><li><strong>Cannot block</strong> — no mutex, no memory allocation with GFP_KERNEL</li><li><strong>Can nest</strong> — rcu_read_lock() inside another rcu_read_lock() is fine</li><li><strong>Must not hold reference after rcu_read_unlock()</strong> — pointer may become invalid</li><li><strong>rcu_dereference() required</strong> — don't just dereference raw pointer (needs acquire barrier on ARM)</li></ul><h4>rcu_dereference() Variants</h4><pre><code>// Standard (inside rcu_read_lock)\np = rcu_dereference(global_ptr);\n\n// For pointers protected by a different lock\np = rcu_dereference_protected(global_ptr,\n        lockdep_is_held(&my_mutex));\n\n// For raw (unchecked) — only in special cases\np = rcu_dereference_raw(ptr);\n\n// Check if pointer is non-NULL atomically\nif (rcu_access_pointer(global_ptr))\n    // pointer has been set (but don't dereference without rcu_read_lock)</code></pre>"
    },
    {
      "title": "3. RCU API — Write Side",
      "content": "<h4>Publishing New Data</h4><pre><code>// === Writer (slow path — does the heavy lifting) ===\nvoid writer_update(int new_value, const char *new_name)\n{\n    struct my_data *new, *old;\n\n    // 1. Allocate new version\n    new = kmalloc(sizeof(*new), GFP_KERNEL);\n    new->value = new_value;\n    strscpy(new->name, new_name, sizeof(new->name));\n\n    // 2. Get old pointer (must hold write-side lock if multiple writers)\n    spin_lock(&writer_lock);\n    old = rcu_dereference_protected(global_ptr,\n              lockdep_is_held(&writer_lock));\n\n    // 3. Publish new version (atomic pointer swap with release semantics)\n    rcu_assign_pointer(global_ptr, new);\n    spin_unlock(&writer_lock);\n\n    // 4. Wait for all pre-existing readers to finish\n    synchronize_rcu();\n    // After this: ALL readers that saw \"old\" have exited their\n    // rcu_read_lock/unlock sections\n\n    // 5. Now safe to free old version\n    kfree(old);\n}</code></pre><h4>Deferred Free (call_rcu / kfree_rcu)</h4><pre><code>// synchronize_rcu() blocks the writer — sometimes not acceptable.\n// call_rcu() defers the free to after the grace period (non-blocking).\n\nstatic void my_data_free_callback(struct rcu_head *rcu)\n{\n    struct my_data *p = container_of(rcu, struct my_data, rcu);\n    kfree(p);\n}\n\nvoid writer_update_nonblocking(int new_value)\n{\n    struct my_data *new, *old;\n\n    new = kmalloc(sizeof(*new), GFP_KERNEL);\n    new->value = new_value;\n\n    spin_lock(&writer_lock);\n    old = rcu_dereference_protected(global_ptr, lockdep_is_held(&writer_lock));\n    rcu_assign_pointer(global_ptr, new);\n    spin_unlock(&writer_lock);\n\n    // Schedule free after grace period (non-blocking)\n    call_rcu(&old->rcu, my_data_free_callback);\n    // Writer continues immediately — callback runs later\n}\n\n// Even simpler (kernel 5.0+):\n// kfree_rcu(old, rcu);  // one-liner, auto-generates callback</code></pre><h4>API Summary</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Function</th><th style=\"text-align:left; padding:0.5rem;\">Side</th><th style=\"text-align:left; padding:0.5rem;\">Purpose</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>rcu_read_lock()</code></td><td style=\"padding:0.5rem;\">Reader</td><td style=\"padding:0.5rem;\">Begin read-side critical section</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>rcu_read_unlock()</code></td><td style=\"padding:0.5rem;\">Reader</td><td style=\"padding:0.5rem;\">End read-side critical section</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>rcu_dereference(ptr)</code></td><td style=\"padding:0.5rem;\">Reader</td><td style=\"padding:0.5rem;\">Load RCU-protected pointer (acquire)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>rcu_assign_pointer(ptr, new)</code></td><td style=\"padding:0.5rem;\">Writer</td><td style=\"padding:0.5rem;\">Publish new pointer (release)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>synchronize_rcu()</code></td><td style=\"padding:0.5rem;\">Writer</td><td style=\"padding:0.5rem;\">Wait for all readers (blocking)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>call_rcu(head, cb)</code></td><td style=\"padding:0.5rem;\">Writer</td><td style=\"padding:0.5rem;\">Defer callback to after grace period</td></tr><tr><td style=\"padding:0.5rem;\"><code>kfree_rcu(ptr, field)</code></td><td style=\"padding:0.5rem;\">Writer</td><td style=\"padding:0.5rem;\">Defer kfree to after grace period</td></tr></table>"
    },
    {
      "title": "4. RCU-Protected Linked Lists",
      "content": "<p>The most common RCU pattern in the kernel: read-mostly linked lists (routing tables, module list, etc.).</p><h4>list_head + RCU Macros</h4><pre><code>#include &lt;linux/rculist.h&gt;\n\nstruct my_entry {\n    int key;\n    int value;\n    struct list_head list;   // embedded list node\n    struct rcu_head rcu;     // for call_rcu\n};\n\nstatic LIST_HEAD(my_list);\nstatic DEFINE_SPINLOCK(list_lock);  // protects writers only\n\n// === Reader (lock-free) ===\nint rcu_lookup(int key)\n{\n    struct my_entry *entry;\n    int result = -ENOENT;\n\n    rcu_read_lock();\n    list_for_each_entry_rcu(entry, &my_list, list) {\n        if (entry->key == key) {\n            result = entry->value;\n            break;\n        }\n    }\n    rcu_read_unlock();\n    return result;\n}\n\n// === Insert (writer, needs lock vs other writers) ===\nvoid rcu_insert(int key, int value)\n{\n    struct my_entry *new = kmalloc(sizeof(*new), GFP_KERNEL);\n    new->key = key;\n    new->value = value;\n\n    spin_lock(&list_lock);\n    list_add_rcu(&new->list, &my_list);\n    spin_unlock(&list_lock);\n    // Readers immediately see new entry (no grace period needed for insert)\n}\n\n// === Delete (writer) ===\nvoid rcu_delete(int key)\n{\n    struct my_entry *entry;\n\n    spin_lock(&list_lock);\n    list_for_each_entry(entry, &my_list, list) {\n        if (entry->key == key) {\n            list_del_rcu(&entry->list);  // remove from list (readers still safe)\n            spin_unlock(&list_lock);\n            kfree_rcu(entry, rcu);       // free after grace period\n            return;\n        }\n    }\n    spin_unlock(&list_lock);\n}\n\n// === Replace (update in-place) ===\nvoid rcu_update(int key, int new_value)\n{\n    struct my_entry *old, *new;\n\n    new = kmalloc(sizeof(*new), GFP_KERNEL);\n\n    spin_lock(&list_lock);\n    list_for_each_entry(old, &my_list, list) {\n        if (old->key == key) {\n            *new = *old;           // copy\n            new->value = new_value; // modify\n            list_replace_rcu(&old->list, &new->list);\n            spin_unlock(&list_lock);\n            kfree_rcu(old, rcu);\n            return;\n        }\n    }\n    spin_unlock(&list_lock);\n    kfree(new);\n}</code></pre>"
    },
    {
      "title": "5. Grace Periods & RCU Internals",
      "content": "<h4>What is a Grace Period?</h4><pre><code># A grace period is the time window during which the kernel guarantees\n# that ALL CPUs have passed through at least one quiescent state.\n#\n# Quiescent state = a point where no RCU read-side critical section is active:\n#   - Context switch (schedule())\n#   - Return to userspace\n#   - Idle loop\n#\n# Once ALL CPUs have passed a quiescent state after the grace period starts,\n# the kernel knows no reader can still hold a reference to old data.\n\n┌──────────────────────────────────────────────────────────────────┐\n│ Grace Period Timeline                                             │\n│                                                                    │\n│ CPU 0: [rcu_read_lock...unlock]  qs  .........                   │\n│ CPU 1: ........  [rcu_read_lock......unlock]  qs  .........      │\n│ CPU 2: ..........  qs  ..................                          │\n│ CPU 3: ....  qs  ....................                              │\n│                                                                    │\n│         ▲ GP start                   ▲ GP end (all CPUs hit qs)  │\n│         │                             │                            │\n│  rcu_assign_pointer()          synchronize_rcu() returns          │\n│  (publish new)                 (safe to free old)                 │\n└──────────────────────────────────────────────────────────────────┘</code></pre><h4>RCU Flavors</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Flavor</th><th style=\"text-align:left; padding:0.5rem;\">Read Side</th><th style=\"text-align:left; padding:0.5rem;\">Grace Period</th><th style=\"text-align:left; padding:0.5rem;\">Use Case</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">RCU (classic)</td><td style=\"padding:0.5rem;\">rcu_read_lock/unlock</td><td style=\"padding:0.5rem;\">synchronize_rcu</td><td style=\"padding:0.5rem;\">Most kernel data structures</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">RCU-bh</td><td style=\"padding:0.5rem;\">rcu_read_lock_bh</td><td style=\"padding:0.5rem;\">synchronize_rcu_bh (deprecated)</td><td style=\"padding:0.5rem;\">Network RX softirq paths</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">RCU-sched</td><td style=\"padding:0.5rem;\">preempt_disable</td><td style=\"padding:0.5rem;\">synchronize_rcu (unified since 5.0)</td><td style=\"padding:0.5rem;\">Preempt-disabled code</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">SRCU</td><td style=\"padding:0.5rem;\">srcu_read_lock/unlock</td><td style=\"padding:0.5rem;\">synchronize_srcu</td><td style=\"padding:0.5rem;\">Readers that may sleep (I/O)</td></tr><tr><td style=\"padding:0.5rem;\">Tasks RCU</td><td style=\"padding:0.5rem;\">voluntary schedule()</td><td style=\"padding:0.5rem;\">synchronize_rcu_tasks</td><td style=\"padding:0.5rem;\">Tracing, BPF trampoline</td></tr></table><h4>SRCU (Sleepable RCU)</h4><pre><code>#include &lt;linux/srcu.h&gt;\n\nDEFINE_SRCU(my_srcu);\n\n// Reader (CAN sleep inside)\nvoid srcu_reader(void)\n{\n    int idx = srcu_read_lock(&my_srcu);\n    // ... can sleep, do I/O, take mutex ...\n    srcu_read_unlock(&my_srcu, idx);\n}\n\n// Writer\nvoid srcu_writer(void)\n{\n    rcu_assign_pointer(ptr, new);\n    synchronize_srcu(&my_srcu);  // wait for SRCU readers\n    kfree(old);\n}</code></pre>"
    },
    {
      "title": "6. RCU Performance & Overhead",
      "content": "<h4>Cost Comparison</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Mechanism</th><th style=\"text-align:left; padding:0.5rem;\">Read Overhead</th><th style=\"text-align:left; padding:0.5rem;\">Write Overhead</th><th style=\"text-align:left; padding:0.5rem;\">Scalability</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">spinlock</td><td style=\"padding:0.5rem;\">~10-50 ns (contended)</td><td style=\"padding:0.5rem;\">~10-50 ns</td><td style=\"padding:0.5rem;\">Poor (>4 CPUs)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">rwlock</td><td style=\"padding:0.5rem;\">~10-30 ns (cacheline bounce)</td><td style=\"padding:0.5rem;\">~10-50 ns</td><td style=\"padding:0.5rem;\">Moderate</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">RCU read</td><td style=\"padding:0.5rem;\">~0-2 ns (preempt_disable only)</td><td style=\"padding:0.5rem;\">N/A</td><td style=\"padding:0.5rem;\">Perfect (linear)</td></tr><tr><td style=\"padding:0.5rem;\">synchronize_rcu</td><td style=\"padding:0.5rem;\">N/A</td><td style=\"padding:0.5rem;\">~10-30 ms (grace period)</td><td style=\"padding:0.5rem;\">N/A (writer pays all)</td></tr></table><h4>When to Use RCU</h4><pre><code># Good fit for RCU:\n#   ✓ Read-mostly data (reads >> writes)\n#   ✓ Pointer-based data structures (lists, trees)\n#   ✓ Short read-side critical sections\n#   ✓ Readers must not block or sleep (use SRCU if they must)\n#   ✓ Performance-critical read path (network, VFS, scheduler)\n#\n# Bad fit for RCU:\n#   ✗ Write-heavy workloads (RCU write path is expensive)\n#   ✗ Large data that can't be atomically swapped (need copy)\n#   ✗ Need immediate effect of updates (RCU has grace period delay)\n#   ✗ Memory-constrained (old copies persist until grace period)</code></pre><h4>Memory Overhead</h4><pre><code># During grace period, OLD versions persist in memory:\n#   - If updates are frequent, many old copies accumulate\n#   - call_rcu callbacks queue up (bounded by CONFIG_RCU_LAZY)\n#   - Monitor with: /sys/kernel/debug/rcu/rcu_preempt/rcudata\n\n# Kernel tunables:\n# /sys/kernel/debug/rcu/rcu_preempt/\n#   rcudata          — per-CPU RCU state\n#   rcuboost         — priority boosting stats\n# /proc/sys/kernel/\n#   rcu_normal_wake_from_gp — grace period wake behavior</code></pre>"
    },
    {
      "title": "7. Debugging & Common Bugs",
      "content": "<h4>Common RCU Bugs</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Bug</th><th style=\"text-align:left; padding:0.5rem;\">Symptom</th><th style=\"text-align:left; padding:0.5rem;\">Fix</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Use-after-free (missing synchronize_rcu)</td><td style=\"padding:0.5rem;\">Random crash accessing freed memory</td><td style=\"padding:0.5rem;\">Add synchronize_rcu() or kfree_rcu() before/instead of kfree()</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Missing rcu_dereference()</td><td style=\"padding:0.5rem;\">Works on x86, crashes on ARM (missing acquire)</td><td style=\"padding:0.5rem;\">Always use rcu_dereference() to load RCU pointers</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Sleeping in rcu_read_lock section</td><td style=\"padding:0.5rem;\">RCU stall warning, softlockup</td><td style=\"padding:0.5rem;\">Use SRCU if sleeping needed, or restructure code</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Holding reference past rcu_read_unlock</td><td style=\"padding:0.5rem;\">Subtle corruption (pointer freed while still used)</td><td style=\"padding:0.5rem;\">Copy needed data before rcu_read_unlock, or take refcount</td></tr><tr><td style=\"padding:0.5rem;\">Missing rcu_assign_pointer()</td><td style=\"padding:0.5rem;\">Reader sees partially initialized object (ARM)</td><td style=\"padding:0.5rem;\">Always publish via rcu_assign_pointer() (has release barrier)</td></tr></table><h4>RCU Stall Warnings</h4><pre><code># If a CPU doesn't pass through a quiescent state for too long:\n# dmesg shows:\n#   \"INFO: rcu_preempt detected stalls on CPUs/tasks\"\n#   \"rcu_preempt: 3 callbacks\"\n#\n# Common causes:\n#   1. Infinite loop in rcu_read_lock section\n#   2. Interrupt handler running too long\n#   3. preempt_disable held too long\n#   4. stop_machine() blocking all CPUs\n\n# Tune stall timeout:\necho 60 > /sys/module/rcupdate/parameters/rcu_cpu_stall_timeout\n# Default: 21 seconds</code></pre><h4>Kernel Debug Config</h4><pre><code># Enable RCU debugging:\nCONFIG_PROVE_RCU=y            # check rcu_dereference context\nCONFIG_RCU_CPU_STALL_TIMEOUT=21  # stall warning timeout\nCONFIG_RCU_TRACE=y            # trace grace periods\nCONFIG_SPARSE_RCU_POINTER=y   # sparse checks for __rcu annotation\n\n# At runtime:\ncat /sys/kernel/debug/rcu/rcu_preempt/rcudata\n# View per-CPU RCU state (callbacks pending, grace period number)\n\n# Lock validator catches RCU misuse:\nCONFIG_PROVE_LOCKING=y\nCONFIG_LOCKDEP=y\n# Will report:\n#   \"suspicious RCU usage: rcu_dereference() used in non-RCU context\"</code></pre><p><strong>References:</strong></p><ul><li><a href=\"https://docs.kernel.org/RCU/index.html\" target=\"_blank\">Linux Kernel RCU Documentation</a></li><li><a href=\"https://lwn.net/Articles/262464/\" target=\"_blank\">LWN: What is RCU, Fundamentally?</a></li><li><a href=\"https://lwn.net/Articles/263130/\" target=\"_blank\">LWN: What is RCU? Part 2: Usage</a></li><li><a href=\"https://lwn.net/Articles/264090/\" target=\"_blank\">LWN: What is RCU? Part 3: The RCU API</a></li><li><a href=\"https://www.kernel.org/doc/Documentation/RCU/whatisRCU.rst\" target=\"_blank\">whatisRCU.rst (canonical kernel doc)</a></li><li><a href=\"https://paulmck.livejournal.com/\" target=\"_blank\">Paul McKenney (RCU maintainer) Blog</a></li></ul>"
    }
  ]
};
