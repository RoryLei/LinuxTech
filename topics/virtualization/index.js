/**
 * Topic: Virtualization (QEMU / KVM / libvirt)
 */
const TOPIC_VIRTUALIZATION = {
  "id": "virtualization",
  "category": "tooling",
  "icon": "🖥️",
  "title": "Virtualization (QEMU/KVM/libvirt)",
  "description": "Linux virtualization from the ground up — how QEMU, KVM and libvirt fit together, hardware-assisted virtualization, the device model, virtio, VM networking & storage, and performance tuning, with flow charts throughout",
  "keywords": [
    "virtualization", "qemu", "kvm", "libvirt", "virsh", "virt-manager",
    "hypervisor", "vm", "virtual machine", "guest", "host", "vcpu",
    "vmx", "svm", "ept", "virtio", "virtio-net", "virtio-blk", "vhost",
    "vfio", "pci passthrough", "tap", "bridge", "qcow2", "cloud-init",
    "domain xml", "device model", "paravirtualization", "nested", "type-1", "type-2"
  ],
  "sections": [
    {
      "title": "1. The Virtualization Stack",
      "content": "<p><strong>Virtualization</strong> runs a full guest operating system inside a host, isolated as a virtual machine (VM). On Linux the standard open-source stack is three cooperating layers: <strong>KVM</strong> (the in-kernel hypervisor), <strong>QEMU</strong> (the user-space machine emulator/device model), and <strong>libvirt</strong> (the management API and tools). This topic covers how they fit together.</p><h4>VM vs container</h4><pre><code># Virtual machine: a full guest KERNEL + userland, isolated by a hypervisor.\n#   strong isolation, run any OS, higher overhead (see this topic).\n# Container: shares the HOST kernel, isolated by namespaces + cgroups.\n#   lightweight, Linux-on-Linux only (see the Docker topic).\n# Use VMs for strong isolation / different OSes; containers for density.</code></pre><h4>Type-1 vs Type-2 — and where KVM sits</h4><pre><code># Type-1 (bare metal): hypervisor runs directly on hardware (ESXi, Xen).\n# Type-2 (hosted):    hypervisor runs on a host OS (VirtualBox).\n#\n# KVM is a HYBRID: it turns the Linux kernel ITSELF into a type-1-class\n# hypervisor. Linux is both the host OS and the hypervisor.</code></pre><h4>How the three layers fit</h4><pre><code>┌───────────────────────────────────────────────────────────────┐\n│ MANAGEMENT   virsh / virt-manager / virt-install / cloud tools  │\n└─────────────────────────────────┬─────────────────────────────┘\n                                   │ libvirt API (define/start/stop domains)\n                                   ▼\n┌───────────────────────────────────────────────────────────────┐\n│ libvirtd     parses domain XML, builds the QEMU command line,   │\n│              manages networks, storage pools, lifecycle          │\n└─────────────────────────────────┬─────────────────────────────┘\n                                   │ launches one process per VM\n                                   ▼\n┌───────────────────────────────────────────────────────────────┐\n│ QEMU (user space)   emulates the machine: devices, BIOS, I/O;   │\n│                     runs guest CPU via /dev/kvm                  │\n└─────────────────────────────────┬─────────────────────────────┘\n                                   │ ioctl(/dev/kvm): run vCPU\n══════════════════════════════════▼════════════════════════════ user/kernel\n┌───────────────────────────────────────────────────────────────┐\n│ KVM (kernel module)  uses CPU virt extensions to run guest code │\n│                      natively; exits to QEMU for device I/O     │\n├───────────────────────────────────────────────────────────────┤\n│ HARDWARE   CPU with VT-x/AMD-V, IOMMU, etc.                     │\n└───────────────────────────────────────────────────────────────┘</code></pre><h4>Who does what</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Layer</th><th style=\"text-align:left; padding:0.5rem;\">Role</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><strong>KVM</strong></td><td style=\"padding:0.5rem;\">Runs guest CPU/memory natively via hardware virt (the hypervisor)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><strong>QEMU</strong></td><td style=\"padding:0.5rem;\">Emulates devices, firmware, I/O; the VM is a QEMU process</td></tr><tr><td style=\"padding:0.5rem;\"><strong>libvirt</strong></td><td style=\"padding:0.5rem;\">Management API/CLI, XML config, networks &amp; storage</td></tr></table>"
    },
    {
      "title": "2. KVM: Hardware-Assisted Virtualization",
      "content": "<p><strong>KVM (Kernel-based Virtual Machine)</strong> is a Linux kernel module that exposes the CPU's virtualization extensions (<strong>Intel VT-x</strong> / <strong>AMD-V</strong>) through <code>/dev/kvm</code>. It lets guest code run <em>directly on the physical CPU</em> at near-native speed, only trapping to software when the guest touches a device.</p><h4>Check your host supports it</h4><pre><code>egrep -c '(vmx|svm)' /proc/cpuinfo    # >0 = CPU has VT-x (vmx) or AMD-V (svm)\nlsmod | grep kvm                      # kvm + kvm_intel / kvm_amd loaded?\nls -l /dev/kvm                        # the interface QEMU opens\nkvm-ok                                 # (Ubuntu) friendly verdict\n# Also enable virtualization in the BIOS/UEFI if vmx/svm is missing.</code></pre><h4>The vCPU run loop (guest exit/entry)</h4><pre><code>QEMU: ioctl(vcpu_fd, KVM_RUN)          ── \"run the guest\"\n   │\n   ▼\nKVM enters GUEST MODE ── guest instructions run NATIVELY on the CPU\n   │\n   │  guest does something that needs handling (I/O, MMIO, halt, ...)\n   ▼\nVM EXIT ── CPU traps back into KVM with an exit reason\n   │\n   ├─ KVM can handle it (e.g. in-kernel APIC) ─► resume guest (fast)\n   │\n   └─ needs a device ─► return to QEMU\n            │\n            ▼\n        QEMU emulates the device access, then ioctl(KVM_RUN) again\n#\n# Fewer VM exits = faster. This is WHY paravirtualized 'virtio' devices\n# (section 5) matter -- they minimize expensive exits vs emulating real HW.</code></pre><h4>What KVM accelerates</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Resource</th><th style=\"text-align:left; padding:0.5rem;\">Hardware feature</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">CPU execution</td><td style=\"padding:0.5rem;\">VT-x / AMD-V (guest runs natively)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Memory (guest→host phys)</td><td style=\"padding:0.5rem;\">EPT / NPT (nested page tables)</td></tr><tr><td style=\"padding:0.5rem;\">Device passthrough</td><td style=\"padding:0.5rem;\">IOMMU (VT-d / AMD-Vi) — see section 8</td></tr></table><pre><code># Without KVM, QEMU can still run a VM by EMULATING the CPU (TCG) -- correct\n# but ~10-50x slower. Use '-accel kvm' (or '-enable-kvm') for real speed;\n# TCG is for emulating a DIFFERENT architecture or when no VT is available.</code></pre>"
    },
    {
      "title": "3. QEMU: Emulator & Device Model",
      "content": "<p><strong>QEMU</strong> is the user-space half. It creates the virtual machine, provides the firmware (BIOS/UEFI) and <strong>emulated devices</strong> (disk, NIC, display, USB), maps guest RAM, and drives the vCPUs through KVM. Each running VM is one QEMU process.</p><h4>A minimal VM on the command line</h4><pre><code>qemu-system-x86_64 \\\n  -accel kvm \\                       # use KVM acceleration (else slow TCG)\n  -cpu host \\                        # expose host CPU features to the guest\n  -smp 4 \\                           # 4 vCPUs\n  -m 4096 \\                          # 4 GiB RAM\n  -drive file=disk.qcow2,if=virtio \\ # virtio disk (fast paravirt I/O)\n  -netdev user,id=n0 -device virtio-net,netdev=n0 \\   # virtio NIC\n  -display gtk                       # a window (or -nographic for serial)</code></pre><h4>QEMU accelerators</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Accel</th><th style=\"text-align:left; padding:0.5rem;\">Use</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>kvm</code></td><td style=\"padding:0.5rem;\">Same-arch, hardware-accelerated (production)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>tcg</code></td><td style=\"padding:0.5rem;\">Pure emulation — run a DIFFERENT arch (e.g. ARM on x86)</td></tr><tr><td style=\"padding:0.5rem;\"><code>hvf</code> / <code>whpx</code></td><td style=\"padding:0.5rem;\">macOS / Windows host accelerators</td></tr></table><h4>Emulate other architectures</h4><pre><code># QEMU can emulate a whole foreign machine (no KVM -> TCG):\nqemu-system-aarch64 -M virt -cpu cortex-a72 -m 2048 -kernel Image ...\n# Great for kernel dev / cross-arch testing (see the LFS / kernel topics).\n\n# User-mode emulation: run a single foreign BINARY on your host:\nqemu-aarch64 ./arm-binary        # via binfmt_misc, transparently in Docker too</code></pre><h4>Disk images</h4><pre><code>qemu-img create -f qcow2 disk.qcow2 40G    # thin, growable, snapshots\nqemu-img info disk.qcow2\nqemu-img snapshot -c snap1 disk.qcow2      # internal snapshot\nqemu-img convert -O qcow2 in.raw out.qcow2\n# qcow2 = copy-on-write, sparse, supports snapshots; raw = plain, fastest.</code></pre>"
    },
    {
      "title": "4. libvirt: Managing VMs",
      "content": "<p>Driving QEMU by hand is fine for one VM; <strong>libvirt</strong> manages many. It provides a stable API, an XML description of each VM (a <em>domain</em>), and tools (<code>virsh</code>, <code>virt-manager</code>) that also handle networks and storage.</p><h4>The management flow</h4><pre><code>you: virsh define vm.xml   (or virt-install / virt-manager GUI)\n   │\n   ▼\nlibvirtd stores the DOMAIN XML (the VM's definition)\n   │\n   ▼\nvirsh start vm\n   │\n   ▼\nlibvirtd translates the XML into a QEMU command line + sets up\n   networking (tap/bridge) and storage (volumes), then launches QEMU\n   │\n   ▼\nQEMU process runs the VM (accelerated by KVM)\n   │\n   ▼\nvirsh console/shutdown/destroy/migrate manage its lifecycle</code></pre><h4>Everyday virsh</h4><pre><code>virsh list --all                # all domains + state\nvirsh define vm.xml             # register a VM from XML\nvirsh start  vm                 # boot it\nvirsh shutdown vm               # graceful (ACPI)\nvirsh destroy  vm               # force off (like pulling power)\nvirsh console vm                # attach to its serial console\nvirsh edit vm                   # edit the domain XML (revalidated)\nvirsh dumpxml vm                # show the full XML\nvirsh snapshot-create-as vm snap1   # snapshot\nvirsh domifaddr vm              # the VM's IP address(es)</code></pre><h4>The domain XML (excerpt)</h4><pre><code>&lt;domain type='kvm'&gt;\n  &lt;name&gt;web01&lt;/name&gt;\n  &lt;memory unit='GiB'&gt;4&lt;/memory&gt;\n  &lt;vcpu&gt;4&lt;/vcpu&gt;\n  &lt;os&gt;&lt;type arch='x86_64'&gt;hvm&lt;/type&gt;&lt;/os&gt;\n  &lt;devices&gt;\n    &lt;disk type='file' device='disk'&gt;\n      &lt;driver name='qemu' type='qcow2'/&gt;\n      &lt;source file='/var/lib/libvirt/images/web01.qcow2'/&gt;\n      &lt;target dev='vda' bus='virtio'/&gt;\n    &lt;/disk&gt;\n    &lt;interface type='network'&gt;\n      &lt;source network='default'/&gt;\n      &lt;model type='virtio'/&gt;\n    &lt;/interface&gt;\n  &lt;/devices&gt;\n&lt;/domain&gt;</code></pre><h4>Fast provisioning</h4><pre><code>virt-install --name web01 --memory 4096 --vcpus 4 \\\n  --disk size=40 --os-variant ubuntu24.04 \\\n  --cdrom /isos/ubuntu.iso\n# For cloud images, cloud-init injects SSH keys / users / network at first\n# boot -- the standard way to provision reproducible VMs at scale.</code></pre>"
    },
    {
      "title": "5. virtio: Paravirtualized Devices",
      "content": "<p>Emulating a <em>real</em> NIC or disk means the guest driver pokes registers that each cause an expensive VM exit. <strong>virtio</strong> is a paravirtualization standard: the guest runs a <em>virtio-aware</em> driver that talks to the hypervisor through a shared-memory ring, minimizing exits — far faster.</p><h4>Emulated vs virtio</h4><pre><code># Emulated (e.g. -device e1000): guest thinks it's a real Intel NIC.\n#   compatible with any OS, but SLOW (many exits per packet).\n# virtio (-device virtio-net):   guest KNOWS it's virtual, uses a ring buffer.\n#   fewer exits, near-native throughput -- the default for Linux guests.</code></pre><h4>How a virtio transfer avoids exits</h4><pre><code>guest virtio driver places buffers on a shared VIRTQUEUE (ring in memory)\n   │\n   ▼\nguest \"kicks\" the host (one notification, not per-register pokes)\n   │\n   ▼\nhost side (QEMU, or the in-kernel 'vhost') consumes the ring, does the I/O\n   │\n   ▼\nhost places results back on the ring + interrupts the guest ONCE\n#\n# One kick + one IRQ per BATCH of buffers, instead of an exit per byte/register.</code></pre><h4>Common virtio devices</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Device</th><th style=\"text-align:left; padding:0.5rem;\">Provides</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>virtio-net</code></td><td style=\"padding:0.5rem;\">Fast paravirt NIC</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>virtio-blk</code> / <code>virtio-scsi</code></td><td style=\"padding:0.5rem;\">Fast paravirt storage</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>virtio-balloon</code></td><td style=\"padding:0.5rem;\">Reclaim guest memory for the host</td></tr><tr><td style=\"padding:0.5rem;\"><code>virtio-rng</code></td><td style=\"padding:0.5rem;\">Entropy from the host</td></tr></table><h4>vhost — moving the datapath into the kernel</h4><pre><code># vhost-net handles the virtio-net ring IN THE KERNEL, bypassing QEMU\n# user space entirely for packets -> even fewer context switches.\n# vDPA / vhost-user push this further (SmartNIC datapaths, DPDK).\n# In the domain XML, virtio + vhost is on by default for interfaces.</code></pre>"
    },
    {
      "title": "6. VM Networking",
      "content": "<p>A VM's virtual NIC connects to the host through a backend. The two common models are <strong>NAT</strong> (simple, outbound) and <strong>bridged</strong> (VM is a first-class device on the LAN).</p><h4>NAT (the libvirt 'default' network)</h4><pre><code># libvirt's default network: a host bridge (virbr0) + dnsmasq (DHCP/DNS) +\n# NAT via nftables. Guests get a private IP and reach the outside; the LAN\n# can't reach them directly. Zero setup -- great for dev.\nvirsh net-list --all\nvirsh net-dumpxml default          # subnet, DHCP range\n#\n#   guest ──virtio-net──► TAP ──► virbr0 (NAT) ──► host uplink ──► internet</code></pre><h4>Bridged (VM on the physical LAN)</h4><pre><code># The VM's tap joins a bridge tied to the host's real NIC, so the VM gets an\n# IP from the LAN's DHCP and is reachable like any physical host.\n#\n#   guest ──virtio-net──► TAP ──► br0 ──► eth0 ──► physical LAN\n#\n# Set up br0 with NetworkManager/netplan (see the Networking topic), then in\n# the domain XML:\n#   <interface type='bridge'><source bridge='br0'/><model type='virtio'/></interface></code></pre><h4>The datapath</h4><pre><code>guest kernel ─► virtio-net driver ─► virtqueue\n   │\n   ▼\nvhost-net (host kernel) ─► TAP device ─► Linux BRIDGE\n   │\n   ├─ NAT mode:     bridge -> nftables MASQUERADE -> uplink\n   └─ bridged mode: bridge -> physical NIC -> LAN\n#\n# A TAP is a virtual L2 interface the hypervisor reads/writes; the bridge is\n# a software switch (see the Networking topic's bridges/namespaces section).</code></pre><h4>Networking models at a glance</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Mode</th><th style=\"text-align:left; padding:0.5rem;\">VM reachable from LAN?</th><th style=\"text-align:left; padding:0.5rem;\">Use</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">NAT (default)</td><td style=\"padding:0.5rem;\">No (outbound only)</td><td style=\"padding:0.5rem;\">Dev laptops, quick VMs</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Bridged</td><td style=\"padding:0.5rem;\">Yes</td><td style=\"padding:0.5rem;\">Servers, VMs acting as hosts</td></tr><tr><td style=\"padding:0.5rem;\">Isolated</td><td style=\"padding:0.5rem;\">No (VM↔VM only)</td><td style=\"padding:0.5rem;\">Private test networks</td></tr></table>"
    },
    {
      "title": "7. Storage, Snapshots & Migration",
      "content": "<h4>Disk formats & backing files</h4><pre><code>qemu-img create -f qcow2 base.qcow2 40G\n# COPY-ON-WRITE overlay: many VMs share one read-only base image, each\n# writing only its diffs -> fast cloning, low disk use:\nqemu-img create -f qcow2 -b base.qcow2 -F qcow2 vm1.qcow2\n#\n#   base.qcow2 (read-only golden image)\n#      ▲            ▲\n#   vm1.qcow2    vm2.qcow2   (only store per-VM changes)</code></pre><h4>Storage pools (libvirt)</h4><pre><code>virsh pool-list                 # storage pools (dirs, LVM, Ceph/RBD, NFS...)\nvirsh vol-list default          # volumes in a pool\nvirsh vol-create-as default web01.qcow2 40G --format qcow2\n# A pool abstracts WHERE images live; volumes are the individual disks.</code></pre><h4>Snapshots</h4><pre><code>virsh snapshot-create-as web01 clean \"fresh install\"\nvirsh snapshot-list web01\nvirsh snapshot-revert web01 clean       # roll back to a known-good state\n# Disk-only vs full (disk + RAM) snapshots; full snapshots capture a running\n# VM's memory so you resume exactly where you were.</code></pre><h4>Live migration</h4><pre><code># Move a RUNNING VM to another host with minimal downtime (shared storage or\n# storage migration). KVM copies memory pages iteratively, then a brief pause\n# to transfer the last dirty pages + CPU state:\nvirsh migrate --live web01 qemu+ssh://host2/system\n#\n#   host1: [running VM] ──iterative memory copy──► host2\n#                        ──final dirty pages + vCPU state (brief pause)──►\n#   host2: [VM resumes]   host1 VM is torn down\n# Enables maintenance and load-balancing without stopping the guest.</code></pre>"
    },
    {
      "title": "8. Passthrough, Performance & Debugging",
      "content": "<h4>Device passthrough (VFIO)</h4><pre><code># Give a VM DIRECT, exclusive access to a physical PCI device (GPU, NIC, NVMe)\n# for near-native performance, using the IOMMU + vfio-pci:\n#   1. enable IOMMU:  kernel cmdline  intel_iommu=on  (or amd_iommu=on)\n#   2. bind the device to vfio-pci instead of its normal driver:\necho vfio-pci | sudo tee /sys/bus/pci/devices/0000:03:00.0/driver_override\n#   3. add a <hostdev> to the domain XML referencing its BDF.\n# The IOMMU (see the DMA topic) isolates the device's DMA to the VM's memory.\n# 'IOMMU groups' must be passed as a whole:\nfor g in /sys/kernel/iommu_groups/*/devices/*; do echo \"$g\"; done</code></pre><h4>Performance tuning</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Technique</th><th style=\"text-align:left; padding:0.5rem;\">Helps</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">virtio + vhost</td><td style=\"padding:0.5rem;\">I/O throughput (fewer exits)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">vCPU pinning + NUMA</td><td style=\"padding:0.5rem;\">Latency, cache locality (see Process topic)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Huge pages</td><td style=\"padding:0.5rem;\">Guest memory TLB efficiency</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>-cpu host</code></td><td style=\"padding:0.5rem;\">Expose native CPU features to the guest</td></tr><tr><td style=\"padding:0.5rem;\">VFIO passthrough</td><td style=\"padding:0.5rem;\">Near-native GPU/NIC performance</td></tr></table><h4>Nested virtualization</h4><pre><code># Run KVM inside a VM (useful for CI / labs). Enable on the host:\ncat /sys/module/kvm_intel/parameters/nested    # Y = enabled (or kvm_amd)\n# Then expose virt features to the guest with -cpu host / <cpu mode='host-passthrough'>.</code></pre><h4>Inspecting & debugging</h4><pre><code>virsh domstats web01            # live CPU/memory/block/net stats\nvirsh dominfo web01\njournalctl -u libvirtd          # libvirt daemon logs\ncat /var/log/libvirt/qemu/web01.log    # the exact QEMU command + errors\nvirsh qemu-monitor-command web01 --hmp 'info registers'   # QEMU monitor\n# Debug a guest KERNEL: QEMU '-s -S' starts a gdb stub on :1234, then\n# 'gdb vmlinux' + 'target remote :1234' -- boot-time kernel debugging.\n# 'perf kvm' and 'kvm_stat' show VM-exit reasons/counts for tuning.</code></pre><h4>Key takeaways</h4><ul><li><strong>KVM</strong> runs guest CPU/memory natively via VT-x/AMD-V; <strong>QEMU</strong> emulates devices; <strong>libvirt</strong> manages it all.</li><li>Fewer <strong>VM exits</strong> = faster — that's why <strong>virtio</strong> (paravirtualized devices) is the default.</li><li>Networking is <strong>NAT</strong> (simple) or <strong>bridged</strong> (VM on the LAN) via a tap + Linux bridge.</li><li><strong>VFIO passthrough</strong> gives near-native device performance using the IOMMU.</li></ul><p><strong>References:</strong></p><ul><li><a href=\"https://www.qemu.org/docs/master/\" target=\"_blank\">QEMU documentation</a></li><li><a href=\"https://docs.kernel.org/virt/kvm/index.html\" target=\"_blank\">Linux Kernel: KVM documentation</a></li><li><a href=\"https://libvirt.org/docs.html\" target=\"_blank\">libvirt documentation</a></li><li><a href=\"https://libvirt.org/formatdomain.html\" target=\"_blank\">libvirt: Domain XML format</a></li><li><a href=\"https://docs.oasis-open.org/virtio/virtio/v1.2/virtio-v1.2.html\" target=\"_blank\">virtio specification (OASIS)</a></li></ul>"
    }
  ]
};
