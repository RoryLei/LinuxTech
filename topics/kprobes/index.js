/**
 * Topic: kprobes
 */
const TOPIC_KPROBES = {
  "id": "kprobes",
  "category": "kernel",
  "icon": "📍",
  "title": "kprobes",
  "description": "Dynamic kernel instrumentation with kprobes — how a probe is planted and fires, kprobe/kretprobe handlers, kprobe events via ftrace, and how eBPF/perf build on them, with flow charts throughout",
  "keywords": [
    "kprobes", "kprobe", "kretprobe", "dynamic instrumentation", "breakpoint",
    "pre_handler", "post_handler", "register_kprobe", "int3", "trap",
    "optimized kprobe", "jump optimization", "kprobe events", "kallsyms",
    "return probe", "instrumentation", "trace", "perf probe", "fprobe",
    "symbol", "offset", "kernel debugging", "live tracing"
  ],
  "sections": [
    {
      "title": "1. What are kprobes?",
      "content": "<p><strong>kprobes</strong> let you dynamically insert instrumentation at almost <em>any</em> instruction in a running kernel — no recompile, no reboot. You register a handler that fires whenever execution reaches the probed address, inspect (or modify) registers and arguments, then let the kernel continue. It's the foundation of much of Linux's live tracing.</p><h4>The three probe types</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Type</th><th style=\"text-align:left; padding:0.5rem;\">Fires</th><th style=\"text-align:left; padding:0.5rem;\">Use</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><strong>kprobe</strong></td><td style=\"padding:0.5rem;\">At a chosen instruction (usually a function entry)</td><td style=\"padding:0.5rem;\">Inspect arguments on entry</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><strong>kretprobe</strong></td><td style=\"padding:0.5rem;\">On function RETURN</td><td style=\"padding:0.5rem;\">Capture return value / latency</td></tr><tr><td style=\"padding:0.5rem;\"><strong>(jprobe)</strong></td><td style=\"padding:0.5rem;\">Deprecated / removed</td><td style=\"padding:0.5rem;\">— use a kprobe instead</td></tr></table><h4>Why kprobes exist</h4><pre><code># Static instrumentation (tracepoints) only exists where a developer put it.\n# kprobes are DYNAMIC: probe any address the kernel exposes, at runtime.\n#   - debug a production kernel without rebuilding it\n#   - measure how often/how long an internal function runs\n#   - inspect arguments of a function that has no tracepoint\n#\n# They are the low-level engine under 'perf probe', kprobe-based bpftrace/BCC\n# programs, and 'kprobe events' in ftrace (sections 5-6).</code></pre><h4>Where kprobes fit</h4><pre><code>┌───────────────────────────────────────────────────────────────┐\n│ USERS:  perf probe | bpftrace/BCC (kprobe progs) | ftrace kprobe │\n│         events | your own kernel module                          │\n└─────────────────────────────────┬─────────────────────────────┘\n                                   ▼  all sit on top of...\n┌───────────────────────────────────────────────────────────────┐\n│ KPROBES CORE   plant a probe at an address, run your handler,    │\n│                single-step the original insn, resume             │\n├───────────────────────────────────────────────────────────────┤\n│ CPU   breakpoint/trap (int3 on x86) + single-step machinery      │\n└───────────────────────────────────────────────────────────────┘</code></pre>"
    },
    {
      "title": "2. How a kprobe Fires (the mechanism)",
      "content": "<p>The magic: kprobes replaces the instruction at the probed address with a <strong>breakpoint</strong> (<code>int3</code> on x86). When the CPU hits it, it traps into the kprobes core, which runs your handler, then <em>single-steps the original instruction</em> and resumes — so the kernel behaves exactly as before, plus your handler ran.</p><h4>Planting a probe</h4><pre><code>register_kprobe(&kp)\n   │\n   ▼\nsave the ORIGINAL instruction byte(s) at the target address\n   │\n   ▼\noverwrite it with a BREAKPOINT (int3)  ── atomically, safe on live CPUs\n   │\n   ▼\nthe probe is armed; any CPU reaching that address will trap</code></pre><h4>Firing flow</h4><pre><code>kernel execution reaches the probed address\n   │\n   ▼\nCPU executes the breakpoint ─► TRAP into the kprobes handler\n   │\n   ▼\nkprobes runs your PRE_HANDLER(regs)   ── inspect args/registers\n   │\n   ▼\nsingle-step the SAVED original instruction (the one we replaced)\n   │\n   ▼\nkprobes runs your POST_HANDLER(regs)  ── after the insn executed\n   │\n   ▼\nresume normal kernel execution at the next instruction\n#\n# Net effect: the probed code runs unchanged; your handlers ran around it.</code></pre><h4>Optimized kprobes (jump optimization)</h4><pre><code># A breakpoint trap is relatively expensive. When safe, kprobes UPGRADES the\n# int3 to a JUMP to a trampoline -> much lower overhead (no trap):\n#\n#   slow path:  ... int3 ... (trap, handler, single-step, return)\n#   optimized:  ... jmp trampoline ...   (handler runs, then jmp back)\n#\n# This happens automatically when conditions allow; you don't manage it.\ncat /proc/sys/debug/kprobes-optimization    # 1 = optimization enabled</code></pre><h4>kretprobe: catching returns</h4><pre><code># A kretprobe plants a kprobe at function ENTRY that hijacks the return\n# address, substituting a trampoline. When the function returns, control\n# lands in the trampoline -> your return handler runs -> real return proceeds:\n#\n#   entry kprobe: save real return addr, install trampoline addr\n#   function runs ...\n#   function returns ─► trampoline ─► your ret_handler(regs) ─► real return\n# This is how you measure a function's return value and latency.</code></pre>"
    },
    {
      "title": "3. Writing a kprobe (kernel module)",
      "content": "<p>The lowest-level way to use kprobes is a small kernel module that registers a <code>struct kprobe</code> with pre/post handlers. (Most people use the higher-level interfaces in sections 5–6, but this shows the mechanism directly.)</p><h4>A basic kprobe</h4><pre><code>#include <linux/module.h>\n#include <linux/kprobes.h>\n\nstatic struct kprobe kp = {\n    .symbol_name = \"do_sys_openat2\",   // probe this kernel function by name\n    // or: .addr = (kprobe_opcode_t *)addr;  .offset = 0;\n};\n\n// Runs BEFORE the probed instruction executes:\nstatic int handler_pre(struct kprobe *p, struct pt_regs *regs)\n{\n    pr_info(\"hit %s: ip=%px\\n\", p->symbol_name, (void *)regs->ip);\n    // On x86-64, syscall-ish args are in regs->di, si, dx, ... (arch-specific)\n    return 0;   // 0 = continue normally\n}\n\n// Runs AFTER the probed instruction single-steps:\nstatic void handler_post(struct kprobe *p, struct pt_regs *regs,\n                         unsigned long flags)\n{\n    /* ... */\n}\n\nstatic int __init my_init(void)\n{\n    kp.pre_handler  = handler_pre;\n    kp.post_handler = handler_post;\n    return register_kprobe(&kp);   // plant it\n}\nstatic void __exit my_exit(void) { unregister_kprobe(&kp); }\nmodule_init(my_init); module_exit(my_exit);\nMODULE_LICENSE(\"GPL\");</code></pre><h4>A kretprobe (return value + latency)</h4><pre><code>#include <linux/kprobes.h>\n#include <linux/ktime.h>\n\nstruct my_data { ktime_t entry; };   // per-call scratch (entry_handler stores it)\n\nstatic int entry_handler(struct kretprobe_instance *ri, struct pt_regs *regs)\n{\n    struct my_data *d = (struct my_data *)ri->data;\n    d->entry = ktime_get();\n    return 0;\n}\nstatic int ret_handler(struct kretprobe_instance *ri, struct pt_regs *regs)\n{\n    struct my_data *d = (struct my_data *)ri->data;\n    long retval = regs_return_value(regs);          // the function's return\n    s64 us = ktime_us_delta(ktime_get(), d->entry); // latency\n    pr_info(\"ret=%ld latency=%lld us\\n\", retval, us);\n    return 0;\n}\nstatic struct kretprobe rp = {\n    .kp.symbol_name = \"vfs_read\",\n    .entry_handler  = entry_handler,\n    .handler        = ret_handler,\n    .data_size      = sizeof(struct my_data),\n    .maxactive      = 20,          // max in-flight instances\n};\n// register_kretprobe(&rp);  /  unregister_kretprobe(&rp);</code></pre>"
    },
    {
      "title": "4. Handler Rules & Constraints",
      "content": "<p>Probe handlers run in a delicate context — deep inside the kernel, possibly with interrupts disabled. A few rules keep the system stable.</p><h4>What handlers may and may not do</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Rule</th><th style=\"text-align:left; padding:0.5rem;\">Why</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Must NOT sleep</td><td style=\"padding:0.5rem;\">Runs in atomic context (like an interrupt handler)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Keep it fast</td><td style=\"padding:0.5rem;\">It runs on the hot path of the probed code</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">No GFP_KERNEL alloc</td><td style=\"padding:0.5rem;\">Use GFP_ATOMIC if you must allocate</td></tr><tr><td style=\"padding:0.5rem;\">Reentrancy handled</td><td style=\"padding:0.5rem;\">kprobes guards against recursively probing itself</td></tr></table><h4>What you can't probe</h4><pre><code># Some code is off-limits (a probe there could deadlock or crash):\n#   - the kprobes machinery itself\n#   - functions marked __kprobes / NOKPROBE_SYMBOL()\n#   - some low-level entry/exception paths\n# register_kprobe() returns an error (e.g. -EINVAL) for a blacklisted addr.\ncat /sys/kernel/debug/kprobes/blacklist   # functions that can't be probed\ncat /sys/kernel/debug/kprobes/list        # currently installed kprobes</code></pre><h4>Reading arguments (architecture-specific)</h4><pre><code># pt_regs holds the CPU registers at the probe point. Argument locations are\n# ARCH-SPECIFIC (calling convention). Portable helpers exist:\n#   regs_get_kernel_argument(regs, n)   // nth function argument\n#   regs_return_value(regs)             // return value (in a kretprobe)\n#   kernel_stack_pointer(regs), instruction_pointer(regs)\n# Prefer these over raw regs->di/si so the code works across x86/ARM.</code></pre><h4>Symbols & addresses</h4><pre><code># kprobes resolves .symbol_name via kallsyms. Probe a specific point with an\n# offset from the symbol:\n#   kp.symbol_name = \"tcp_sendmsg\";  kp.offset = 0;   // function entry\n# Find symbols and check they're probeable:\nsudo grep tcp_sendmsg /proc/kallsyms\n# Note: many static/inlined functions aren't in kallsyms -> can't be probed\n# by name (they were inlined into the caller).</code></pre>"
    },
    {
      "title": "5. kprobes via perf & the CLI",
      "content": "<p>You rarely write a module. <strong><code>perf probe</code></strong> lets you create kprobes from the command line — including reading function arguments and return values by name — then record or trace them.</p><h4>perf probe</h4><pre><code># Add a kprobe on a function entry, capturing an argument by name:\nsudo perf probe --add 'do_sys_openat2 filename:string'\nsudo perf probe -l                       # list added probes\n\n# Add a RETURN probe capturing the return value:\nsudo perf probe 'do_sys_openat2%return $retval'\n\n# Record events from the probe, then report:\nsudo perf record -e probe:do_sys_openat2 -a sleep 5\nsudo perf script                         # per-event trace with the captured arg\n\n# Clean up:\nsudo perf probe --del 'do_sys_openat2*'</code></pre><h4>The flow: perf probe → kprobe → data</h4><pre><code>perf probe --add ...\n   │  parses DWARF debuginfo to find the address + how to read 'filename'\n   ▼\ncreates a KPROBE EVENT in the kernel (via the tracefs interface, section 6)\n   │\n   ▼\nperf record attaches to the event ─► each hit plants a sample\n   │\n   ▼\nperf script / perf report shows the captured arguments &amp; stacks</code></pre><h4>Argument access needs debuginfo</h4><pre><code># Reading args BY NAME (filename:string, $retval) needs kernel debuginfo\n# (DWARF) or BTF. Without it you can still probe by register:\nsudo perf probe 'tcp_sendmsg size=%dx'    # read a raw register\n# Distros ship debuginfo in a 'kernel-debuginfo' / '-dbgsym' package.</code></pre>"
    },
    {
      "title": "6. kprobe Events via tracefs (ftrace)",
      "content": "<p>The kernel exposes kprobes directly through <strong>tracefs</strong> as \"kprobe events\" — no module, no perf. You write a definition into a special file and read results from the trace buffer. (This is the ftrace integration; see the <strong>ftrace</strong> topic for the tracing infrastructure.)</p><h4>Creating a kprobe event</h4><pre><code>cd /sys/kernel/debug/tracing\n\n# Define a kprobe on do_sys_openat2, capturing the 2nd arg as a string:\necho 'p:myopen do_sys_openat2 filename=+0(%si):string' \\\n    | sudo tee kprobe_events\n#   p:          a kprobe (r: for a return probe)\n#   myopen      event name\n#   +0(%si):string   fetch a string from the address in register %si\n\n# Enable it and read the trace:\necho 1 | sudo tee events/kprobes/myopen/enable\ncat trace_pipe\n#   cat-1234 [002] .... open: (do_sys_openat2+0x0/0x...) filename=\"/etc/hosts\"\n\n# Disable and remove:\necho 0 | sudo tee events/kprobes/myopen/enable\necho '-:myopen' | sudo tee -a kprobe_events</code></pre><h4>A return probe capturing the return value</h4><pre><code>echo 'r:myread vfs_read $retval' | sudo tee -a kprobe_events\necho 1 | sudo tee events/kprobes/myread/enable\ncat trace_pipe        # shows vfs_read return values\n\n# List all defined kprobe events:\ncat kprobe_events</code></pre><h4>How the pieces relate</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Interface</th><th style=\"text-align:left; padding:0.5rem;\">Best for</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Kernel module</td><td style=\"padding:0.5rem;\">Full control / custom logic</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>perf probe</code></td><td style=\"padding:0.5rem;\">Ad-hoc probing with arg names + recording</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">tracefs kprobe_events</td><td style=\"padding:0.5rem;\">Scriptable, no tools beyond the shell</td></tr><tr><td style=\"padding:0.5rem;\"><strong>eBPF</strong> (BCC/bpftrace)</td><td style=\"padding:0.5rem;\">Safe, powerful in-kernel processing (usual choice)</td></tr></table>"
    },
    {
      "title": "7. kprobes, eBPF & Alternatives",
      "content": "<p>Today most kprobe use goes through <strong>eBPF</strong> — you attach a verified, sandboxed BPF program to a kprobe and do aggregation in the kernel. It's safer and more powerful than raw modules.</p><h4>eBPF on a kprobe (bpftrace one-liners)</h4><pre><code># Count calls to a kernel function:\nsudo bpftrace -e 'kprobe:tcp_sendmsg { @[comm] = count(); }'\n\n# Read an argument (arg1 = 2nd param) at entry:\nsudo bpftrace -e 'kprobe:do_sys_openat2 { printf(\"%s\\n\", str(arg1)); }'\n\n# Measure a function's latency with entry + return probes:\nsudo bpftrace -e '\n  kprobe:vfs_read { @s[tid] = nsecs; }\n  kretprobe:vfs_read /@s[tid]/ {\n    @us = hist((nsecs - @s[tid]) / 1000); delete(@s[tid]); }'\n# (See the eBPF topic for maps, verifier, and BCC/bpftrace.)</code></pre><h4>kprobe vs the alternatives</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Mechanism</th><th style=\"text-align:left; padding:0.5rem;\">Where it can attach</th><th style=\"text-align:left; padding:0.5rem;\">Overhead / stability</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><strong>kprobe</strong></td><td style=\"padding:0.5rem;\">Almost any instruction</td><td style=\"padding:0.5rem;\">Higher; can break if the function changes</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><strong>tracepoint</strong></td><td style=\"padding:0.5rem;\">Only where developers placed them</td><td style=\"padding:0.5rem;\">Low; STABLE API (preferred if one exists)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><strong>fprobe / ftrace</strong></td><td style=\"padding:0.5rem;\">Function entry/exit (via ftrace)</td><td style=\"padding:0.5rem;\">Low; cheaper than a trap-based kprobe</td></tr><tr><td style=\"padding:0.5rem;\"><strong>uprobe</strong></td><td style=\"padding:0.5rem;\">USER-space functions</td><td style=\"padding:0.5rem;\">The user-space analogue of kprobes</td></tr></table><h4>Practical guidance</h4><pre><code># Prefer a TRACEPOINT if one exists (stable, low overhead). Reach for a\n# KPROBE when you need a spot with no tracepoint. Do it through eBPF\n# (bpftrace/BCC) unless you specifically need a kernel module.\n#\n# Caveat: kprobes attach to INTERNAL functions -> not a stable ABI. A probe\n# on 'foo()' may break when the kernel renames/inlines/changes foo().</code></pre>"
    },
    {
      "title": "8. Debugging & References",
      "content": "<h4>Inspecting kprobes</h4><pre><code>cat /sys/kernel/debug/kprobes/list        # installed kprobes + addresses\ncat /sys/kernel/debug/kprobes/blacklist   # functions that can't be probed\ncat /sys/kernel/debug/kprobes/enabled     # global on/off (1/0)\n\ncat /proc/kallsyms | grep <func>          # find a symbol's address\ncat /sys/kernel/debug/tracing/kprobe_events   # tracefs-defined kprobe events\ndmesg | tail                              # pr_info output from module handlers</code></pre><h4>Config prerequisites</h4><pre><code># Needs these kernel configs (on by default in distro kernels):\n#   CONFIG_KPROBES=y\n#   CONFIG_KPROBE_EVENTS=y     (for tracefs kprobe_events)\n#   CONFIG_OPTPROBES=y         (jump optimization)\ngrep -E 'KPROBE|OPTPROBE' /boot/config-$(uname -r)</code></pre><h4>Common pitfalls</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Symptom</th><th style=\"text-align:left; padding:0.5rem;\">Cause</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>register_kprobe</code> returns -EINVAL</td><td style=\"padding:0.5rem;\">Symbol is blacklisted / not probeable</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Symbol not found</td><td style=\"padding:0.5rem;\">Function was inlined — not in kallsyms</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">System hang / crash in handler</td><td style=\"padding:0.5rem;\">Handler slept or did heavy work (must be atomic)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">kretprobe misses returns</td><td style=\"padding:0.5rem;\"><code>maxactive</code> too small for concurrency</td></tr><tr><td style=\"padding:0.5rem;\">Probe stops working after update</td><td style=\"padding:0.5rem;\">Kernel changed/inlined the target (not a stable ABI)</td></tr></table><h4>Key takeaways</h4><ul><li>kprobes plant a <strong>breakpoint</strong> (or optimized jump) at an address; your handler runs, the original instruction single-steps, execution resumes.</li><li><strong>kretprobe</strong> hijacks the return address to capture return values &amp; latency.</li><li>Handlers run in <strong>atomic context</strong> — fast, no sleeping.</li><li>Use them via <strong>eBPF/perf/tracefs</strong>, not usually a module; prefer a <strong>tracepoint</strong> when one exists.</li></ul><p><strong>References:</strong></p><ul><li><a href=\"https://docs.kernel.org/trace/kprobes.html\" target=\"_blank\">Linux Kernel: Kernel Probes (kprobes)</a></li><li><a href=\"https://docs.kernel.org/trace/kprobetrace.html\" target=\"_blank\">Linux Kernel: Kprobe-based Event Tracing</a></li><li><a href=\"https://docs.kernel.org/trace/fprobe.html\" target=\"_blank\">Linux Kernel: fprobe (ftrace-based function probe)</a></li><li><a href=\"https://lwn.net/Articles/132196/\" target=\"_blank\">LWN: An introduction to kprobes</a></li></ul>"
    }
  ]
};
