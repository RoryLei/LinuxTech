/**
 * Topic: Package Management
 */
const TOPIC_PACKAGES = {
  "id": "packages",
  "category": "core",
  "icon": "📦",
  "title": "Package Management",
  "description": "How Linux installs, updates and removes software — apt/dpkg, dnf/rpm, pacman, zypper, repositories & GPG trust, universal packages (snap/flatpak/AppImage), and building your own packages",
  "keywords": [
    "package", "package management", "apt", "dpkg", "dnf", "yum", "rpm",
    "pacman", "aur", "zypper", "snap", "flatpak", "appimage", "repository",
    "repo", "gpg", "signing", "keyring", "dependency", "dependencies",
    "install", "upgrade", "remove", "deb", "rpm package", "sources.list",
    "pinning", "hold", "unattended-upgrades", "checkinstall", "mirror"
  ],
  "sections": [
    {
      "title": "1. How Package Management Works",
      "content": "<p>A <strong>package</strong> bundles a piece of software together with everything needed to install it cleanly: the files, metadata (name, version, description), a list of <strong>dependencies</strong>, and optional install/remove scripts. A <strong>package manager</strong> installs packages, resolves their dependencies, tracks what's installed, and lets you upgrade or remove everything cleanly later.</p><h4>The two-layer model</h4><pre><code># Every ecosystem has TWO layers:\n#\n#   High-level manager   apt / dnf / pacman / zypper\n#     - talks to REPOSITORIES over the network\n#     - RESOLVES and downloads dependencies automatically\n#         │\n#         ▼\n#   Low-level tool       dpkg / rpm\n#     - installs a SINGLE local package file\n#     - does NOT fetch dependencies (just reports what's missing)\n#\n# You normally use the high-level tool; the low-level tool is for inspecting\n# or installing a downloaded .deb/.rpm directly.</code></pre><h4>Dependency resolution</h4><pre><code># \"Install nginx\" -> the manager reads nginx's dependency list, finds every\n# required library (and THEIR dependencies), computes a consistent set, and\n# installs them in the right order. This is why you use apt/dnf, not dpkg/rpm\n# directly -- manual dependency chasing is \"dependency hell\".</code></pre><h4>The package-manager families</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Family</th><th style=\"text-align:left; padding:0.5rem;\">Distros</th><th style=\"text-align:left; padding:0.5rem;\">High-level</th><th style=\"text-align:left; padding:0.5rem;\">Low-level / format</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Debian</td><td style=\"padding:0.5rem;\">Debian, Ubuntu, Mint</td><td style=\"padding:0.5rem;\"><code>apt</code></td><td style=\"padding:0.5rem;\"><code>dpkg</code> / <code>.deb</code></td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Red Hat</td><td style=\"padding:0.5rem;\">RHEL, Fedora, Rocky</td><td style=\"padding:0.5rem;\"><code>dnf</code> (was yum)</td><td style=\"padding:0.5rem;\"><code>rpm</code> / <code>.rpm</code></td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Arch</td><td style=\"padding:0.5rem;\">Arch, Manjaro</td><td style=\"padding:0.5rem;\"><code>pacman</code></td><td style=\"padding:0.5rem;\"><code>.pkg.tar.zst</code></td></tr><tr><td style=\"padding:0.5rem;\">SUSE</td><td style=\"padding:0.5rem;\">openSUSE, SLES</td><td style=\"padding:0.5rem;\"><code>zypper</code></td><td style=\"padding:0.5rem;\"><code>rpm</code> / <code>.rpm</code></td></tr></table>"
    },
    {
      "title": "2. Debian / Ubuntu — apt & dpkg",
      "content": "<h4>Everyday apt workflow</h4><pre><code>sudo apt update              # refresh the package index (do this first)\nsudo apt upgrade             # upgrade all installed packages\nsudo apt full-upgrade        # upgrade, allowing removals if needed\n\nsudo apt install nginx       # install (with dependencies)\nsudo apt install ./pkg.deb   # install a local .deb (resolves deps too)\nsudo apt remove nginx        # remove, keep config files\nsudo apt purge nginx         # remove AND delete config files\nsudo apt autoremove          # remove orphaned dependencies</code></pre><h4>Searching & inspecting</h4><pre><code>apt search keyword           # search names/descriptions\napt show nginx               # version, size, dependencies, description\napt list --installed         # everything installed\napt list --upgradable        # what has updates pending\napt-cache depends nginx      # what nginx depends on\napt-cache rdepends nginx     # what depends ON nginx</code></pre><h4>Pinning a version (prevent upgrades)</h4><pre><code>sudo apt-mark hold nginx     # freeze nginx at its current version\nsudo apt-mark unhold nginx   # allow upgrades again\napt-mark showhold            # list held packages</code></pre><h4>dpkg — the low-level tool</h4><pre><code>sudo dpkg -i package.deb     # install a local .deb (does NOT fetch deps)\nsudo apt -f install          # fix missing dependencies after a dpkg -i\n\ndpkg -l                      # list installed packages\ndpkg -L nginx                # list files installed BY nginx\ndpkg -S /usr/sbin/nginx      # which package owns this file?\ndpkg --configure -a          # finish configuring half-installed packages\ndpkg -s nginx                # status/details of an installed package</code></pre>"
    },
    {
      "title": "3. RHEL / Fedora — dnf & rpm",
      "content": "<h4>Everyday dnf workflow</h4><pre><code>sudo dnf install httpd       # install (dnf refreshes metadata as needed)\nsudo dnf upgrade             # upgrade everything (dnf update is an alias)\nsudo dnf remove httpd        # remove\nsudo dnf autoremove          # remove unused dependencies\n\ndnf search keyword           # search\ndnf info nginx               # package details\ndnf list installed           # installed packages\ndnf provides /usr/sbin/nginx # which package provides a file/command</code></pre><h4>Groups & history (a dnf superpower)</h4><pre><code>dnf group list               # available package groups\nsudo dnf group install \"Development Tools\"\n\ndnf history                  # every transaction, numbered\nsudo dnf history undo 42     # ROLL BACK transaction 42 (install/remove)\nsudo dnf history redo 42</code></pre><h4>rpm — the low-level tool</h4><pre><code>sudo rpm -i package.rpm      # install a local .rpm (no dependency fetch)\nsudo dnf install ./pkg.rpm   # better: installs the .rpm AND its deps\n\nrpm -qa                      # list all installed packages\nrpm -q nginx                 # is nginx installed? which version?\nrpm -ql nginx                # list files installed by nginx\nrpm -qf /usr/sbin/nginx      # which package owns this file?\nrpm -qi nginx                # detailed info\nrpm -V nginx                 # VERIFY files vs the package (detect tampering)\n\nsudo rpm --import KEY.gpg     # import a repo's signing key</code></pre>"
    },
    {
      "title": "4. Arch (pacman) & openSUSE (zypper)",
      "content": "<h4>Arch Linux — pacman</h4><pre><code>sudo pacman -Syu             # SYNC + refresh + UPGRADE everything (the Arch way)\nsudo pacman -S firefox       # install a package\nsudo pacman -R firefox       # remove\nsudo pacman -Rns firefox     # remove + its unused deps + config\n\npacman -Ss keyword           # search the repositories\npacman -Qs keyword           # search INSTALLED packages\npacman -Ql firefox           # list files owned by a package\npacman -Qo /usr/bin/firefox  # which package owns this file?\npacman -Qe                    # explicitly-installed packages\n\n# Note: Arch is a ROLLING release -- always use -Syu as a whole; never\n# refresh without upgrading (partial upgrades can break the system).</code></pre><h4>The AUR (Arch User Repository)</h4><pre><code># The AUR is a community repo of build recipes (PKGBUILDs), not binaries.\n# An \"AUR helper\" (e.g. yay) automates building + installing from source:\nyay -S some-aur-package      # download PKGBUILD, build, install\n# Always review a PKGBUILD before building -- it runs arbitrary commands.</code></pre><h4>openSUSE — zypper</h4><pre><code>sudo zypper refresh          # refresh repositories\nsudo zypper update           # apply updates\nsudo zypper install nginx    # install\nsudo zypper remove nginx     # remove\nzypper search keyword        # search\nzypper info nginx            # details\nzypper repos                 # list configured repositories</code></pre>"
    },
    {
      "title": "5. Repositories, GPG Keys & Trust",
      "content": "<p>Packages come from <strong>repositories</strong> — servers hosting packages plus a signed index. Packages are <strong>cryptographically signed</strong> so the manager can verify they came from the real source and weren't tampered with in transit.</p><h4>Where repos are configured</h4><pre><code># Debian/Ubuntu\n/etc/apt/sources.list                 # main list\n/etc/apt/sources.list.d/*.list        # extra repos (one file each)\n/etc/apt/sources.list.d/*.sources     # modern deb822 format\n\n# Fedora/RHEL\n/etc/yum.repos.d/*.repo               # each repo as a .repo file\n\n# Arch\n/etc/pacman.conf  +  /etc/pacman.d/mirrorlist</code></pre><h4>Adding a third-party apt repo (the modern, secure way)</h4><pre><code># 1. Download the repo's signing key into its OWN keyring (not apt-key,\n#    which is deprecated):\ncurl -fsSL https://example.com/key.gpg \\\n  | sudo gpg --dearmor -o /etc/apt/keyrings/example.gpg\n\n# 2. Add the repo, telling apt exactly which key signs it:\necho \"deb [signed-by=/etc/apt/keyrings/example.gpg] \\\n  https://example.com/apt stable main\" \\\n  | sudo tee /etc/apt/sources.list.d/example.list\n\nsudo apt update              # now apt trusts ONLY this key for this repo</code></pre><h4>Why signing matters</h4><pre><code># Without signature verification, a compromised mirror or a man-in-the-middle\n# could serve malicious packages. The manager checks the repo index signature\n# and each package's checksum before installing. A \"NO_PUBKEY\" or\n# \"signature verification failed\" error means the key is missing/untrusted --\n# import the correct key rather than disabling verification.\n\ngpg --dearmor        # convert an ASCII-armored key to a binary keyring\nrpm --import KEY     # trust a key on RPM systems</code></pre><h4>Pinning & priorities</h4><pre><code># APT pinning (/etc/apt/preferences.d/) controls which repo/version wins,\n# e.g. keep a package from 'stable' even when a newer one exists elsewhere.\n# Fedora uses 'priority=' in .repo files (with dnf-plugins-core).</code></pre>"
    },
    {
      "title": "6. Universal & Sandboxed Packages",
      "content": "<p>Native packages (deb/rpm) are distro-specific and share system libraries. <strong>Universal package formats</strong> bundle their dependencies and run across distros — trading disk space for portability and isolation.</p><h4>Snap</h4><pre><code>sudo snap install code --classic   # 'classic' = fewer sandbox restrictions\nsnap list                          # installed snaps\nsudo snap refresh                  # update all (snaps auto-update too)\nsudo snap remove code\nsnap info code                     # channels/versions</code></pre><h4>Flatpak</h4><pre><code>flatpak remote-add --if-not-exists flathub \\\n  https://flathub.org/repo/flathub.flatpakrepo\nflatpak install flathub org.gimp.GIMP\nflatpak list\nflatpak update\nflatpak uninstall org.gimp.GIMP\nflatpak run org.gimp.GIMP</code></pre><h4>AppImage</h4><pre><code># A single self-contained executable file -- no install, no package manager.\nchmod +x MyApp-x86_64.AppImage\n./MyApp-x86_64.AppImage            # just run it\n# Delete the file to \"uninstall\". Great for portable/try-it-out apps.</code></pre><h4>Comparison</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\"></th><th style=\"text-align:left; padding:0.5rem;\">Snap</th><th style=\"text-align:left; padding:0.5rem;\">Flatpak</th><th style=\"text-align:left; padding:0.5rem;\">AppImage</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Sandboxed</td><td style=\"padding:0.5rem;\">Yes</td><td style=\"padding:0.5rem;\">Yes</td><td style=\"padding:0.5rem;\">No (by default)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Install needed</td><td style=\"padding:0.5rem;\">Yes (snapd)</td><td style=\"padding:0.5rem;\">Yes (flatpak)</td><td style=\"padding:0.5rem;\">No — just run</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Auto-update</td><td style=\"padding:0.5rem;\">Yes</td><td style=\"padding:0.5rem;\">Manual/GUI</td><td style=\"padding:0.5rem;\">No</td></tr><tr><td style=\"padding:0.5rem;\">Focus</td><td style=\"padding:0.5rem;\">Desktop + server/CLI</td><td style=\"padding:0.5rem;\">Desktop apps</td><td style=\"padding:0.5rem;\">Portable single apps</td></tr></table><p><strong>When to use which:</strong> prefer native packages (integrated, small, fast) for system software and CLI tools; reach for a universal format when the app isn't packaged for your distro, needs a newer version than the repos offer, or benefits from sandboxing.</p>"
    },
    {
      "title": "7. Language & Container Package Managers",
      "content": "<p>System package managers install OS-level software. <strong>Language package managers</strong> install libraries for a specific programming ecosystem, and <strong>container images</strong> package an app with its whole userland. Knowing the boundaries avoids conflicts.</p><h4>Per-language managers</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Language</th><th style=\"text-align:left; padding:0.5rem;\">Manager</th><th style=\"text-align:left; padding:0.5rem;\">Installs into</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Python</td><td style=\"padding:0.5rem;\"><code>pip</code> (+ venv)</td><td style=\"padding:0.5rem;\">A virtual environment</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Node.js</td><td style=\"padding:0.5rem;\"><code>npm</code> / <code>pnpm</code> / <code>yarn</code></td><td style=\"padding:0.5rem;\"><code>node_modules</code></td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Rust</td><td style=\"padding:0.5rem;\"><code>cargo</code></td><td style=\"padding:0.5rem;\">Project / <code>~/.cargo</code></td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Go</td><td style=\"padding:0.5rem;\"><code>go install</code> / modules</td><td style=\"padding:0.5rem;\"><code>~/go</code></td></tr><tr><td style=\"padding:0.5rem;\">Ruby</td><td style=\"padding:0.5rem;\"><code>gem</code> / bundler</td><td style=\"padding:0.5rem;\">Project / system</td></tr></table><h4>The golden rule: don't fight the system manager</h4><pre><code># DON'T 'sudo pip install' into the system Python -- it can overwrite files\n# the OS package manager owns and break system tools. Instead:\npython3 -m venv .venv && . .venv/bin/activate   # isolated environment\npip install requests\n\n# Modern distros even block system-wide pip by default\n# (\"externally-managed-environment\") -- that's the OS protecting itself.</code></pre><h4>Containers as packaging</h4><pre><code># An OCI/Docker image bundles the app + its entire userland (libraries, even\n# a mini distro), so it runs identically anywhere -- the ultimate \"package\":\ndocker pull nginx:latest\ndocker run -p 8080:80 nginx\n# See the Docker topic for details. Use containers to ship apps; use the\n# system manager to build the base image and install OS packages inside it.</code></pre>"
    },
    {
      "title": "8. Building Packages & Best Practices",
      "content": "<h4>Turning software into a package</h4><pre><code># checkinstall: wrap 'make install' so it produces a .deb/.rpm you can\n# later remove cleanly (instead of scattering files untracked):\nsudo checkinstall            # run in place of 'sudo make install'\n\n# Proper Debian package: debian/ metadata + dpkg-buildpackage\ndpkg-buildpackage -us -uc    # build a .deb from a debian/ source tree\n\n# Proper RPM: a .spec file + rpmbuild\nrpmbuild -ba package.spec    # build source + binary RPMs from a spec</code></pre><h4>Keeping systems updated safely</h4><pre><code># Automatic security updates:\n#   Debian/Ubuntu:\nsudo apt install unattended-upgrades\nsudo dpkg-reconfigure unattended-upgrades\n#   Fedora/RHEL:\nsudo dnf install dnf-automatic && sudo systemctl enable --now dnf-automatic.timer\n\n# Clean up caches to reclaim disk:\nsudo apt clean          # remove downloaded .deb cache\nsudo apt autoremove     # drop orphaned dependencies\nsudo dnf clean all\nsudo pacman -Sc         # clean pacman cache</code></pre><h4>Troubleshooting</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Symptom</th><th style=\"text-align:left; padding:0.5rem;\">Fix</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Broken/half-installed (apt)</td><td style=\"padding:0.5rem;\"><code>sudo apt -f install</code>, <code>sudo dpkg --configure -a</code></td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">NO_PUBKEY / signature error</td><td style=\"padding:0.5rem;\">Import the repo's GPG key correctly</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Lock error (\"could not get lock\")</td><td style=\"padding:0.5rem;\">Wait for/stop the other apt/dnf process</td></tr><tr><td style=\"padding:0.5rem;\">Bad update (Fedora)</td><td style=\"padding:0.5rem;\"><code>dnf history undo &lt;id&gt;</code></td></tr></table><h4>Best-practice checklist</h4><ul><li><code>apt update</code> before <code>apt install/upgrade</code> so the index is fresh.</li><li>Prefer the <strong>distro repos</strong>; add third-party repos sparingly and always with a proper GPG key.</li><li>Never <code>sudo pip install</code> into system Python — use a virtualenv.</li><li>Enable <strong>automatic security updates</strong> on servers.</li><li>Use <code>hold</code>/pinning to freeze a version you must keep stable.</li></ul><p><strong>References:</strong></p><ul><li><a href=\"https://wiki.debian.org/PackageManagement\" target=\"_blank\">Debian: Package Management</a></li><li><a href=\"https://docs.fedoraproject.org/en-US/quick-docs/dnf/\" target=\"_blank\">Fedora: Using the DNF software package manager</a></li><li><a href=\"https://wiki.archlinux.org/title/Pacman\" target=\"_blank\">Arch Wiki: pacman</a></li><li><a href=\"https://docs.flatpak.org/en/latest/\" target=\"_blank\">Flatpak documentation</a></li><li><a href=\"https://snapcraft.io/docs\" target=\"_blank\">Snap documentation</a></li></ul>"
    }
  ]
};
