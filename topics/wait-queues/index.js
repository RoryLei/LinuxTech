/**
 * Topic: Wait Queues & Sleep/Wakeup
 */
const TOPIC_WAIT_QUEUES = {
  "id": "wait-queues",
  "category": "kernel",
  "icon": "😴",
  "title": "Wait Queues & Sleep/Wakeup",
  "description": "How the Linux kernel blocks and wakes tasks — task states, wait queues, wait_event/wake_up, the sleep/wakeup race and how the condition-recheck loop avoids it, plus completions and polling, with flow charts",
  "keywords": [
    "wait queue", "waitqueue", "sleep", "wakeup", "wake_up", "wait_event",
    "wait_event_interruptible", "task state", "TASK_RUNNING",
    "TASK_INTERRUPTIBLE", "TASK_UNINTERRUPTIBLE", "schedule", "blocking",
    "prepare_to_wait", "add_wait_queue", "wake_up_interruptible", "thundering herd",
    "wait_event_timeout", "completion", "poll", "condition", "runqueue",
    "process state", "D state", "signal", "ESTALE"
  ],
  "sections": [
    {
      "title": "1. Why Sleep? Task States",
      "content": "<p>A CPU is precious: a task waiting for something slow — disk I/O, a key press, a network packet, a lock — must not <em>busy-wait</em> and burn cycles. Instead it <strong>sleeps</strong>: it tells the scheduler \"take me off the CPU until my event happens,\" and is later <strong>woken</strong>. Wait queues are the kernel's mechanism for exactly this blocking-and-waking.</p><h4>The task states involved</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">State</th><th style=\"text-align:left; padding:0.5rem;\">Meaning</th><th style=\"text-align:left; padding:0.5rem;\">Woken by</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>TASK_RUNNING</code></td><td style=\"padding:0.5rem;\">On CPU or runnable (in the run queue)</td><td style=\"padding:0.5rem;\">—</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>TASK_INTERRUPTIBLE</code></td><td style=\"padding:0.5rem;\">Sleeping; can be woken by its event OR a signal</td><td style=\"padding:0.5rem;\">event or signal (S in ps)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>TASK_UNINTERRUPTIBLE</code></td><td style=\"padding:0.5rem;\">Sleeping; signals cannot wake it</td><td style=\"padding:0.5rem;\">only its event (D in ps)</td></tr><tr><td style=\"padding:0.5rem;\"><code>TASK_KILLABLE</code></td><td style=\"padding:0.5rem;\">Uninterruptible but fatal signals CAN wake it</td><td style=\"padding:0.5rem;\">event or SIGKILL</td></tr></table><h4>The sleep/run lifecycle</h4><pre><code>┌──────────────┐   waits for event    ┌────────────────────┐\n│ TASK_RUNNING │ ────────────────────► │ TASK_INTERRUPTIBLE │\n│ (on the CPU) │      (goes to sleep)  │  (on a wait queue) │\n└──────┬───────┘                       └─────────┬──────────┘\n       ▲                                          │\n       │        event happens / signal            │\n       │        wake_up() marks it runnable        │\n       └──────────────────────────────────────────┘\n              back to TASK_RUNNING (in run queue,\n              scheduler will put it back on a CPU)</code></pre><h4>Interruptible vs uninterruptible — the practical rule</h4><pre><code># TASK_INTERRUPTIBLE (the default choice): a signal (Ctrl-C, SIGTERM) can\n#   wake the task, so it stays responsive. USE THIS for normal waits.\n# TASK_UNINTERRUPTIBLE (D state): nothing but the event wakes it -- not even\n#   kill -9. Use ONLY for very short waits where a partial operation would\n#   corrupt state. A task stuck in D (e.g. dead NFS mount) can't be killed;\n#   that's why persistent 'D' processes are a red flag (see Process topic).\n# TASK_KILLABLE is the modern compromise: uninterruptible, but fatal signals\n#   still work -- fixes the 'unkillable D-state' problem for many waits.</code></pre>"
    },
    {
      "title": "2. What is a Wait Queue?",
      "content": "<p>A <strong>wait queue</strong> is a list of tasks all sleeping until the <em>same</em> event occurs. When the event happens, code calls <code>wake_up()</code> on that queue and the kernel makes the waiting task(s) runnable again. It's the rendezvous point between the sleeper and the waker.</p><h4>The two structures</h4><pre><code>#include <linux/wait.h>\n\n// The QUEUE HEAD -- one per event source (a device, a buffer, a flag):\nwait_queue_head_t my_wq;\ninit_waitqueue_head(&my_wq);\n// or statically:\nDECLARE_WAIT_QUEUE_HEAD(my_wq);\n\n// Each SLEEPING TASK is represented by a wait_queue_entry added to the head.\n// You rarely build these by hand -- the wait_event*() macros do it for you.</code></pre><h4>The relationship</h4><pre><code>  wait_queue_head  my_wq\n        │\n        ├──► entry (task A, TASK_INTERRUPTIBLE)   ┐\n        ├──► entry (task B, TASK_INTERRUPTIBLE)   ├ all sleeping for the\n        └──► entry (task C, TASK_INTERRUPTIBLE)   ┘ same condition\n\n  producer side:  data arrives ─► wake_up(&my_wq) ─► A, B, C become runnable\n#\n# One queue head, many waiters. The waker doesn't know or care WHO is\n# waiting -- it just signals the queue.</code></pre><h4>The two halves of the API</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Side</th><th style=\"text-align:left; padding:0.5rem;\">Does</th><th style=\"text-align:left; padding:0.5rem;\">Key calls</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><strong>Sleeper</strong> (consumer)</td><td style=\"padding:0.5rem;\">Blocks until a condition is true</td><td style=\"padding:0.5rem;\"><code>wait_event_*()</code></td></tr><tr><td style=\"padding:0.5rem;\"><strong>Waker</strong> (producer)</td><td style=\"padding:0.5rem;\">Sets the condition, wakes waiters</td><td style=\"padding:0.5rem;\"><code>wake_up*()</code></td></tr></table><p>The classic use is <strong>blocking I/O</strong>: a driver's <code>read()</code> sleeps on a wait queue until an interrupt handler receives data and wakes it. (See the Character/Serial/Input driver topics — they all use this.)</p>"
    },
    {
      "title": "3. The Sleeper: wait_event()",
      "content": "<p>The <code>wait_event*()</code> family is how a task sleeps until a <strong>condition</strong> becomes true. You pass a queue and a boolean expression; the macro blocks until the expression evaluates true, re-checking it on every wakeup.</p><h4>The variants</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Macro</th><th style=\"text-align:left; padding:0.5rem;\">Signals?</th><th style=\"text-align:left; padding:0.5rem;\">Returns</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>wait_event(wq, cond)</code></td><td style=\"padding:0.5rem;\">Uninterruptible</td><td style=\"padding:0.5rem;\">void (only the event wakes it)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>wait_event_interruptible</code></td><td style=\"padding:0.5rem;\">Interruptible</td><td style=\"padding:0.5rem;\">0, or <code>-ERESTARTSYS</code> if a signal</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>wait_event_timeout</code></td><td style=\"padding:0.5rem;\">Uninterruptible</td><td style=\"padding:0.5rem;\">remaining jiffies, or 0 on timeout</td></tr><tr><td style=\"padding:0.5rem;\"><code>wait_event_interruptible_timeout</code></td><td style=\"padding:0.5rem;\">Interruptible</td><td style=\"padding:0.5rem;\">jiffies left / 0 / <code>-ERESTARTSYS</code></td></tr></table><h4>A driver read that blocks until data</h4><pre><code>#include <linux/wait.h>\n\nstatic DECLARE_WAIT_QUEUE_HEAD(read_wq);\nstatic bool data_ready;      // the CONDITION (shared with the waker)\n\nstatic ssize_t my_read(struct file *f, char __user *buf, size_t n, loff_t *p)\n{\n    // Honor non-blocking mode:\n    if (!data_ready && (f->f_flags & O_NONBLOCK))\n        return -EAGAIN;\n\n    // Sleep until data_ready is true. Returns -ERESTARTSYS if a signal fires.\n    if (wait_event_interruptible(read_wq, data_ready))\n        return -ERESTARTSYS;    // let the syscall be restarted / interrupted\n\n    // Woken AND condition true: safe to read the data now.\n    /* copy_to_user(...) */\n    data_ready = false;\n    return len;\n}</code></pre><h4>What wait_event_interruptible expands to (the safe loop)</h4><pre><code>for (;;) {\n    prepare_to_wait(&wq, &entry, TASK_INTERRUPTIBLE);  // add to queue, set state\n    if (condition)          break;     // ← re-check BEFORE sleeping (crucial!)\n    if (signal_pending(current)) { ret = -ERESTARTSYS; break; }\n    schedule();             // give up the CPU -> sleep until woken\n}\nfinish_wait(&wq, &entry);   // remove from queue, back to TASK_RUNNING\n#\n# The macro hides all of this. The KEY detail is the condition is re-checked\n# inside the loop -- that's what defeats the sleep/wakeup race (section 5).</code></pre>"
    },
    {
      "title": "4. The Waker: wake_up()",
      "content": "<p>The other half: whoever makes the condition true (an interrupt handler, another thread, a timer) must set the condition <em>and</em> call <code>wake_up()</code> on the same queue.</p><h4>The wake_up variants</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Call</th><th style=\"text-align:left; padding:0.5rem;\">Wakes</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>wake_up(&wq)</code></td><td style=\"padding:0.5rem;\">All waiters in any state (matches <code>wait_event</code>)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>wake_up_interruptible(&wq)</code></td><td style=\"padding:0.5rem;\">Only <code>TASK_INTERRUPTIBLE</code> waiters</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\"><code>wake_up_all(&wq)</code></td><td style=\"padding:0.5rem;\">Every waiter (even exclusive ones)</td></tr><tr><td style=\"padding:0.5rem;\"><code>wake_up_interruptible_sync</code></td><td style=\"padding:0.5rem;\">Wake without preempting the waker immediately</td></tr></table><h4>The waker side (e.g. an interrupt handler)</h4><pre><code>static irqreturn_t my_isr(int irq, void *dev)\n{\n    /* ... read the data from the device into the buffer ... */\n\n    data_ready = true;               // 1. SET THE CONDITION first\n    wake_up_interruptible(&read_wq); // 2. THEN wake the sleepers\n    // Order matters: set the condition BEFORE waking, so a waiter that\n    // re-checks sees it true. (The barriers inside these calls make it safe.)\n    return IRQ_HANDLED;\n}</code></pre><h4>What wake_up does (flow)</h4><pre><code>wake_up_interruptible(&read_wq)\n   │\n   ▼\nwalk the queue's waiter list\n   │\n   ▼\nfor each matching waiter:\n     set its state to TASK_RUNNING\n     enqueue it on a CPU's RUN QUEUE (try_to_wake_up)\n   │\n   ▼\nscheduler will run it -> it returns from schedule() inside wait_event,\nre-checks the condition (now true) -> proceeds\n#\n# wake_up does NOT run the waiter immediately; it just makes it RUNNABLE.\n# The scheduler decides when it actually gets a CPU (see the EEVDF topic).</code></pre><h4>The golden ordering rule</h4><pre><code># WAKER:    set condition ─► wake_up()\n# SLEEPER:  (inside wait_event) re-check condition ─► sleep only if false\n#\n# If you wake WITHOUT setting the condition, the waiter re-checks, sees false,\n# and goes right back to sleep (a spurious wakeup -- harmless but wasteful).\n# If you set the condition but FORGET to wake, the sleeper never re-checks\n# and hangs FOREVER. Always do both, in that order.</code></pre>"
    },
    {
      "title": "5. The Sleep/Wakeup Race",
      "content": "<p>The whole design of wait queues exists to defeat one subtle bug: the <strong>lost wakeup</strong> race between checking the condition and going to sleep. Understanding it explains why the API looks the way it does.</p><h4>The race (naive, BROKEN approach)</h4><pre><code>// DON'T DO THIS -- a naive sleep has a fatal window:\nif (!condition)          // (A) check: condition is false\n    /* --- WAKER RUNS HERE on another CPU: sets condition=true, wakes queue,\n           but WE are not on the queue yet, so the wakeup is LOST --- */\n    sleep_on(&wq);       // (B) sleep... FOREVER. The event already passed.\n#\n# Between (A) and (B) the event can happen and the wakeup is missed.\n# Result: the task sleeps forever waiting for something that already occurred.</code></pre><h4>The fix: add-to-queue BEFORE the final check</h4><pre><code>CPU 0 (sleeper)                          CPU 1 (waker)\n──────────────                           ─────────────\nprepare_to_wait():\n   add self to wait queue\n   set state = TASK_INTERRUPTIBLE\n                                          condition = true\nre-check condition:                       wake_up(&wq):\n   already on the queue, so...              finds us on the queue,\n   if condition is TRUE -> DON'T sleep       sets us TASK_RUNNING\n   (or if wakeup set us RUNNING,\n    schedule() returns immediately)\n#\n# KEY: because we're ON THE QUEUE and set our state to sleeping BEFORE the\n# final condition check, either (a) we see the condition true and skip\n# sleeping, or (b) the waker's wake_up finds us and makes us runnable so\n# schedule() returns at once. The wakeup can NO LONGER be lost.</code></pre><h4>Why the loop (not just an if)</h4><pre><code>while (!condition) {          // a LOOP, not a single check\n    prepare_to_wait(...);\n    if (condition) break;\n    schedule();\n}\n#\n# Even after being woken, the condition might be false again by the time we\n# run (another task grabbed the data first = SPURIOUS/stolen wakeup). So we\n# RE-CHECK in a loop and sleep again if needed. Never assume \"woken == ready\".</code></pre><h4>The rule the API enforces for you</h4><pre><code># wait_event*() does prepare_to_wait + re-check + schedule + finish_wait in a\n# loop -- so you get race-free sleeping for free. This is why you should use\n# wait_event() and NEVER hand-roll 'if (!cond) schedule()'.</code></pre>"
    },
    {
      "title": "6. Completions & Exclusive Waits",
      "content": "<h4>Completions — the \"wait for a one-shot event\" helper</h4><pre><code>#include <linux/completion.h>\n\n// A completion is a wait queue + a flag, purpose-built for \"wait until this\n// one thing finishes\" (e.g. async init, a DMA transfer, a worker done).\nDECLARE_COMPLETION(dma_done);\n\n// Waiter:\nwait_for_completion(&dma_done);                 // blocks until complete()\n// or with a timeout / interruptible:\nwait_for_completion_interruptible_timeout(&dma_done, HZ);   // 1 second\n\n// Completer (e.g. the DMA-done interrupt):\ncomplete(&dma_done);        // wake ONE waiter\ncomplete_all(&dma_done);    // wake ALL waiters\n#\n# Prefer a completion over a hand-built wait_queue+flag for one-shot\n# \"done?\" signalling -- it's simpler and race-free by design.</code></pre><h4>completion vs wait queue</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Use</th><th style=\"text-align:left; padding:0.5rem;\">Choose</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">\"Wait until X finishes once\"</td><td style=\"padding:0.5rem;\">completion</td></tr><tr><td style=\"padding:0.5rem;\">\"Wait until a repeating condition holds\"</td><td style=\"padding:0.5rem;\">wait queue + <code>wait_event</code></td></tr></table><h4>The thundering herd &amp; exclusive waits</h4><pre><code># THUNDERING HERD: if 100 tasks sleep on one queue and wake_up() wakes them\n# ALL, but only ONE can consume the event (e.g. one accept()able connection),\n# 99 wake, re-check, find nothing, and sleep again -- wasted CPU + contention.\n#\n#   wake_up() ─► [T1 T2 T3 ... T100] all wake ─► only T1 gets it ─► 99 re-sleep\n#\n# FIX: EXCLUSIVE waiters. wait_event_interruptible_exclusive() adds the task\n# as 'exclusive'; wake_up() then wakes only ONE exclusive waiter (plus any\n# non-exclusive ones). Used by accept(), futex, etc. to wake just one server.\nwait_event_interruptible_exclusive(wq, condition);\nwake_up(&wq);        // wakes one exclusive waiter, not the whole herd</code></pre>"
    },
    {
      "title": "7. Waking user space: poll/select & epoll",
      "content": "<p>Wait queues are also how the kernel implements <strong>readiness notification</strong> for user space — <code>poll()</code>, <code>select()</code>, and <code>epoll</code>. A driver's <code>.poll</code> method registers the caller on its wait queue so the kernel knows to wake it when the fd becomes ready.</p><h4>A driver's poll method</h4><pre><code>#include <linux/poll.h>\n\nstatic __poll_t my_poll(struct file *f, struct poll_table_struct *pt)\n{\n    __poll_t mask = 0;\n\n    poll_wait(f, &read_wq, pt);        // register this fd's wait queue with poll\n    if (data_ready)\n        mask |= EPOLLIN | EPOLLRDNORM; // readable\n    if (space_available)\n        mask |= EPOLLOUT | EPOLLWRNORM;// writable\n    return mask;                        // report current readiness\n}\n// The SAME wake_up_interruptible(&read_wq) from the ISR also wakes any\n// poll()/select()/epoll_wait() blocked on this fd.</code></pre><h4>How poll uses the wait queue (flow)</h4><pre><code>user: epoll_wait() / poll() on the fd\n   │\n   ▼\nkernel calls the driver's .poll(): poll_wait() adds the caller to read_wq,\n   returns the current readiness mask\n   │\n   ├─ already ready?  return immediately\n   └─ not ready?      the caller SLEEPS on read_wq\n            │\n            ▼   (later) ISR: data_ready = true; wake_up_interruptible(&read_wq)\n            │\n            ▼\n   poll/epoll wakes, re-runs .poll(), sees EPOLLIN, returns the fd as ready\n#\n# One wake_up() serves blocking read(), poll(), select(), AND epoll at once --\n# that's the payoff of the wait-queue abstraction.</code></pre><h4>Relationship to the async I/O topics</h4><pre><code># poll/select/epoll are the readiness model built ON wait queues. For the\n# modern high-performance alternative (submission/completion rings) see the\n# io_uring topic; for the driver .poll fundamentals see the Character Drivers\n# topic. Wait queues sit UNDER all of them.</code></pre>"
    },
    {
      "title": "8. Debugging & References",
      "content": "<h4>Spotting sleep problems</h4><pre><code># A task stuck sleeping shows up in its state. Find who's blocked and WHERE:\nps -eo pid,stat,wchan:24,comm     # STAT (S/D), WCHAN = the kernel fn it sleeps in\n#   PID STAT WCHAN                    COMMAND\n#  1234 D    wait_event_interruptible myapp    ← blocked in a wait_event\n\ncat /proc/1234/stack              # kernel stack: exact sleep call site (root)\ncat /proc/1234/wchan              # the symbol it's sleeping in\n\n# Persistent 'D' (uninterruptible) tasks that never wake = a lost wakeup or a\n# dead resource (hung disk/NFS). See the Process Management topic's D-state.\necho w > /proc/sysrq-trigger      # dump the stacks of all BLOCKED tasks (root)\ndmesg                              # look for 'hung_task' warnings (120s+)</code></pre><h4>Common bugs</h4><table style=\"width:100%; border-collapse:collapse; margin:1rem 0;\"><tr style=\"border-bottom:1px solid #30363d;\"><th style=\"text-align:left; padding:0.5rem;\">Symptom</th><th style=\"text-align:left; padding:0.5rem;\">Cause</th></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Task sleeps forever</td><td style=\"padding:0.5rem;\">Set condition but forgot <code>wake_up()</code>; or hand-rolled sleep raced (lost wakeup)</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Unkillable D-state process</td><td style=\"padding:0.5rem;\">Used <code>TASK_UNINTERRUPTIBLE</code> on a long/blocked wait — use interruptible/killable</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">Wakes but data already gone</td><td style=\"padding:0.5rem;\">Checked with <code>if</code> not a loop — always re-check the condition</td></tr><tr style=\"border-bottom:1px solid #30363d;\"><td style=\"padding:0.5rem;\">High CPU on many sleepers</td><td style=\"padding:0.5rem;\">Thundering herd — use exclusive waits</td></tr><tr><td style=\"padding:0.5rem;\">\"scheduling while atomic\"</td><td style=\"padding:0.5rem;\">Called <code>wait_event</code>/<code>schedule</code> in atomic context (IRQ/spinlock held)</td></tr></table><h4>Golden rules</h4><pre><code># 1. You CANNOT sleep in atomic context (interrupt handler, holding a\n#    spinlock, preempt-disabled). wait_event()/schedule() there = bug.\n#    (Use a threaded IRQ / workqueue if you must sleep -- see those topics.)\n# 2. WAKER: set the condition, THEN wake_up().\n# 3. SLEEPER: use wait_event*() (never hand-roll) -- it re-checks in a loop.\n# 4. Prefer INTERRUPTIBLE so the task stays killable/responsive.</code></pre><h4>Key takeaways</h4><ul><li>A task <strong>sleeps</strong> (gives up the CPU) on a <strong>wait queue</strong> until an event, then is made runnable by <strong>wake_up()</strong>.</li><li><code>wait_event*()</code> re-checks a <strong>condition in a loop</strong> — this defeats the lost-wakeup race and spurious wakeups.</li><li>Waker rule: <strong>set the condition, then wake</strong>. Prefer interruptible waits (killable).</li><li><strong>Completions</strong> handle one-shot \"done?\" events; <strong>exclusive waits</strong> avoid the thundering herd; <strong>poll/epoll</strong> are built on wait queues.</li></ul><p><strong>References:</strong></p><ul><li><a href=\"https://docs.kernel.org/driver-api/basics.html\" target=\"_blank\">Linux Kernel: Wait queues &amp; Wake events (driver API)</a></li><li><a href=\"https://www.kernel.org/doc/html/latest/scheduler/completion.html\" target=\"_blank\">Linux Kernel: Completions - \"wait for completion\" barrier APIs</a></li><li><a href=\"https://docs.kernel.org/core-api/wrappers/memory-barriers.html\" target=\"_blank\">Linux Kernel: Memory barriers (sleep/wakeup ordering)</a></li><li><a href=\"https://lwn.net/Articles/577370/\" target=\"_blank\">LWN: The wait/wound mutex &amp; wait-queue background</a></li><li><a href=\"https://lwn.net/Articles/288056/\" target=\"_blank\">LWN: TASK_KILLABLE</a></li></ul>"
    }
  ]
};
